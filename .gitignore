# === 機密來源素材（只留本機，絕不推送）===
# reference/           : Secure-Development Guidance + DoD checklist，衍生自
#                        公司自身的 Qualys/Rapid7 掃描結果
# docs/reference/      : RCI 公司程序、Risk Management Report、
#                        APAC ISMS profile、授權的 ISO 27001/27002 PDF
# docs/02-architecture/ 的 00-16 已承載建置所需的一切；這些原始檔只存在本機磁碟。
/reference/
/docs/reference/

# === 機密 ===
# 絕不 commit。若不慎 commit：**立即輪替，不要只是 git rm** ——
# git history 裡還在，而且可能已經被 clone。force-push 也不夠：
# GitHub 會保留 unreachable object，仍可用舊 SHA 取回（本專案 2026-08-07 實測過，
# 最後是刪除並重建 repo 才真正清除）。見 docs/rules-on-demand/release-process.md §Secret 管理
.env
.env.*
!.env.example
*.pem
*.key
*.pfx
*.p12
*.jks
*.keystore
secrets.*
secrets.json
credentials
credentials.json
appsettings.*.local.json
.npmrc
.netrc

# === 相依與建置產物 ===
node_modules/
dist/
build/
out/
.next/
.nuxt/
.turbo/
coverage/
*.tsbuildinfo
__pycache__/
*.py[cod]
.venv/
venv/
target/
bin/
obj/

# === 工具快取 ===
.cache/
.parcel-cache/
.eslintcache
.pytest_cache/
.mypy_cache/
.ruff_cache/

# === 流程工具產物 ===
*.new
# ↑ init_template.py --merge 產生的衝突檔。merge 完就刪，不要 commit
.playwright-mcp/
# ↑ 瀏覽器驅動時的暫存 snapshot / console log。drive-through 的**證據**要留，
#   但留的是 W{NN}-*/artifacts/ 底下經過挑選的截圖，不是每次呼叫的原始輸出。

# === 編輯器 ===
.vscode/*
!.vscode/settings.json.example
!.vscode/extensions.json
.idea/
*.swp
*.swo
*~

# === OS ===
Thumbs.db
ehthumbs.db
Desktop.ini
.DS_Store

# === Office 文件鎖定檔 ===
~$*.docx
~$*.xlsx
~$*.pptx

# === 日誌與本機暫存 ===
*.log
npm-debug.log*
yarn-error.log*
pnpm-debug.log*
tmp/
scratch/

# === 技術棧專屬（ADR-0001：NestJS + Prisma monorepo；填於 W01）===
# Prisma 產生的 client。生成物不進版控 —— `npm run prisma:generate` 隨時可重建，
# 而把它 commit 進去會讓 schema 與 client 有兩個真相來源。
apps/api/src/generated/
# Next.js 型別產物
next-env.d.ts
.next/types/

# === 刻意「不」忽略的（這些是流程的一部分，必須進 git）===
# .claude/rules/     — 規則是共用的真相，每個 session 的 AI 依據同一份
# memory/            — 教訓要共享，否則每次 session 各自漂移
# docs/              — 執行紀錄與決策的單一來源
# 見 docs/rules-on-demand/collaboration.md §AI 輔助團隊的特有問題
