# Release vX.Y.Z — <一句話：這一版的重點>

> **用於**：對外發布的 release notes。
> 位置：GitHub Release 描述 / `docs/releases/vX.Y.Z.md`（若你要留檔）
>
> **寫給使用者看，不是寫給自己看。** 每一條都要能回答「這對我有什麼影響」。
> 複製時刪掉這個 blockquote。

**Date**: YYYY-MM-DD
**Tag**: `vX.Y.Z`
**Commit**: `<sha>`
**Previous**: `vX.Y.Z-1`

---

## ⚠️ Breaking Changes

<沒有就寫「無」。有的話**放最上面**，不要藏在下面。>

### <變更標題>

**影響誰**：<哪些使用者 / 哪些呼叫方式會受影響>

**原本**：
```
<舊的用法 / 行為>
```

**現在**：
```
<新的用法 / 行為>
```

**怎麼遷移**：
1. <具體步驟>

**為什麼要這樣改**：<1-2 句。沒有理由的 breaking change 會消耗使用者的信任>

---

## Added

- <使用者視角的描述>（#PR）

## Changed

- <什麼改了、使用者會注意到什麼>（#PR）

## Fixed

- <修了什麼問題、什麼情況下會遇到>（BUG-NNN, #PR）

## Deprecated

- <標記為過期的東西> —— <會在哪一版移除> —— <改用什麼>

## Removed

- <這一版移除的東西>（上一版已 deprecated）

---

## 升級說明

### 前置需求

- [ ] <新的最低版本需求 / 新的依賴>

### 設定變更

| 環境變數 | 動作 | 預設值 | 說明 |
|---------|------|-------|------|
| `<NAME>` | 新增 / 變更 / 移除 | `<value>` | <用途> |

### Migration

- **需要 migration**: yes / no
- **可逆**: yes / no ⚠️（不可逆的話**明確標示無法單純回滾**）
- **預估時間**: <對多大的資料量>
- **停機需求**: 需要 / 不需要

```bash
<migration 指令>
```

### 升級步驟

1. <步驟>

---

## 已知問題

<這一版已知但沒修的。誠實列出比讓使用者自己踩到好。>

- <問題> —— <暫時的 workaround> —— <預計哪一版修>

---

## 驗證

<這一版怎麼被驗證的。給使用者信心，也是給自己的紀錄。>

- Gate: <lint / typecheck / test 數字>
- Staging: <部署後實際驅動了哪些路徑>
- **Drive-through**: <主要新功能實際用過 —— 不是只看 health check>

---

## Rollback

- **怎麼回**: <重新部署 `vX.Y.Z-1` / 藍綠切換 / feature flag>
- **資料相容性**: <這一版寫的資料，舊版讀得懂嗎>
- **不可逆的部分**: <若有，明確列出>

---

**完整變更**: <compare 連結 vX.Y.Z-1...vX.Y.Z>
