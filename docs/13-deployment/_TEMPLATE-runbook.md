# Deploy Runbook — {environment}

> ✅ **這是 as-built 版本**（照著做真的會成功），不是原始藍圖。
> 藍圖與決策理由在 `../14-adr/`。

**最後成功執行**: YYYY-MM-DD · **執行者**: {name}

## 前置條件

- [ ] {權限 / 工具 / 網路}

## 已知的環境限制

<!-- 這一段是 runbook 最有價值的部分：實際部署常與藍圖有出入
     （proxy 擋住某條路、權限拿不到、SDK 行為不同）。寫在這裡，
     下一個人不必再撞一次。 -->

| 限制 | 影響 | 繞法 |
|---|---|---|
| | | |

## 步驟

1. **{階段}**
   ```bash
   {指令}
   ```
   **預期**：{輸出}
   **失敗的話**：{怎麼判斷 + 怎麼回退}

## 上線後 smoke test

```bash
{指令}
```
預期：{結果}

## 回退

```bash
{回退指令}
```
**回退的判準**：{什麼情況該回退，不要臨場決定}
