# Session Summary — APAC ISMS Governance Platform

> Copy 成 `SESSION_SUMMARY.md`（同目錄）。**`scripts/session_bootstrap.py` 會自動注入它**
> —— SessionStart hook 預設已接好（`.claude/settings.json`）。
> **保持 slim** —— 它每次都要付 context 成本，超過一頁就是放錯東西了（腳本會在 4000 字元截斷）。
>
> **不用寫「現在在哪個 phase」的判斷邏輯** —— bootstrap 會從各 phase 的 `status:`
> frontmatter 自動偵測，比人手維護的一行可靠。這裡只寫**推導不出來**的東西。
>
> **維護時機**：每個 phase closeout 的 doc-sync 步驟順手更新。

**Updated**: YYYY-MM-DD

## 現在在做什麼
`W{NN}-{slug}` — {一句話}。狀態：{Day N / 卡在什麼}。

## 最近三次交付
| | 一句話 |
|---|---|
| `W{NN}` | |
| `W{NN-1}` | |
| `W{NN-2}` | |

## 現在不要碰的
- {正在被別的分支改的 / 已凍結的}

## 待答問題
- {見 docs/decision-form.md}
