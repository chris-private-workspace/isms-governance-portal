# Skill: {name}

**Purpose**: {這個 skill 一次跑完哪一組檢查}
**Trigger**: {什麼時候該叫它 —— commit 前 / PR 前 / 特定類型的改動}
**實作位置**: `.claude/skills/{name}/` （若有）

## 為什麼需要這個 skill

{這組檢查在專案裡反覆被漏掉的證據。沒有證據就不該有 skill ——
 那只是增加 ceremony。}

## 檢查項

| # | 檢查 | 怎麼查 | 失敗代表什麼 |
|---|---|---|---|
| 1 | | `{指令}` 或「讀 X 確認 Y」 | |

## 能機械化的部分

{哪些項已經寫成 lint（`scripts/lint/`）。skill 只該保留無法機械化的部分 ——
 能寫成 lint 的留在 skill 裡，是把機械工作交給人做。}

## 已知的漏網

{這個 skill 抓不到什麼。誠實寫出來，避免有人以為跑完就安全了。}
