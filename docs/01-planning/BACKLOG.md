# BACKLOG — 待辦 / 未決事項單一來源

**Purpose**: 所有 carryover AD、pending 決策、下個 phase 候選的**唯一權威清單**。

**Created**: 2026-08-07
**Last Modified**: 2026-08-16
**Status**: Active

> ⚠️ **這是待辦事項的單一來源。**
> 待辦**不可以**出現在 `CLAUDE.md` 的表格格、`MEMORY.md` 條目、或散落在各個 retrospective 裡。
> 那正是導航檔膨脹的路徑。

> 🔎 **最新跨來源審計：2026-08-15（#6，`c403522`）—— 6 條漂移（AD-21 ~ AD-26），
> 其中 4 條的矛盾兩半住在同一列或同一段裡**（見 [`STATUS_AUDIT.md`](./STATUS_AUDIT.md) §2.7）。
> ⏳ **未處置** —— 審計只負責揪出。
> ⛔ **審計 #5 的四條只處置了一條**（AD-20，且是被 W14 closeout 順手改對的）——
> AD-17 / AD-18 / AD-19 已跨 W13 + W14 兩個 phase。
>
> 前次：2026-08-14（#5，`91dd1cb`）—— 4 條漂移（AD-17 ~ AD-20），
> 其中 2 條由當日的 W12 closeout 造成、2 條是「條件成立了而沒有人回頭翻」。
> ⭐ 該次首度不必手數 BACKLOG（`check_backlog_counts.py` 導出 100 / 8 / 56 / 36）。
>
> 前次：2026-08-12（#4，`d7733b5`）—— 5 條漂移（AD-12 ~ AD-16），
> 其中 3 條是「計數器沒有機械推導」**（見 [`STATUS_AUDIT.md`](./STATUS_AUDIT.md) §2.7）。
> ⏳ **未處置** —— 審計只負責揪出，修是另一件事。
> ⚠️ **該次原標 `#3`，2026-08-13 改號** —— 真正的 #3（2026-08-10，W04）從未進 main，
> 已撿回並登記，兩次的 `AD-12`–`AD-14` 是**不同的事**（見 §3.1）。
> 前次：2026-08-10（#2，AD-7 ~ AD-11）**✅ 同日全數修正，審計 #4 複驗未再漂移**。

> **本檔回答「有什麼工作」，不回答「先做哪個」。**
> 順序層 [`ROADMAP.md`](./ROADMAP.md) **已於 2026-08-10 啟用**（CH-016）——
> 啟用當時本檔 §Open 達 48 條，超過「讀完仍不知道下一步」的門檻
> （**現為 123 條 —— P0 7 / P1 69 / P2 47**，⛔ **不要手數也不要 grep** ——
> 2026-08-16 **W17 post-merge**：新增 1 條（`AD-RebaseStaleShaRef-1` 🟡 ——
> rebase merge 改寫**全部**六個 SHA，closeout 文件的 commit 錨點在 `main` 上不存在，
> **而舊 SHA 在本機仍可解析 ⇒ 所有本地驗證都是偽陰性**；W16 的 `020fe11` 同形狀未修）。
> ⭐ detector 報 total 122→123 / P1 68→69，照抄。
> 2026-08-16 **W17 closeout**：新增 5 條（`AD-LegalHoldScopeRefUnguarded-1` 🟡
> **W14 的多型守衛結構上不可用**：`::uuid` cast 早於 mapping walk 而 `class` 不是 uuid ·
> `AD-SpecFieldValueSourceUnverified-1` 🟡 **Day-0 能證明欄位名對、不能證明欄位填得出來** ·
> `AD-RetentionDurationUnstructured-1` 🟢 · `AD-CatalogAssertionPredictionBlindSpot-1` 🟢 ·
> `AD-GuardMatchesItsOwnDisclaimer-1` 🟢 **斷言「某構造不存在」時命中了說明它不存在的註解**）、
> **更新 1 條**（`AD-DevDbChecksumDrift-1` —— ⭐ `migrate deploy` 一次把 `isms_dev` 補到 24/24，
> 六次繞開沒有人試過它 ⇒ 該條射程比標題窄，降級為記錄）。
> ⭐ detector 報 total 117→122 / P1 66→68 / P2 44→47，照抄。
> 2026-08-16 **W16 post-merge**：新增 1 條（`AD-MergeLabelRuleConflict-1` 🟢 ——
> `task-workflow` 禁止 CLAUDE.md 表格格放 PR/SHA，`git-workflow` 要求放，
> **而衝突只有在 byte 預算恰好卡住時才現形**：翻標籤使 CLAUDE.md 30,003 > 30,000 而 lint fail）。
> ⭐ detector 報 total 116→117 / P2 43→44，照抄。
> 2026-08-16 **W16 closeout**：新增 5 條（`AD-IsmsProfileSpecGaps-1` 🟡 · `AD-IntSuiteNoMutex-1` 🟡 ·
> `AD-NeutralisationCountUnderPredicted-1` 🟡 · `AD-IsActiveNeverBuilt-1` 🟢 **`02a` §1.1 的 base field 有一項零張表實作** ·
> `AD-SchemaTemporalConventionSplit-1` 🟢）、**關閉 1 條**（`AD-DesignAlign-7` —— 使用者裁定保留三個
> certifier / reply 欄）、更新 5 條（`AD-DevDbChecksumDrift-1` **第 5 次，且五個 phase 來首次拿到真實數字**：
> `isms_dev` 17/22、head 自 W10 未動 · `AD-UniqueKeyOracle-1` **第 3 個資料點，且是第一個「正面」的** ——
> 前兩次是發現 oracle 並移除，本片在建表前套判準並用 N2b 證明拿掉它 oracle 就出現 ·
> `AD-SchemaMigrationDrift-1` **+2 個具體實例** · `AD-PartialGateReportedAsFull-1` **第 4 次而形狀是新的** ——
> gate 有跑、回報正確，而 commit 用 `;` 串接所以不受它 gate · `AD-SchemaHeaderStale-1` **數字改為自我可重現**）。
> ⭐ **這四個數字是 detector 印出來的** —— 它報 total 112→116 / P1 63→66 / P2 42→43，照抄。
> 2026-08-16 **W15 closeout**：新增 2 條（`AD-RegulationVersionCollision-1` 🟢 **同形狀第 2 次**，
> `Policy` 是第 1 次且無人記錄 · `AD-CalibrationNoTimeRecord-1` 🟡 **執行發生了而追蹤文件一格沒動** ——
> progress 零時間記錄 + checklist 21 項未勾，⇒ calibration 分子事後反推、量法事後選，**1.235 是下限**）、
> 關閉 0 條。⭐ **這四個數字是 detector 印出來的** —— 它報 total 110→112 / P1 62→63 / P2 41→42，照抄。
> 2026-08-16 **W15 外部審查**（三個平行 review agent 分正確性／範疇隔離＋稽核／測試證明力三個角度，逐條複驗後採納）：
> 新增 3 條（`AD-W15ConstraintSurfaceUntested-1` 🟡 · `AD-EntityScopeNoDriftGuard-1` 🟡 · `AD-W15InvariantInCommentOnly-1` 🟢）、
> 關閉 0 條、更新 4 條（`AD-SchemaMigrationDrift-1` **首個真實實例，已修，並成為該條 detector 的現成測試向量** ·
> `AD-PrismaEnumThreeTruths-1` **留下的未解問題被實測回答 ⇒ 兩個候選解法砍掉一個** ·
> `AD-TestNameWiderThanProof-1` **3/3，依強度階梯應升級為結構性解法** · `AD-SchemaHeaderStale-1` **第 3 次且射程比原描述更寬**）。
> ⭐ **這四個數字是 detector 印出來的** —— 我先改表、跑它，它報 total 107→110 / P1 60→62 / P2 40→41，照抄。
> 2026-08-15 **CH-032**：關閉 `AD-TrivyExempt-1`（🟡 P1）—— runtime base 移到 debian13，
> 六條 `libssl3` 豁免的標的不再存在於出貨映像中，⏰ **本 repo 唯一有日期的死線因此消失**。
> ⭐ **這三個數字是 detector 印出來的** —— 我先移除該列、跑它、它報 total 108→107 / P1 61→60，照抄。
> 其前 108 條，2026-08-15 W14 closeout：新增 2 條（`AD-PrismaEnumThreeTruths-1` 🟡 · `AD-TextEditStructuralScope-1` 🟡）、
> 關閉 0 條、更新 4 條（`AD-CalibrationWindowCrossSession-1` **首個實測但只證明一半，不關閉** ·
> `AD-AuditWriteOpsUntested-1` 16 個寫入仍全是 create · `AD-ModuleCoverageDilution-1` **再現但成因不同** ·
> `AD-DevDbChecksumDrift-1` **第三次撞上、第三次繞開**）。
> ⭐ **這四個數字是 detector 印出來的** —— 我先改表格、跑它、它報 total 106→108 / P1 59→61，照抄。
> 其前 106 條，2026-08-15 W14 Day 1：新增 1 條（`AD-PolicyAttestationFlag-1` 🟢）——
> plan 的 US-3 在 Day 1 撞到 AP-3，使用者裁定移出本片 → M6（detector 報 total 105→106 / P2 39→40）。
> 跑 `python scripts/lint/check_backlog_counts.py`，不符即 fail 並印出兩邊的值與差額。
> 2026-08-14 W12 closeout：新增 5 條（`AD-AuditCoverageOneTable-1` 🔴 · `AD-DeferralUnwatched-1` ·
> `AD-VacuousScopeTest-1` · `AD-BenchOrderBias-1` · `AD-StrategyBSunset-1`）、關閉 0 條、
> **升級 1 條**（`AD-RiskTableCountManual-1` P2 → P1 —— 它第一次被實地擊中，且錯的數字已污染三份文件）、
> 更新 6 條（`AD-BorrowedRefusal-1` **第 7 次且首次得到答案** · `AD-MetaVerificationBug-1` **3/3 可回流** ·
> `AD-EstimateAsMeasurement-1` **首次執行其提議的修法** · `AD-NarrowPatternWideClaim-1` 再現 3 次 ·
> `AD-DualLayerHighRisk-1` 標的已存在 · `AD-DevDbChecksumDrift-1` 確認仍在）。
> ⛔ **關掉的是 OQ-4 不是任何 AD** —— 它在 `decision-form.md`，不佔本表的計數。
> 其前 95 條，2026-08-14 W11 closeout：新增 4 條（`AD-NarrowPatternWideClaim-1` 🔴 · `AD-PolicyClaimUnmeasured-1` ·
> `AD-DevDbChecksumDrift-1` · `AD-ModuleFileZeroCoverage-1`）、關閉 0 條、更新 5 條
> （`AD-UniqueKeyOracle-1` 第 2 個資料點且**判準描述太窄** · `AD-BorrowedRefusal-1` **第 6 次且判準首次不可滿足** ·
> `AD-CalibrationDay0InOrOut-1` 第 3 個模糊處 · `AD-PartialGateReportedAsFull-1` 第 3 次但成因不同 ·
> `AD-SchemaMigrationDrift-1` **其提議的現成解法被實測投下疑慮**）。
> ⭐ **這四個數字是 detector 印出來的**，我沒有手數 —— 它報 total +4 / P0 +1 / P1 +2 / P2 +1，照抄。
> 其前 91 條，2026-08-14 CH-027：關閉 `AD-CountBeforeLastEdit-1`、新增 `AD-TemplateStatusValue-1`
> —— **一出一進，總數不變而優先度移動一格**。⭐ **這一行是照 detector 的輸出改的** ——
> 表格改完後它回報 P1 −1 / P2 +1，那是它第一次在真實情境下擋住東西，而不是在 fixture 上。
> 其前 91 條，2026-08-13 W10 closeout：新增 5 條（`AD-UniqueKeyOracle-1` 🔴 · `AD-CalibrationDay0InOrOut-1` ·
> `AD-ImmutableRowRetention-1` · `AD-SchemaHeaderStale-1` · `AD-PartialGateReportedAsFull-1`）、關閉 0 條、更新 3 條
> （`AD-BorrowedRefusal-1` 第 5 次 · `AD-ReturningMasksCheck-1` **再次被踩** ·
> `AD-BottomUpBlueprint-1` 第 2 點且**方向與 W09 相反**）。計數以 §Open **逐列解析**取得。
> 2026-08-13 CH-025：關閉 `AD-AssessmentProcessSubject-1`（使用者裁決補 `process`），
> 留下通則 `AD-SpecMergeFieldByField-1`（P2）—— **一出一進，總數不變而優先度移動一格**。
> 其前 86 條，2026-08-13 W09 closeout：新增 6 條（含已關閉的 `AD-AssessmentProcessSubject-1` ·
> `AD-NeutraliseRebuiltState-1` · `AD-AssessmentDefinitionUnvalidated-1` ·
> `AD-AssessmentQuestionNoFk-1` · `AD-ResponseRefCodeCost-1` · `AD-VendorAuditorSod-1`）、
> 更新 3 條（`AD-TestNameWiderThanProof-1` 2/3 · `AD-CalibrationIdleGap-1` 1/3 **且其提議被推翻** ·
> `AD-BottomUpBlueprint-1` 1/3 **且新估法首次對照成功**）、關閉 0 條。
> 其前 80 條，2026-08-13 分支清理：新增 `AD-UnpushedWorkInvisible-1` —— 遠端 25 → 1、本機 3 → 1，
> `deleteBranchOnMerge` 翻為 `true`，**但它管不到從未 push 的分支**，而那正是本次弄丟審計的機制。
> 其前 79 條，2026-08-13 審計 #3 撿回：關閉 `AD-LostAudit20260810-1`（使用者裁決撿回，見
> [`STATUS_AUDIT.md`](./STATUS_AUDIT.md) §3.1）、新增 `AD-RiskTableCountManual-1`
> —— **一出一進，總數不變而優先度移動一格**。
> 其前 79 條，2026-08-13 W08 post-merge：撿回 `AD-RebaseMergeBranchCheck-1`（寫於 W03，
> 承載它的分支從未被 merge，清理本機分支時才發現 —— **而發現它的方法正是它自己主張的那個**）
> + 新增 `AD-CountBeforeLastEdit-1`。
> ⛔ **W08 closeout 寫的「75 條」是錯的，真值 76** —— 兩種數法對照做在**加入第 6 條之前**，
> 之後沒有重數；錯誤數字已進 `74d8d56`、checklist 與 PR #47 描述 → `AD-CountBeforeLastEdit-1`。
> 其前（W08 closeout，實為 76 條）：新增 5 條
> （`AD-IssueBareEnum-1` · `AD-MigrationTimestampTz-1` · `AD-ModuleCoverageDilution-1` ·
> `AD-TestNameWiderThanProof-1` · `AD-MetaVerificationBug-1`）、關閉 2 條
> （`AD-EntityCountDerivation-1` · `AD-EntityIndexIncomplete-1`，由 `check_entity_index.py` 取代）。
> ⚠️ **數法 —— 不要用手數，也不要用 grep**：跑 `python scripts/lint/check_backlog_counts.py`
> （CH-027；`run_all` 8 個 detector 之一，所以 CI 每次都會跑）。它比對本檔開頭宣告的四個數字
> 與 §Open **逐列解析**的真值，不符即 fail，並把兩邊的值與差額都印出來。
> ⛔ **仍然必須在最後一次編輯之後重跑** —— detector 擋的是「宣告與表格不符」，
> 它擋不住「你改完表格就不跑了」。差別在於**現在有東西會替你叫**。
> ⛔ **2026-08-13 實測：用 grep 數優先度標記回 `0 / 0 / 0`（真值 5 / 48 / 26）** ——
> emoji 在該 pipeline 下不匹配，與審計 #3 的 `AD-14@#3` 同一個機制
> （`AD-GrepAssertion-1`，**在計數這件事上的第 3 次**）。
> ⚠️ **本段刻意不寫出被計數的字面樣式** —— 第一版寫了，於是這段說明自己被計進去三次，
> 讓總和多 3 而看起來像數錯（`AD-CountBeforeLastEdit-1` 的第二個實例，同一天）。
> ⛔ **2026-08-14（CH-027）枚舉又推翻兩件事**：優先度儲存格有 **3 種**寫法（不是 1 種 ——
> 嚴格 pattern 只中 88 / 91，**差額本身就是異格式清單**），且有儲存格內含**裸 pipe**
> 使固定欄位索引錯位。兩者都會**靜默**給出錯的答案，所以 detector 對無法解析的儲存格
> **一律 fail、絕不 skip**。
> ✅ **會 fail 的檢查已交付**（原排 W09 Day 0，漏了兩次，2026-08-14 改由獨立 CH 關掉）。
> 其前為 72 條，2026-08-12 審計 #4 後：新增 3 條
> （`AD-DualLayerHighRisk-1` · `AD-EntityCountDerivation-1` · `AD-EntityIndexIncomplete-1`）。
> 其前為 69 條，2026-08-12 W07 closeout：
> 新增 4 條（含 post-merge 的 `AD-RequiredCheckPrereq-1`）、關閉 3 條（`AD-GroupRowTheft-1` ·
> `AD-ReturningMasksCheck-1` · `AD-CalibrationNoActual-1`）、升級 2 條；
> **逐列重數所得**，不是在前一個數字上加 —— ⚠️ 第一版數法的欄位索引假設對兩列失效
> （得到 67 而非 69），換成掃全部儲存格的優先度標記才對得上總數）。
> 它**只放順序 + 前置條件，細節一律 link 回這裡**。
> ⚠️ **兩份都存在，收尾要同時改兩處** —— 只改一處就是下一次審計的漂移發現。

---

## 怎麼用

| 動作 | 何時 | 做什麼 |
|------|------|--------|
| **新增** | phase retrospective Q6/Q7 | 把新 AD 加到 §Open Carryover ADs |
| **關閉** | phase closeout | 把該 AD 從 §Open 移除 + 在 §Shipped Pointer Index 加 1 行 |
| **選取** | 開新 phase 前 | 從 §Open 挑一個當下個 phase 的目標 |
| **審計** | 每 5-10 個 phase | 掃一次 §Open —— 有沒有東西早就做完卻沒關掉？ |

---

## §Open Carryover ADs

<!-- 格式：一個 AD 一行。超過 2 行代表細節該進 retrospective 而非這裡。 -->

| AD ID | 症狀 / 缺口 | 來源 phase | 優先度 | 備註 |
|-------|------------|------------|--------|------|
| AD-PolicyAttestationFlag-1 | **`Policy.requires_attestation` 規格有（`02a:202`）而欄位未建** —— W14 原列為 US-3，Day 1 撞到它今天**沒有讀者也沒有寫者**：attestation 的 create 不可能讀它（`ScopedAttestationClient` 刻意不暴露 `policy`，那是 oracle 防線）· `policy.controller.ts` 是 UNTOUCHED 所以 create 不接受它 · 無 M5 workflow · 無 UI ⇒ 永遠為 `false`，**關掉不會壞任何東西** | W14 Day 1 | 🟢 P2 | 使用者裁定 2026-08-15 **移出本片 → M6**。⭐ 判準完全複製 W07 拒絕在 `EvidenceLinkedType` 建第二個值時用的那把尺（"a setting nobody can exercise"）—— 差別是那次拒絕的是 enum 值、這次是欄位，同一個問題「關掉它會壞什麼」給出同一個答案。**解封條件**：Policy 模組真的要讀它的那一片（M6）。⚠️ 登記在這裡而不只留在 W14 checklist 1.3，因為 ROADMAP 第 9 列的教訓是**有解封條件的東西必須出現在一份會被讀的清單上**，而 phase checklist 收尾後沒有人回頭掃 |
| AD-PrismaEnumThreeTruths-1 | ⭐ **一個 enum 值有三份真相，而中間那份會安靜地擋住 DB 已經接受的東西** —— W14 的中性化 N3b 預測「移除 `schema.prisma` 的 enum 值後 int suite 仍全綠（DB 的 enum 由已套用的 migration 建立）」，**預測錯了**：Prisma 的 **generated client 在 runtime 也驗證 enum 值**。⇒ 真相有三份 —— `schema.prisma` · **generated client** · DB catalog。實務後果：一個含 `ALTER TYPE … ADD VALUE` 的 migration 若**未配 `prisma generate`**，會產生「**DB 接受、應用層拒絕**」的分歧 | W14 Day 3 | 🟡 P1 | ⛔ **沒有任何 gate 會叫**：schema 與 client 不同步時 type-check 仍綠（型別來自 client，client 是舊的但自洽）、migration 仍套用成功。⚠️ 而它**只在部署時現形** —— 本機 `test:int` 的 global setup 每次重建 DB 又剛好會重新產生 client，所以開發迴圈**結構上看不見**這個分歧。與 `AD-SchemaMigrationDrift-1` 相鄰但不同：那條是「schema 與 migration 不同步」，本條是「**schema 與已產生的 client 不同步**」。候選解法：CI 在套用 migration 後跑 `prisma generate` 並斷言 `git diff --exit-code` 對產物為空；或改在 `postmigrate` hook 強制 generate。⚠️ 兩者都要先確認產物是否進版控 —— 若不進，第一個做法不可行。⭐⭐ **2026-08-16 量到了，答案是「不進」⇒ 第一個候選解法確定不可行，本條的解空間縮到只剩第二個** —— `.gitignore:96` 排除整個 `apps/api/src/generated/`，`git ls-files apps/api/src/generated` 回 **0 個追蹤檔案**。⇒ CI 每次都是 fresh checkout + `postinstall: prisma generate`，client **結構上不可能在 CI 端漂移**；W14 踩到的必然是**本機**未重跑 generate 的陳舊副本。⚠️ 這同時把本條的**真正射程**縮小了：三份真相裡只有 `schema.prisma` 與 migration 兩份進版控，而**那一對正是 `AD-SchemaMigrationDrift-1` 的標的且同樣沒有守衛** —— 兩條應該合併考慮解法。⛔ **W15 是本條寫下後的第一個 enum，而緩解手段是「這一次記得跑 `prisma generate`」，一個一次性人工動作** —— `residency_policy` 的三份真相今天一致，但把 `CREATE TYPE` 改成只剩一個值、或從 Prisma enum 刪掉一個值，`test:int` 225/18 全綠、type-check 0，**沒有任何東西會紅**（全樹 grep：`residency` 在 `.spec.ts` **零命中**，seed 不供給該欄而靠 `DEFAULT 'none'`）。最小成本守門：seed 一列非預設值並在測試中讀回 |
| AD-TextEditStructuralScope-1 | ⭐⭐ **用一個便宜的字串操作去做一件需要理解結構的工作 —— 同一天兩次** —— (a) W14 Day 2：seed 的 id 從 `ab0/ab1` 改成 `ac0/ac1`，用了**全檔 replace**，而 `int-global-setup.js` 裡 `ab0` **出現兩次**（另一次是 `assessment_instances` 的 seed）⇒ `assessment.int.spec.ts` **7 個測試紅**；(b) W14 Day 3：用 `t.index('  ],')` 找陣列結尾，它匹配到**第一筆資料的** `    ],`（四空格縮排包含兩空格模式），切出語法錯誤的檔案 | W14 Day 2-3 | 🟡 P1 | ⛔ **兩次都不是我發現的** —— 一次是 int suite 報 7 紅，一次是 node 拒絕載入。⇒ 目前唯一有效的偵測是**下游的執行**，而那只在有測試覆蓋的檔案上成立。⚠️ 與 `AD-NarrowPatternWideClaim-1` **同源但不同層**：那條是拿代理指標**做推論**（讀），本條是拿代理指標**做修改**（寫）—— 錯誤的推論會被交叉檢查抓到，錯誤的修改**直接落地**。提議的規則：**任何跨越多行或多處的文字操作，必須錨定到結構邊界（唯一 ref code / 含換行的完整界定符），並在操作後 assert 切出來的東西符合預期計數**（W14 兩次的修法都正好是這個形狀：`ASIN-` ref code · `\n  ],\n` + `seg.count('ATT-') == 2`）。⛔ 依 `.claude/rules/README.md` 強度階梯，**第 3 次應改結構性解法**而非再寫一次紀律。⭐⭐ **提議被寫下的同一天就攔下一次真實錯誤，但攔的方向不是預期的那個**：W14 post-merge 重指 9 個 rebase 改寫的 SHA 時，腳本帶了 `assert total == 17`（取自先前 Grep 的輸出），實際替換 **27** 處而 fire。**腳本沒錯，我的預期值錯了** —— Grep 回報的是**行數**，我拿它當**出現次數**（`\| 7bea684 → 652fb64 \|` 是一行兩個 SHA；`CALIBRATION-LOG.md` 的 9 行裡有 15 個）。⇒ 該 assert **保護的不只是操作，也是操作者對操作的理解**：操作本身完全正確（零殘留舊 SHA、27 個新 SHA、9/9 `ANCESTOR-OF-MAIN`），被證偽的是我的心智模型。⚠️ 這同時是 `AD-NarrowPatternWideClaim-1` 的微縮再現 —— 拿便宜的代理指標（行數）回答一個需要精確計數的問題 |
| AD-CalibrationWindowCrossSession-1 | ⛔ **calibration 的 commit 窗口法在跨 session 的 phase 上失效** —— W13 的窗口（首個 commit → closeout commit）給出 **13.67 hr / ratio 6.08**，因為 `73f4d9b` → `25ce0e6` 之間有 **729 分鐘**是隔天等使用者說「開始 Day 4」。W11 / W12 能用是因為它們**都在一個 session 內完成** —— 那不是方法成立的證明，是條件恰好滿足 | W13 Day 4 | 🟡 P1 | ⚠️ `AD-CalibrationDay0InOrOut-1` 要求「量法要事先宣告」，W13 照做了，**而宣告的量法本身是錯的** ⇒ 宣告解決的是「回溯歸類」不是「方法適用性」。本次改用兩段（Day 0-3 commit 逐段相加 + Day 4 產物 mtime → commit），並把不確定性報成**區間**再檢查它會不會改變 band —— 兩端同 band 故判定穩健。候選規則：plan §7 宣告量法時**同時宣告「若出現 > 60 min 的 commit 間隙該怎麼辦」**；或改用「逐段相加並排除 > 60 min 的間隙」當預設。⭐ 與 `AD-EstimateAsMeasurement-1` 同族：那條是「別把估當量」，這條是「**別把量錯的東西當量到**」。⭐ **W14 是候選規則的第一次實測，而它只證明了一半**：plan §7 **事先宣告**採「逐段相加並排除 > 60 min 間隙」，實測 Day 0–3 六個間隙**最大 34.9 min**，兩種算法**同給 100.9 min**（逐位相同）。⇒ 新量法在**無間隙**的情境下**不擾動**舊量法答對的那一類 —— 那是必要條件，但**不是**它宣稱要解決的那一類。⛔ **本 phase 沒有跨 session 間隙，所以 W13 的失效模式未被重現，也就未被修復驗證**。本條**不關閉**：解封條件是**下一個真正跨夜／跨 session 的 phase**，屆時兩種算法必須給出**不同**的值，且窗口法的那個必須是明顯荒謬的那個 |
| AD-MemoryEntryRatchet-1 | ⭐ **`MEMORY.md` 的 phase 條目長度 13 個 phase 從 186 漲到 401 字元（+116%），而 closeout policy 寫的是 ~250-300** —— W01 186 · W03 237 · W07 283 · W08 319 · W10 360 · W12 363 · W13 **401**。曲線單調上升，因為每個 closeout 都拿前一個當範本 | W13 Day 4 | 🟢 P2 | ⛔ **這正是 `task-workflow.md` §Phase Closeout 描述的相對錨點棘輪，而它自己沒有機械守門** —— `check_rules_hygiene.py` 只管 always-loaded 檔案，`MEMORY.md` 不在預算內。⚠️ W13 發現時已自行壓回 ~280，但**下一個 phase 會再看到 280 並超過它**。候選：把 `MEMORY.md` 的 `- W\d+ [` 行納入 hygiene 檢查（每行 ≤ 400 字元，與 calibration matrix 同一個機制）。⭐ 診斷價值高於修法：這是**規則寫了但沒有東西在數**的第 N 次 |
| AD-Day0ReadNotApplied-1 | ⭐ **Day 0 讀到了關鍵事實，卻沒有把它套用到 plan 的做法上** —— `scoped-prisma.provider.ts:151-165` 明寫「eleven integration suites build their graph from one module」，我在 W13 Day 0 的 D-reach 讀過那段；而 plan §3.0 正是要在那十一個 suite 裡加覆蓋測試。兩者直接矛盾，Day 0 三-prong 沒有發現 | W13 Day 4 | 🟡 P1 | ⚠️ **讀過 ≠ 用上。** Day 2 靠一次對照實驗才抓到（module-local 圖 `before=9 after=9` vs AppModule 寫一列），若直接開寫會得到 11 條永遠紅的測試，或寫成 `>= 0` 而**成為本片正要消滅的 Potemkin**。候選修法：Day 0 Prong 2 加一步 —— **每讀到一段解釋「為什麼現狀是這樣」的 docstring，回頭對照 plan §3 的做法一次**，判準是「該段話若成立，plan 的哪一步會失敗？」 |
| AD-NeutralisationConsumerGrep-1 | **中性化預測漏算 suite** —— W13 Day 3 的 N1 預測 26 紅實際 27、N3 預測 2 紅實際 5。兩次的缺口都是 `bench.int.spec.ts`，因為我列的是「預期會受影響的檔案」而不是「**誰 import 這個符號**」 | W13 Day 4 | 🟡 P1 | 候選修法：中性化預測**必須以一次 `grep <被改的符號>` 開場**，命中的每個檔案逐一判定（受影響 / 不受影響 + 理由）。實測 `grep AUDITED_MODELS` 回 **4 個**檔案，而我列了 2 個。⚠️ 這比「想一想還有誰」便宜且完整。⭐ 與 `AD-NarrowPatternWideClaim-1` 同族但方向相反：那條是 pattern 太窄導致漏，這條是**根本沒下 pattern** |
| AD-AuditWriteOpsUntested-1 | **`update` / `delete` 的稽核路徑零測試覆蓋** —— `WRITE_OPERATIONS`（`audit.recorder.ts:87`）列了它們，但 W13 Day 0 枚舉到全 codebase **零個 `client.*.update` / `.delete`**，所以「已涵蓋」這個宣稱**目前無人驗證** | W13 Day 4 | 🟡 P1 | ⚠️ **不是遺漏，是今天沒有那種寫入路徑** —— 現在寫測試等於測一個不存在的東西（AP-3 的鏡像）。**解封條件明確**：第一個 `update` 端點落地的 phase，**必須**同時加一條「update 留下稽核列且 `operation` 是 `X.update`」的測試。⛔ 那個 phase 的 checklist 要有這一列 —— `AD-5`：ROADMAP 的備註欄不是 checklist 的列。⚠️ **W14 確認仍然成立且敞口又大了一格**：`AUDITED_MODELS` 15 → **16**，而新增的 `attestation.repository.ts` 同樣**只有 create**（`attestations` 甚至連 `GRANT UPDATE` 都沒有，見 CH-031）⇒ 覆蓋率必須繼續連著「**全是 create**」這個限定一起讀 |
| AD-CoverageStatementNoTrigger-1 | ⚠️ **覆蓋聲明寫下了盲點，而盲點兩天後正好造成失誤** —— W13 Day 0 的覆蓋聲明明寫「**未掃 `bench.int.spec.ts`**」，Day 3 的 N1 / N3 預測失誤**正是漏算了它**。記下來了，卻沒有任何東西讓我回頭看 | W13 Day 4 | 🟢 P2 | ⭐ 與 `AD-DeferralUnwatched-1` 同族（條件寫得好、沒人在看），但**這次的距離只有兩天、在同一個 phase 內** —— 說明問題不是「時間久了會忘」而是「**寫下來本身不產生動作**」。候選修法：覆蓋聲明的每一條「未掃 X」在**同一份文件內**要配一個判定：「X 若與本片相關會怎樣」 |
| AD-AssessmentTitleMismatch-1 | **測試標題宣稱的範圍大於它實際斷言的範圍** —— `assessment.int.spec.ts:346` 標題是「cross-entity READ returns nothing, **on all three tables**」，而 body 只斷言 `templates` 與 `instances` 兩張，`responses` 未測 | W13 Day 0 | 🟢 P2 | AP-7 的形狀（命名與行為不符）。⚠️ **W13 順路發現，依 `CH-017` 節流閘不當場修**。⛔ 補的時候要注意：`assessment_responses` 的跨實體讀取斷言需要對造實體確實有 response 列（`AD-VacuousScopeTest-1` 的教訓），而 seed **沒有** `assessmentResponses` |
| AD-DeferralUnwatched-1 | ⭐⭐ **一個延後決定的解封條件寫得很好、可檢查、而且早就成立了 —— 沒有任何東西在看** —— `14-adr/README.md:106` 把 OQ-4 延後的理由是具體的（「判準是**寫入吞吐量**，零 code 時量不出來」），⭐ 那比「等以後再說」好得多。但條件失效發生在**好幾個 phase 之前**（20 張表、172 個 int 測試、真 PostgreSQL），而 `decision-form.md` 的「誰能決定」欄是 **⚠️ 未指定**，所以也沒有人會被問 | W12 Day 4 | 🟡 P1 | ⚠️ **這是 `CH-015` / `CH-016` 那個根因的第 6、7 次**（解封條件寫在沒人會回頭翻的地方），而前幾次的實例是 `AD-DesignNoteAnchor-1` 的「slice 3 處理」與 `AD-DualLayerHighRisk-1` 的 D002。⛔ **不要做成「提醒有人回頭看」** —— 回頭看正是失效的那個動作。候選：`decision-form.md` 每列加一個**機器可讀的解封條件**欄（例：`tables >= 5`、`ADR-0005 adopted`），detector 在 closeout 時求值，成立而該列仍在「開放中」即 **fail 並指名該列**。⚠️ 條件寫不成機器可讀的（如 OQ-8「Wave 3」）就明寫 `manual`，讓「哪些是真的無法自動檢查」變成可見的少數而不是全部 |
| AD-BenchOrderBias-1 | 🚩 **第一版 benchmark 量出「稽核讓寫入變快」**（對照組 p50 44.860 · A 42.722 · B 40.672，overhead **−2.138**）—— 多插一列不可能讓寫入變快。根因是**順序偏差**：三組依序跑、各自建 `TestingModule` 因而各自一個連線池，對照組排第一付了暖機成本，而偏差（~4 ms）**大於**被量的效應（~2 ms）| W12 Day 2 | 🟢 P2 | ✅ **本 phase 已修**：兩組**交錯**（逐筆輪替）+ 兩個內建儀器檢查 —— (1) 量測前先斷言對照組寫入後 `audit_log` 列數**不變**、稽核組**+1**（N2 證明這個檢查真的會紅）；(2) 每次都印出**兩個 phase 各自對照組的 drift**，drift 與被報告的 overhead 同量級時該比較不值得讀。⇒ 通則：**比較型 benchmark 一律交錯，且必須印出對照組自身的漂移**。⚠️ 這是 `AD-EstimateAsMeasurement-1` 的鄰居 —— 不是編數字，是**量錯東西**，而兩者的共同解都是「讓量測自帶一個可證偽的自檢」 |
| AD-StrategyBSunset-1 | ⚠️ **被 ADR-0003 否決的策略 B 留在 repo，因為 FC1 / FC2 拿它當基準線** —— `AuditLogRecorder` 的 `app-chain` 模式（`audit.recorder.ts:76`）是**沒有任何 production 路徑會選**的 production code，那是 AP-1 的形狀 | W12 Day 4 | 🟢 P2 | ⭐ **判準通過**（checklist 4.2）：ADR **有**引用它的量測數字 —— FC1（A/B 併發比 > 2×，今天 1.63 / 1.59）與 FC2（單鏈驗證 > 60 s）都以它為對照。ADR 明寫那是**當下的用途**不是「將來可能有用」。⛔ **解封條件（= 期限）**：Wave 1 結束前重量一次 FC1 / FC2；**未重量就刪掉 B**，並把兩條可證偽條件改寫成絕對值（例：A 的併發 overhead p50 > X ms）。⚠️ 刪 B 的同時要刪 `verifyAnchoredChain` 與 `anchorHash`，以及 `chain.spec.ts` / `verify.spec.ts` 裡對應的測試 —— **但 `contentHash` 要留**，它是 A 與 B 共用定義的錨點（`chain.spec.ts:243`）|
| AD-Residency-1 | 比較矩陣須對**所有**實體統一讀 `posture_snapshot`。**驅動力已換**（CH-008）：原因不再是中國月快照 vs 即時，而是「部分格子即時算、部分讀快照」本身就在比較不同時刻 | CH-001 → 重述 CH-008 | 🟡 P1 | M8 前確認；`02a` §7 已更新 |
| AD-DesignAlign-5 | `15` §7 #5：**OS portfolio** 模組尚未規格化；Risk programme 的**實體已定義**（`02a` §3.1）但**螢幕未規格化** | `15` | 🟡 P1 | `02a` §0 已列為「未規格化，不得建置」 |
| AD-DesignAlign-2 | `15` §7 #2：確認語言集與日本地位 —— i18n 從 M0 就要在位 | `15` | 🟡 P1 | |
| AD-Mockup-2 | **`data.js` 不可原樣移植** —— 旗艦儀表板以**國家**為鍵，結構上無法容納 13 OpCo（新加坡 2 家、香港 2 家）。約束 6 的 STOP-and-ask | CH-002 | 🔴 P0 | 阻斷 M8 |
| AD-Mockup-3 | OpCo fixture 需重建為 **13 家**（CH-008 後）：`opcos.js` 的 `RIN` 印度要刪，**且不補 `RCN`**；`Japan` 在 5 個檔案被當成營運實體 | CH-002 → 更新 CH-008 | 🔴 P0 | 移植前必做。⚠️ 原稽核建議的「`RIN`→`RCN` 代換」已作廢，改為直接刪列 |
| AD-Model-Gaps | `02a` 待決欄位級缺口：business unit 是階層節點還是自由文字、治理機構（ISC/ITSC）能否作為核准者、報告排程、通知規則、Policy 的檔案 metadata / TOC / 版本陣列、OrgEntity 的 OpCo function 欄位 | CH-002 | 🟡 P1 | 全數列在 `02a` §0.1 與 §0「未規格化」表 |
| AD-RiskForm-1 | **風險表單實作的是另一套方法論**，不是欄位不足：僅 7 欄、**完全沒有 before/after 結構**、缺整條 asset→threat→vulnerability→CIA 鏈、`Owner` 是自由文字非 user 參照 | CH-004 | 🔴 P0 | `15` §4 已更新 |
| AD-Incident-1 | **事件表單完全沒有 `11` 要求的 restricted block**（violating acts / motives / disciplinary action / president view）—— 連同它的 CISO/HR 權限隔離與存取稽核一併缺席 | CH-004 | 🔴 P0 | 隱私控制，`11` §Access control 明訂 |
| AD-Nav-1 | **Wave 3 的 AI agent 是導航第一項**（自成 Intelligence 組，在旗艦儀表板之上）。Wave 1–2 會出現顯眼的死連結或空群組 | CH-004 | 🟡 P1 | 產品定位決策，尚未有人做過 |
| AD-Switcher-1 | 實體/角色切換器把兩個正交軸壓成 7 個預組合的扁平清單（含國旗）。`15` §5.1 要求 entity scope 疊在 role 之上 | CH-004 | 🟡 P1 | 13 OpCo × 6 角色無法用扁平清單 |
| AD-Auth-1 | 註冊畫面帶著 pre-`15` 詞彙：Entity 是 6 個國家（含 Japan）、Requested role 是 `Risk Owner / Control Owner / Auditor (read-only) / Regional Governance`——**都不是已確認的六角色** | CH-004 | 🟡 P1 | 是 `AccessRequest` 的自助入口，非裝飾 |
| AD-Nav-2 | 導航計數徽章（Assessments `5`、Incidents `2`）未在任何規格中定義：算什麼、誰的範疇、多久刷新 | CH-004 | 🟢 P2 | |
| AD-Port-BFSI | 移植時須剝除金融業殘留：AML/CTF/制裁/對帳內容與 `juris` 的審慎監理機關。**CH-004 已確認僅存在於 `data/*.js` 與 standalone prototype——markup 乾淨**，所以這是資料 fixture 工作，不是全庫掃描 | CH-002 | 🟢 P2 | 成本已下修（原 🟡 P1）|
| AD-CssToken-1 | **`mockup-fidelity.md:38` 紅線 7 在本專案是錯的**：它規定一律 `oklch(var(--token))`，但交付物 token 是 HEX（`styles/tokens.css:24` `--primary: #2A5BD7`）。`oklch(#2A5BD7)` 是無效 CSS 且**靜默失效**。同一缺陷在 playbook §4.2 Layer 3 | CH-005 | 🟡 P1 | ⚠️ **W01 前端第一頁之前必修**（一行）。CLAUDE.md 約束 6「不做色彩空間轉換」在本專案亦不適用 —— 那是模板帶來的通用警語 |
| AD-DocIndex-1 | **`docs/02-architecture/README.md` §核心設計文件 仍是未填模板**：列的 `00-vision.md` / `01-architecture.md` / `02-tech-stack-decisions.md` 全部不存在，實際是 `00-project-charter.md` 等 27 份。**五個 detector 全部抓不到** —— 那些幻影檔名是純表格文字不是連結 | CH-005 | 🟢 P2 | 使用者反映「文件很多不知從何看起」的直接成因 |
| AD-Constraint7-1 | **CLAUDE.md 約束 7（LLM provider neutrality）的理由已失效** —— 原論證是「中國在範圍內，推論可能須境內發生 → 主權槓桿」，前提隨 ADR-0010 消失。約束保留（供應商鎖定／成本／可用性），但「合規機制」框架不可再引用 | CH-008 | 🟡 P1 | Wave 3 之前重寫。`14`、`14-adr/README.md` ADR-0009 列已標同一警語 |
| AD-ThrottleEfficacy-1 | **`CH-017` 的節流閘尚未被證明會擋任何東西** —— 規則寫進 `task-workflow.md` §Step 0.0 + hook 已上，但那只證明「寫進去了、預算沒爆、JSON 合法」。依 `AD-NegativeGate-1`，宣稱會擋東西的機制需要一個**被它擋住**的案例 | CH-017 | 🟡 P1 | 驗證點是**下一次助手提議「順手修一下 X」而被擋回 BACKLOG**。⛔ 量法要具體，不要用感覺：**下個 phase 收尾時數該 phase 產生的治理／工具 CH 數**（配額是 1）。⚠️ 這條本身也是治理待辦 —— 若它變成又一輪工具建設，那就是規則失效的證據而非成功 |
| AD-RegisterUpkeep-1 | **三份 living 追蹤文件的維護沒有任何機械檢查** —— `ROADMAP.md` · `RISK_REGISTER.md` · `DEFERRED_REGISTER.md` 於 2026-08-10 首次填入（CH-016）。在此之前它們空了 3 天、跨 2 個 phase 與 15 個 CH，**沒有任何一次收尾察覺**。填完之後同一個失效模式仍然存在：停更兩個月不會有東西叫 | CH-016 | 🟡 P1 | `check_status_markers.py` 掃的是三軌 pre-doc 的 `status:`，不掃這三份。可行守衛：detector 比對 `Last Reviewed:` 與最近一次 phase closeout 的日期，落後 N 個 phase 即 fail。⚠️ **不要做成「檔案有沒有被改過」** —— 那會被一次 typo 修正騙過去 |
| AD-CheckNameCoupling-1 | **workflow 的 `job.name:` 從 2026-08-10 起是 branch protection 的一部分，而沒有任何東西在看** —— 六個 required context 有五個是中文含 em dash 與全形括號（`憑證外洩 — gitleaks（全歷史）`），比對是逐字的。改一個字 → 所有 PR 卡在等一個永遠不出現的 context，**錯誤訊息不會說明原因** | CH-015 | 🟡 P1 | branch protection **不在版控**，所以這種漂移連 detector 都寫不了（本次 AD-5 就是靠人工審計才發現）。可行守衛：`ci.yml` 加一個 job 用 `gh api` 比對「required contexts ⊆ 本次 workflow 實際產生的 check name」——但它自己也得是 required 才有意義，形成先有雞先有蛋。**現階段的實際保護是本條 AD + `CH-015` 的 load-bearing 段落** |
| AD-WriteTextCRLF-1 | **Python 的 `pathlib.write_text` 在 Windows 上把 `\n` 寫成 `\r\n`，靜默把整檔從 LF 轉成 CRLF** —— 而 `.gitattributes:9` 規定 `* text=auto eol=lf`。W06 closeout 用它改了 9 個 markdown，`CLAUDE.md` 工作區 byte 數因此虛增 **522**（495 行 × 1），使 headroom 從 1,521 被誤量成 1,026。⛔ **我把錯的數字寫進了三份文件才發現** | W06 closeout | 🟡 P1 | ⭐ **`git add` 早就印過警告**（`CRLF will be replaced by LF`）**而我看到了沒查** —— 這是「證據就在眼前但沒去讀」的形狀。⚠️ commit 內容不受影響（git 正規化），受影響的是**本機量測**與假 diff。修法：改檔一律用 Edit 工具，或 Python 用 `write_bytes` / `open(newline='')`。⚠️ `.gitattributes:8` 的註解「工作區仍會依平台呈現（Windows 上是 CRLF）」**與 `eol=lf` 的實際行為不符**（`eol=lf` 連工作區都是 LF）—— 順帶待修 |
| AD-IssueBareEnum-1 | ⭐ **`Issue` 的兩條入邊都只有 enum、沒有 FK，所以「誰產生了這個 finding」在資料層追不回去** —— `02a:229` 的 `source`（test/manual）**沒有配對的 id 欄位**，`02a:372-383` 的 `risk_accepted` 狀態**沒有指向 `risks` 的欄位**。對照組是 `Evidence`：同一個問題它有 `linked_type` + `linked_id`，`Issue` 被規格成只有前半 | W08 Day 1 | 🟡 P1 | ⛔ **這是規格缺口不是實作選擇** —— 發明 `source_id` 就是自行發明欄位（已確認參數 #9），所以照規格建 + 寫進 `schema.prisma` 的兩個 enum docstring。⚠️ **今天的替代方案是把它塞進 `description` 自由文字**，那不可查詢也不可稽核。⭐ 影響面比看起來大：`02a:392` 的 `Failed → raises Issue` 是 M7 控制測試閉環的一半，而那條邊**現在只能單向走**。落點：規格層（要 `02a` 補欄位）不是實作層，**M7 之前必須拍板** |
| AD-MigrationTimestampTz-1 | **Prisma 用 UTC 命名 migration 目錄，而手建的 migration 用了本地時間，兩者相差 8 小時** —— W08 生成的 `20260812131655`（UTC）排在**已套用**的 `20260812164500_correct_parent_guard_comment`（W07 手建、本地）**之前** | W08 Day 1 | 🟢 P2 | 處置：重命名為 `20260812211801`（本地當下，晚於它）。⛔ **不是編時間** —— 目錄名的唯一功能是排序，正確順序就是它現在表示的。⚠️ **今日限定**（隔天 UTC 戳自然勝出），但**手建 migration 的時區慣例從未定過**，下次仍會踩。修法二選一並寫進 `docs/rules-on-demand/` 或 migration README：手建一律用 UTC（與 Prisma 對齊），或一律 `prisma migrate dev --create-only` 生成再改內容 |
| AD-ModuleCoverageDilution-1 | **每加一個 NestJS module 就把整體覆蓋率往下拉一次** —— `modules/*/*.module.ts` **全部 0%**（W08 當下 8 個：asset · control · control-test · evidence · issue · action · policy · risk），因為它們是 DI metadata、沒有可測邏輯。W08 加兩個就讓四項中的三項跌破 Day-0 baseline（92.07/91.9/96.63/93.62 vs 92.58/92.32/96.26/94）| W08 Day 2 | 🟢 P2 | **今天不是問題**：jest 門檻 80/70/80/80，四項遠高於。⚠️ **但它是單調下降的** —— 每個 slice 加 1-2 個 module，到第 20 個時門檻會被自然侵蝕，而那時的 root cause 會很難歸因（看起來像「測試品質下降」）。⛔ **不要靠給 module 寫測試解決** —— 測 `@Module` 的 metadata 是測 framework，AP-3。候選：`collectCoverageFrom` 排除 `*.module.ts`（它們的正確性由 int spec 的 `Test.createTestingModule` 證明）。⚠️ 排除任何東西都要同時說明「誰來證明它對」。⭐ **W14 再現，而這次的成因與本條原載的不同**：掉到 **87.33 / 85.56 / 95.93 / 88.59** 不是 `*.module.ts` 拉的，是 `attestation.repository.ts` 與 `attestation.controller.ts` **完全沒有 unit spec**（其他每個模組都有）—— 補 29 個測試後回到 92.14 / 91.77 / 98.98 / 93.56。⇒ 稀釋有**兩個**來源，而 `collectCoverageFrom` 排除 `*.module.ts` 只治得了一個。⚠️ 附帶：plan §4 列了 `attestation.repository.spec.ts` 卻**漏了 controller.spec** —— File Change List 漏一個檔就是覆蓋率掉一次 |
| AD-TestNameWiderThanProof-1 | **兩個整合測試的名稱宣稱的比它們證明的寬** —— `issue.int.spec.ts` 測試 6 與 `action.int.spec.ts` 測試 9 都叫 "cross-entity WRITE through the repository is refused"，而 W08 Day 3 的 N2/N3 量到：INSERT policy 中性化後**它們仍然綠**。實際擋住的是 `ref_code_counters` 的 policy（W05 的形狀）| W08 Day 3 | 🟢 P2 | ⚠️ **這不是缺口** —— 測試 7/8 才是 INSERT policy 的專屬測試，且 N2/N3 證實它們正確轉紅。所以覆蓋是完整的，**問題只在命名**。⛔ 但這正是 `AD-BorrowedRefusal-1` 每次得以藏身的那層外衣：一個名字寬鬆的綠燈，讀的人會以為它涵蓋了它沒涵蓋的。修法：改名為「repository 路徑上的某一層會拒絕」，或在測試 docstring 明寫「本測試不釘哪一層擋的，那是測試 7/8 的工作」。⚠️ 通則值得推廣到既有 int spec —— 但那是掃全樹的工作，不當場做。⭐⭐ **W09 第一次由元驗證主動抓到一個實例，而不是靠讀測試名**：測試 9c 叫「SoD 的拒絕不是 404 形狀」，只有兩個 `.not.toBeInstanceOf`；N4 移掉 SoD CHECK 後 create **成功**、`.catch()` 回傳一列資料，而資料本來就不是那兩個錯誤 class → **綠**。名字說「拒絕不是 404 形狀」，而當時**根本沒有拒絕**。修法已套用並當場在中性化狀態下驗證會紅：**每個「不是 X」的斷言旁邊要有一個「是 Y」的錨點**。已驗證 2/3。⭐ **2026-08-16 外部審查：第 3 次，3/3 —— 依 `.claude/rules/README.md` §強度階梯應升級為結構性解法，不要再寫第四次紀律** —— W15 `jurisdiction.int.spec.ts:192` 的測試 7 名為「the application role can write **none** of the three」，而 body 三個 `rejects` **全是 INSERT**；把 GRANT 改成 `SELECT, UPDATE, DELETE` 它仍然全綠。⚠️ 與前兩次的差別值得記：那兩次是**覆蓋其實完整、只有命名過寬**，這次是**覆蓋真的缺了三分之二**，而名字讓人讀不出來 ⇒ 本條的危害從「命名瑕疵」升級為「**名字掩蓋了真實缺口**」。⛔ 結構性修法的形狀已經有了：改用 catalog 斷言（對 `information_schema.role_table_grants` 斷言 privilege 集合**恰好相等**）而不是逐個 verb 寫 `rejects` —— 集合相等的斷言**無法比它的名字窄**，這正是「不是 X」型斷言的通解。詳見 `AD-W15ConstraintSurfaceUntested-1` |
| AD-MetaVerificationBug-1 | ⭐⭐ **元驗證本身會有 bug，而它的 bug 長得跟成功一模一樣** —— W08 Day 3 的 N6 第一版把 detector fixture 的孤兒 model 改名成 `Policy2` + table `policies`，以為它就不再是孤兒；而**那兩個名字也不在索引上**（索引寫的是 `Policy`），所以它仍然是孤兒、`run_all` 照樣 7/7、**EXIT=0**。更正為 `Risk`/`risks`（真的在索引上）才得到 6/7 FAIL | W08 Day 3 | 🟡 P1 | ⭐ **這是 `AD-NegativeGate-1` 往上一層的變體**：那條說「宣稱會擋東西的機制要有負面案例」，本條說「**負面案例本身也需要一個能證明它在測對東西的判準**」。⚠️ 抓到它的唯一原因是我**預期它會紅而它沒紅**，然後去查了 —— 若當初把 N6 寫成「預期綠」，這個壞掉的元驗證會被記成通過。提議加進 `verification-discipline.md`：**中性化的預期方向必須在跑之前寫下來**，且「零轉紅先查再下結論」要擴充成「**轉紅方向不符預期時，先懷疑元驗證本身**」。已在本 phase 驗證 1 次（1/3）。⭐⭐ **W12 是第 3 次，而這次答案是「元驗證本身沒 bug，配對錯了」**：N4 預測 `chains the rows it writes` 轉紅、`keeps a separate chain per entity` 不動，**實際完全相反**。先懷疑元驗證後量到的是 —— **測試檔內的執行順序決定第二個實體的列何時存在**，而我按語義而非時序配對。機制本身量測無誤（N4 下 SG1 scope 看得到兩個實體）。⇒ 提議補一句：**預測「哪個測試會紅」之前先看該 spec 的執行順序**，因為斷言依賴的資料是被前面的測試建立的。已驗證 **3/3 → 可回流模板** |
| AD-CalibrationIdleGap-1 | ⭐ **拍板的 `actual` 定義在 phase 被切成多次對話時，把「等待使用者」算成工時** —— `AD-CalibrationMetric-2` 定義為 branch 第一個 commit → closeout commit。W08 該窗口 **238 min**，而**其中 169 min（71%）是四次等待使用者回覆的間隔**（每個 Day 的起始時間戳減前一個 commit：55+78+28+8）。扣掉後 **69 min**，與逐段兩端錨點量測的 **66 min** 對得上 —— 兩個方法交叉驗證成功，**問題不在量測而在定義** | W08 Day 4 | 🟡 P1 | ⚠️ **後果具體**：官方 ratio 因此是 **0.84（IN band）**，而逐段量測是 **0.23（UNDER）** —— 那 169 分鐘的等待**恰好把它抬進了 band**，掩蓋了真實的估算問題（`AD-BottomUpBlueprint-1`）。⭐ `AD-CalibrationMetric-1` 讓窗口含間隙是**刻意的**，論證是「一致的高估可被乘數吸收」；那個論證預設間隙是**工作的一部分**（思考／CI／除錯），而這裡的間隙是**使用者不在**。提議：窗口扣掉「Day N 起始時間戳 − 前一個 commit 時間戳」的加總 —— **該值可從 commit 機械導出，不依賴 AI 有一個鐘**（那正是原定義的優點，本修正不放棄它）。⚠️ W04-W07 是單次連續 session，本條只影響**多次對話**的 phase；⛔ **不要回頭重算舊資料點**，那是決定不是整理（`AD-CalibrationMetric-2` 的前例）。⛔⭐ **W09 推翻了本條的提議**：它說那個扣除值「**可從 commit 機械導出，不依賴 AI 有一個鐘**」—— 而 W09 四個 Day 各由一次使用者訊息啟動，**使用者訊息的時間戳不在 git 裡**，所以扣不出來。W08 之所以算得出 169 分鐘，是因為那次的 Day 起始被記進 progress.md，而**那是人寫的**，正是 `AD-EstimateAsMeasurement-1` 警告的來源。⇒ 二選一：找一個 git 以外的機械錨點（session 起始戳），或**放棄窗口、只用單段閉合區間**。⚠️ 後果具體：W09 的四段全部閉合（54.4/16.4/24.6/17.5 min）卻仍只能報「上界」。已驗證 1/3 |
| AD-RebaseMergeBranchCheck-1 | **`git branch --merged main` 在 rebase-merge 下永遠答錯，兩個方向都錯** —— 本 repo 的 PR 走 rebase-merge，main 上是**改寫過的 hash**（W03: `6db88c4..17c2b80` → `f07105f..b20f3f1`）。刪 `feature/W03-governed-extension` 前跑 `git branch --merged main` **回傳空的**，`git log main..<branch>` **列出 7 個 commit** —— 從 ancestry 看它「沒被 merge」，實際上已經 merged | W03 → post-merge · **W08 撿回** | 🟢 P2 | 兩個方向的危害：(a) 用 `--merged` 清分支的人**永遠看不到** rebase-merge 過的分支 → 越積越多；(b) 看到 `main..branch` 非空的人會以為**工作遺失了**。⛔ **判準不能用 ancestry**。⭐⭐ **本條自己就是危害 (a) 的受害者**：它寫於 W03，而承載它的 `chore/w03-status-flip` **從未被 merge**，於是這條 AD 在本機躺了五個 phase，**直到 W08 清理分支時才被發現** —— 發現它的方法正是它自己主張的「不要用 ancestry」。⭐ **W08 量到一個比原文更好的判準**：`git cherry main <branch>`（patch-id 比對）同時處理 rebase 改寫**與**內容比對，比原文提的 `git cat-file -e main:<path>`（只驗路徑存在、不驗內容）與 `gh pr view`（需要 PR 編號）都便宜。實測：21 個本地分支中 **19 個回 0 個未合併 patch**，剩 2 個各有 1 個真的沒進 main —— **其中一個就是本條**。⚠️ 原文記的「origin 現有 11 個 ref」已 stale，W08 當時是 **25 個**。✅ **2026-08-13 已清空**：25 個遠端 + 3 個本機逐一 `git cherry` 驗過後刪除，遠端現為 **1 個分支（`main`）**；使用者同時把 `deleteBranchOnMerge` 翻成 `true`（`gh repo view` 複驗）。⛔ **設定只解決累積，不解決本條的判準問題** —— 它刪的是**遠端**分支，本機仍會累積，而 `git branch --merged` 在本機依然答錯。⭐ 且它**動不到從未 push 的分支**（本次遺失的審計就是那種）→ `AD-UnpushedWorkInvisible-1`。⚠️ **副作用要記著**：分支在 merge 當下即消失，**「清理分支順便發現東西」這條路以後不存在了** —— 本條與那份審計都是靠它撈回來的。取代它的是 `STATUS_AUDIT.md` §6 第 5 條（審計必須 merge），而那只管審計。⛔⭐ **W14 量到一個比「`-d` 會拒絕」更糟的行為：它會 *放行*，而理由與工作有沒有進 main 完全無關。** W13 的 `git branch -d` 拒絕（本條原載的形狀），W14 的**成功刪除**並印出 `warning: deleting branch 'feature/W14-attestation' that has been merged to refs/remotes/origin/feature/W14-attestation, but not yet merged to HEAD`。⇒ GitHub 於 merge 當下刪了遠端分支，但**本機 tracking ref 尚未 prune**，於是 `-d` 拿該分支去比對**它自己的一份陳舊副本**（兩者當然相同）而判定安全 —— 它自己的訊息就說了「**not yet merged to HEAD**」。⚠️ **這是「綠燈但比對錯東西」的教科書形狀**（`AD-NegativeGate-1` 同族）：`-d` 的安全檢查在這裡**恆真**，因為被比對的兩側是同一個 commit。⇒ 判準收斂成一句：**`git branch -d` 的結果無論拒絕或放行都不構成證據，唯一權威是 `git cherry origin/main <branch>` 的 `+` 數為 0**（W14 實測九個全 `-`）。⚠️ 且應在刪除前 `git remote prune origin`，否則陳舊的 tracking ref 會讓 `-d` 的判斷失去意義。⭐⭐ **本條自 2026-08-15 起是受控對照的實測，不再是解釋**：同一個變數（tracking ref 在不在）兩側各有資料點 —— **未 prune ⇒ `-d` 放行**（W14）· **prune 後 ⇒ `-d` 拒絕**（W14 交接前的刻意對照實驗，以及**審計 #6 的 PR #65 在真實工作流中自然重現**，exit 1 / `not fully merged`，⚠️ 該次是**先寫下預測再執行**）。⇒ 兩個方向都被觀測，**唯一的差別就是那個 ref** —— 從此不需要再論證 `-d` 為什麼不可信 |
| AD-UnpushedWorkInvisible-1 | ⛔ **沒有 push 的工作對每一個追蹤面都是隱形的，而本專案已經因此弄丟過一份完整的稽核報告** —— `docs/status-audit-20260810-3` 的 `31f76e2`（2026-08-10 審計 #3，109 行）**從未 push 到 origin**，只存在一個本機 clone。`gh pr list` 看不到它、`git ls-remote` 看不到它、`check_status_markers.py` 掃的是 `docs/` 的檔案（它在 main 上不存在，所以也掃不到）。它撐了三天沒被發現，代價是**下一次審計重用了它的序號與 AD 編號，而那組編號今天已無法修復** | W08 post-merge · 2026-08-13 分支清理 | 🟢 P2 | ⚠️ **2026-08-13 翻的 `deleteBranchOnMerge` 對本條無效** —— 那個設定管的是 merge **之後**的遠端分支，而本條的失效發生在**根本沒到達遠端**之前。⭐ 兩個已知實例成因相同（本條與 `AD-RebaseMergeBranchCheck-1`），**都是「用來防止漏看的東西」自己被漏看**。候選守衛（**擇一，都要使用者排序**）：(a) 收工前跑 `git for-each-ref refs/heads` + `git cherry main <each>`，非空即提醒 —— 便宜且不需新 detector；(b) session 收尾 checklist 加一列「本機沒有帶著未 push commit 的分支」。⛔ **不要做成 CI 檢查** —— CI 看不到別人本機的分支，那正是本條的形狀 |
| AD-TemplateStatusValue-1 | **`_templates/change/spec.md.tpl:26` 建議的 body 狀態值有兩個不被 `check_status_markers.py` 認得** —— 模板列「提案中 / 已核准 / 進行中 / 已完成」，而 `OPEN_STATES` 收的是「進行中 / 待處理 / 調查中 / 規劃中 / 草稿」、`CLOSED_STATES` 收「已完成」等：**四個裡「提案中」「已核准」對不上**。照模板抄會讓 `coarse()` 原樣回傳中文字串、與 frontmatter 的 `proposed`（coarse 後 `open`）不一致 → E2 violation | CH-027 起草時順帶發現 | 🟢 P2 | ⚠️ **不是模板錯也不是 detector 錯，是兩者從未對照過**。CH-027 本次改用「進行中」繞過。修法二選一：把兩個值加進 `OPEN_STATES`，或改模板的建議值。⛔ **不要只改自己那一份** —— 那會讓下一個照模板抄的人再踩一次，而這正是模板存在的意義被抵銷的方式 |
| AD-RiskTableCountManual-1 | **`RISK_REGISTER` R4 的「幾張表無稽核」是一個逐 phase 手寫累加的計數器** —— 內文寫「W02 兩張 → W04 +2 → W05 +3 → W06 +1 → W07 +2 → W08 +2 ＝ 十二張」，而真值的機械來源（`schema.prisma` 的 `^model` 數）就在旁邊。審計 #3（2026-08-10）當時量到它**落後兩個 phase**（仍寫「兩張表」而實際 5 張），並明確建議「**改結構，不只改文字**」 | 審計 #3 `AD-12@#3` → **W08 撿回** · **W12 實地擊中** | 🟡 P1 | ⚠️ ~~**數字今天是對的（12），所以這條不急**~~ **⛔ 這句話已於 W12 作廢（見末段）** —— 但它對的原因是每個 phase 都有人記得改，而審計 #3 已經量到那個「記得」失效過一次。⭐ 同族的兩條（`AD-EntityCountDerivation-1` · `AD-EntityIndexIncomplete-1`）已由 `check_entity_index.py` 關閉，**該 detector 已經在數 `^model` 了** —— 讓 R4 引用它的輸出、或改寫成「**所有已建業務表**（見 `schema.prisma`）」不必再寫數字，兩者都比再加一個手寫計數器好。⛔ **不要只把數字改對**，那正是審計 #2 的 AD-8 已證明無效的做法。⛔⭐⭐ **2026-08-14（W12）：本條第一次被實地擊中，而上面「數字今天是對的，所以這條不急」那句話因此作廢** —— 為了寫 R4 的覆蓋率分母而機械導出時發現：R4 手寫累加得 **18**，真值 **21**（`schema.prisma` 22 個 `^model` 減 `audit_log` 自己；逐個 migration 的 `CREATE TABLE` 加總交叉驗證相符：2·**1**·2·**5**·1·2·2·3·2·1）。兩個缺口指名得出來：**R4 的鏈完全跳過 W03 的 `extension_fields`**，且 **W05 記 +3 而實際建了 5 張**（`threats` / `vulnerabilities` 兩個全域庫）。⚠️ 而那個錯的 18 **已經傳染出去** —— W12 closeout 起初把覆蓋率寫成「1 / 19」並進了 CH-029 / design note / **ADR-0003** 三份文件，導出分母時才當場更正。⇒ 優先度 🟢 P2 → **🟡 P1**：它不再是「會漂移的風險」，是**已漂移且已污染下游**。R4 的原文保留不刪，因為錯的是計數方式不是某一次筆誤 |
| AD-DualLayerHighRisk-1 | ⭐ **ADR-0004 選項 C（RLS + 應用層過濾雙層）的解封條件已成立，而它從 `DEFERRED_REGISTER` D002 恢復** —— 使用者 2026-08-12 裁決條件是「**證據表出現**」而非 M3 里程碑（`STATUS_AUDIT.md` §2.7 AD-14）。W07 建了 `evidence`（`schema.prisma` `model Evidence`），且 `multi-tenant-data.md:212` 對「高風險表（稽核軌跡 / 證據 / 事件）」明訂**兩者都用** | D002 恢復 · W07 | 🟡 P1 | ⛔ **當初 defer 的理由現在是假的**：ADR-0004 §Options C 寫「Nothing to apply it to yet — **one business table exists**」，今天是 **12 / 35**。⚠️ **但不要因為條件成立就直接開工** —— 該 ADR 否決 C 的實質理由是「一個不可證偽的防護層就是 AP-3」，所以第一步是**先定義第二層要抓什麼 RLS 抓不到的東西**（候選：repository 層對 `evidence` / 未來 `audit_log` 的 `org_entity_id` 斷言，且**中性化 RLS 後它仍會紅**）。沒有這個定義就實作，交付的仍然是註解。⚠️ **`evidence` 目前同時沒有稽核軌跡**（R4，10 張表無一有）—— 兩件事都指向 M3，值得一起排。⭐ **2026-08-14（W12）：本條等的那個標的現在存在了** —— `audit_log` 已建，而它正是 `multi-tenant-data.md:212` 點名的高風險表之一。⚠️ **但第二層仍未定義**，本條的核心要求（先說清楚第二層要抓什麼 RLS 抓不到的東西）**一個字都沒動**。⭐ 順帶量到一個現成候選：W12 的 N3 證明 append-only 的兩層**可觀測行為不同**（GRANT 給 42501、缺席的 policy 給安靜的 `UPDATE 0`）—— 那是「兩層都在、但只有一層會說話」的實例，第二層若要有價值就得補上**會說話**的那部分 |
| AD-RequiredCheckPrereq-1 | **required check 的前置下載沒有重試，一次網路層攔截就讓所有 PR 停止可 merge** —— PR #44 首跑 `gates` **10 秒即 fail**，掛在「安裝 actionlint（釘版本 + 校驗）」：`curl: (60) SSL certificate problem: self-signed certificate`，`exit code 60`。同一個 run 的其他五個 job 全 pass；同一個 workflow 四小時前在 `main`（run `31562244727`）success；本 PR 對 `^\.github/` 與 `^scripts/` **零命中**。**重跑即過**，故確認為暫時性 | W07 post-merge | 🟡 P1 | ⛔ **失敗訊息完全不提示「這與你的程式碼無關」** —— 只有 `exit code 60`，而它發生在任何檢查跑到之前。⚠️ `gates` 是六個 required context 之一（`CH-015`），所以這個 10 秒的下載步驟**是全 repo merge 能力的單點**。候選修法（**都要動 `ci.yml` → 需使用者確認 + 吃節流閘配額**）：curl 加 `--retry` / 改用釘 SHA 的 action / 快取二進位檔。⚠️ **不要因為「重跑就好」而不記** —— 這條與 `AD-CheckNameCoupling-1` 同族：required check 的脆弱處不在檢查本身，在它周邊沒有人在看的前置條件 |
| AD-EstimateAsMeasurement-1 | ⭐⭐ **「逐日記錄工時」被遵守了，而記下的數字是編的** —— W07 Day 3 的 progress 工時表寫 **1 hr 22 min**、宣稱結束於 **16:05**；Day 4 起手取 `date` 得到 **15:41**，**一個還沒發生的時刻**。commit 時間戳給出真值 `b9f7611` 14:46:05 → `889993d` 15:17:06 = **31 min**，**超估 2.6 倍**。根因：15:14 之後沒有再取過任何一次 `date`，剩下四段全是**先寫下預期耗時、再把它當成量測** | W07 Day 4 | 🟡 P1 | ⭐ **這是 `AD-CalibrationNoActual-1` 的盲點**：那條要求「有沒有記」，本條講「記的是不是真的」——**它在被完整遵守的情況下仍然產出了假資料**，而該資料的去向是 calibration matrix 的第 5 個資料點。與 `AD-NegativeGate-1` 同構（規則被遵守但無效）。提議：每個時間區段**兩端都要有可觀察錨點**（`date` 或 commit 時間戳）；沒有閉合錨點的區段一律標 `~est` 且**不得進 calibration**。⚠️ 每段單看都「合理」是這個形狀最危險的地方 —— 沒有任何一段離譜到會被自己察覺。⭐⭐ **W11 是第 2 次，而這次錯的是 band 判定本身**：closeout 當下 closeout commit **還不存在**，於是 actual 用了「~12:00」這個**估的**收尾時刻得 ratio **1.13 IN**，並在四份文件裡寫成「本欄第一個 IN-band 點」；merge 後拿 author date 逐秒重算是 12:12:26 ⇒ **1.236 OVER**。差 12 分鐘、跨過 band 邊界。⛔ **W07 的修法沒有涵蓋這個位置** —— 它管的是 progress 的逐段工時，而本次出錯的是 retro §Q2 的**總量**，且成因是結構性的：**closeout 的 actual 必然要在 closeout commit 存在之前寫**。⇒ 修法要加一條：retro §Q2 的 actual **留白到 closeout commit 之後再回填**，或明確標 `~est` 並在 merge 後複算。⭐ 抓到它的是 **rebase merge 逼我回頭改 SHA 引用**，不是任何 gate —— 換成 squash merge，錯的數字會留在 main 上而全部 gate 皆綠。✅ **2026-08-14（W12）第一次執行提議的修法**：retro §Q2 的 actual / ratio / band **三格留白**並寫明「待 closeout commit 落地後由 author date 逐秒量測回填」，calibration matrix 與 log 一併延後到那時。⚠️ **代價要記著** —— closeout 因此是**兩個 commit** 而不是一個，第二個只做回填。⭐ 這也讓「窗口的右端點」第一次真的是量到的而不是宣告的。已驗證 1/3 |
| AD-BottomUpBlueprint-1 | **bottom-up 估算系統性高估約 5 倍，而乘數吸收不了** —— W07 `actual / bottom-up` = 3.4 / 19.75 = **0.17**，遠低於 `CALIBRATION-MATRIX.md` §乘數表明訂的 **0.4 下限**；該表自己寫著「低於 0.4 代表你的 bottom-up 估算方式有系統性問題，**該修的是估算不是乘數**」 | W07 Day 4 | 🟡 P1 | 歸因（W07 Day 1 / Day 2 progress 各獨立記過一次，方向一致）：**bottom-up 假設從零建，實際藍本複用率極高** —— repository / controller / module / int spec 的形狀由 W03-W06 定死，寫的是**差異**不是全部；量測床 W06 已備好，「量測 2.0 hr」實際 **10 min**。提議：bottom-up 逐項標 **「有藍本 / 無藍本」**，有藍本的按「寫差異」估。⛔ **不要靠調乘數解決** —— 乘數下限 0.4 是刻意的，把它調到 0.2 等於用校準掩蓋估算方法的問題。⭐⭐ **W08 照著提議改了估法，而它沒有收斂** —— bottom-up 逐項標了【有藍本】並按「寫差異」估（9.5 hr vs W07 的 19.75，工作量相近），四個**兩端錨點閉合**的區段實測 **46.4 min**，`actual/bottom-up` = **0.097**，比 W07 的 0.17 還低。逐段：Day 0 detector 8 min（估 45）· Day 1 16.5（估 195）· Day 2 12.2（估 150）· Day 3 9.7（估 90），**四段全在 9-12 倍之間，無離群值**。⭐ **所以「拆得更細」不是答案** —— 每一項都用同一個錯誤的單位成本，拆細只是把同一個錯誤乘上更多項。新提議（W08 Day 4）：**bottom-up 不再逐項估「要多久」，改為逐項標三級藍本度**（無藍本 / 有藍本改內容 / 有藍本改差異），乘上該級的**實測**單位成本 —— 本專案目前只有「有藍本改差異」這一級的資料，而它的實測值是 **≈ 8 分鐘/項**（46.4 min ÷ 6 個交付項）。⚠️ 樣本只有 W08 一個 phase，**下個 phase 先照舊估一次再比對**，不要現在就改 plan 模板。⭐⭐ **W09 照做了，而新方法準**：plan §7 同時寫下舊估（bottom-up 7.5 hr → committed 3.75）與新法預測（8 個交付項 × 8 min/項 = **64 min**）。實測 Day 1-3 的閉合區間加總 = **58.5 min**（上界）→ **新法誤差 < 10%，舊法誤差 7.7 倍**。`actual/bottom-up` = **0.25**（W07 0.17 · W08 0.097），三點皆遠低於 matrix 的 0.4 下限。⇒ **提議升級為「可回流模板」的候選**：plan §7 改為逐項標三級藍本度 × 該級實測單位成本，bottom-up 小時數降為參考欄。⚠️ 仍只有 1 個對照點，**再收 1-2 個 phase 才動模板**。已驗證 1/3 |
| AD-MdAnchorLineShift-1 | ⭐ **`AD-DesignNoteAnchor-1` 的上游成因被指認**：一次 markdown 編輯插入 8 行，就讓 **~30 個 `02a:NNN` 錨點**（`schema.prisma` 12 處 · 6 個原始碼檔 · ADR-0013/0014 · W06 design note · BACKLOG）**全部偏 +13** | W07 Day 4 | 🟡 P1 | ⭐ **本 phase 已用結構性解法當場處理**：把多行註記改寫成**同一行追加**，於是 `git diff --numstat` **4/4**、總行數 **495 = 495**、第 413 行 HEAD 與工作區**逐字相同**（三項都是量的不是推的）。**通則：被大量錨定的文件，編輯不得改變行數。** ⚠️ 這比 detector 便宜得多 —— detector 只能在偏移**之後**告警，本規則讓偏移不發生。⛔ 但 detector 仍需要：本規則管不了「刻意的結構性改寫」。**現成驗收命中**：`02a:413` 被 ADR-0014（3 處）· `schema.prisma:894` · W06 design note `:64` 引用，而它指向的是 §5.2 的**節標題**，Scoping 那一條在 `:415` —— 偏移由 W05/W06 造成，**W07 零位移已驗證**。⛔⭐ **W15 第一次違反本條，而違反者是知道這條規則存在的同一個 repo**：`multi-tenant-data.md` 加一列 `obligations` ⇒ 舊行號 ≥ 65 全偏 1，波及 **100+ 個 `file:line` 引用**；已修 7 檔並開始為「已採納 ADR 改不改」寫判準，**直到要寫 BACKLOG 條目時才查到本條已存在** → 全數還原，改為**併入既有的 `jurisdictions`/`regulations` 那一列**（同一行追加），量得 **390 → 390 行**、`--numstat` **1/1**、`:64`/`:67`/`:81`/`:145`/`:161`/`:197`/`:212`/`:294` 八個錨點逐字不變、**0 檔需重新指向**。⭐ 併入反而更貼合該表慣例（`frameworks`/`framework_controls`、`threats`/`vulnerabilities` 本就一家族一列），**零位移是附帶的不是硬凹的**，且順帶取消了「上面五類→六類」那個連帶修改。⛔ **根因不是不知道，是規則沒有載體**：本條是一句通則卻只住在 BACKLOG 的 P1 列，**沒有任何 always-loaded 規則帶它、沒有任何 lint 檢查行數**，於是它只能在事後被人想起來 —— `AD-NegativeGate-1` 家族的另一種形態（規則存在、正確、無人執行）。⇒ 修法候選（**擇一即可，不要都做**）：(a) 在被大量錨定的檔案頂端加一行 `<!-- anchored: 編輯不得改變行數 -->` 並由 `check_path_references.py` 順帶驗 `git diff --numstat` 的增刪為 0；(b) 併入 ROADMAP 第 9 列的 detector 一起做 |
| AD-SilentFieldDrop-1 | **`POST /controls` 對 `appliesToScope` / `effectiveness` 是靜默丟棄不是拒絕** —— W06 Day 3 走查 A4：送 `{"appliesToScope":"group","effectiveness":"effective"}` 回 **201**，而回應裡是 `entity` / `not_tested`。呼叫者得不到任何回饋 | W06 Day 3 | 🟢 P2 | 設計是刻意的（DTO 無此欄位，DB 也會拒 `group`），`risk.controller.ts` 檔頭早記過這個不對稱（「忽略」vs「覆寫」從外面看一樣）。⚠️ 但同一個問題會隨每個 server-owned 欄位重演（`refCode` · M3 的稽核欄位 · M7 的 `effectiveness`）→ **值得一次性決定策略**：忽略 / 400 拒絕 / 回應帶 `_ignoredFields`。⛔ 不當場改 —— 它是 API 契約決策不是 bug |
| AD-DAST-1 | **DAST 從來就不存在** —— `security-scan.yml` 四個 job 是 `secret-scan` / `dependency-scan` / `static-analysis` / `container-scan`，沒有 DAST；但 `04:71` 與 `07:50` 都要求它。⚠️ **私有 VNet 讓它更難補**：infra team 已確認一定接公司 private VNet，而 GitHub 託管 runner 在公網上，接不到只存在私有網路的 staging | Azure 資源盤點 2026-08-08 | 🟡 P1 | 與 `AD-SecScan-1` 同源但**不同缺口**（那三個是有 job 但 skip，這個是根本沒 job）。補法需 VNet 內 self-hosted runner 或等價路徑 —— **M0 規劃 CI 時一併決定**，部署後才發現代價高得多 |
| AD-SecScan-1 | ~~SCA / SAST / 容器掃描是 skip 不是 clean~~ → **W01 部分關閉**：三者現在真的執行（SCA `npm audit --audit-level=low` · SAST `Ran 462 rules on 47 files: 0 findings` · trivy `Detected config files num=2`）。~~⚠️ 剩餘缺口：Dockerfile 從未被 build 過~~ → **CH-013 關閉該項**（`image-smoke.yml` 每個 PR build 兩個 image）。⚠️ **剩餘缺口只剩 DAST 無 job** | CH-006 → W01 → CH-013 | 🟡 P1 | **仍不可視為關閉** —— guardrail 7 要求 SCA/SAST/DAST 三者，DAST 見 `AD-DAST-1`。完全關閉條件現在**只剩 DAST 有 job** |
| AD-SecDoDAutomation-1 | **`16` 的 28 點 secure-development DoD 沒有任何自動化檢查** —— `07:31` 的 M0 DoD 明文要求 "plus the **automated** secure-development DoD checks (`16`)"；CLAUDE.md guardrail 7 亦要求「每個 story 必須通過 28 點」。grep `scripts/` + `.github/` 對 `16-secure-development-dod` / `28-point` / `28 點` **零命中** —— 今天完全靠人記得 | STATUS_AUDIT 2026-08-10（AD-4）| 🟡 P1 | ⚠️ **在此之前這個缺口沒有任何 AD 在追蹤**，`AD-SecScan-1` 只涵蓋 SCA/SAST/容器掃描，兩者不重疊。✅ **分類已完成**（2026-08-10）→ [`09-analysis/secure-dev-dod-automation-classification-20260810.md`](../09-analysis/secure-dev-dod-automation-classification-20260810.md)：**A 已覆蓋 4+2 · B 今天可做 3+2 · C 無標的 7 · D 不可機械化 5 · N 需拍板 5**。⛔ **實作未做** —— 下一步是 B 類三點（#17 seed 檢查 · #10 瀏覽器儲存 · #25 危險 sink）。⛔ M0 收尾不得逕行打勾；**N 類五點需要一次責任邊界拍板**（見報告 §需要拍板的）|
| AD-HelmetSilentOption-1 | ⭐ **傳一個不存在的 helmet 選項不會報錯，會靜默關掉一個預設開啟的保護**。CH-012 實測：`helmet()` → `X-Powered-By` 不存在；**`helmet({xPoweredBy:false})` → `X-Powered-By: Express`**（`xPoweredBy` 不是 helmet 的選項名，它的是 `hidePoweredBy`）。W01 出貨的就是中間那個 | CH-012 | 🟡 P1 | 這是 `AD-NegativeGate-1` 形狀的**函式庫層版本**，比自己寫的設定更難察覺 —— 連「選項名不存在」都不會被告知。`security.spec.ts` 現在會對這個錯誤失敗。通則：**任何以物件傳選項的安全中介層，都要有一條在 wire 上驗證的斷言**，不能只讀設定 |
| AD-EslintSettingsClaim-1 | **`eslint.config.mjs:110-114` 的註解在 ESLint 10 下重現不出來** —— 該註解稱「`settings` 掛在有 `files` 的區塊會使 `boundaries/elements` 為空」，CH-012 實測加上 `files: ['apps/**/*.ts']` 後，從 repo 根與從 workspace cwd **兩種呼叫規則都照樣生效** | CH-012 | 🟢 P2 | ⚠️ **刻意不改那段註解** —— 只知道「試的那個形狀不會失效」，不知道「W01 當時是哪個形狀」。改掉等於用猜測取代紀錄。解法：下次動 eslint 設定時順手重現一次，確認後再改寫或刪除 |
| AD-NegativeGate-1 | ⭐ **「設定型強制力靜默失效」W01 一個 phase 內出現 5 次** —— boundaries 規則六種設定失效方式全部表現為 lint 全綠 · 三個掃描 job success 卻掃 0 個目標 · build 成功但產物起不到 · helmet `xPoweredBy:false` 對 Express 無效 · CI 跑 `test` 不是 `test:cov` 導致 45% 覆蓋率一路綠。共同結構：**設定損壞時不會報錯，而是安靜地什麼都不做**，外層 EXIT=0 讀起來像通過 | W01 | 🔴 **P0 候選** | 依 `.claude/rules/README.md` 強度階梯，同形狀 ≥3 次應改結構性解法。提議：**每個宣稱會「擋住某件事」的機制，必須附一個會被它擋住的常駐負面案例，且該案例在 CI 裡被執行**。**CH-012 交付 3 個**（boundaries fixture + `lint:negative` · i18n parity 雙向 · 安全標頭逐條）。**CH-013 交付第 4 個**：build 產物真的能啟動（`image-smoke.yml` + `scripts/smoke-probe.mjs`，4 項元驗證）。**W02 交付第 5 個**：RLS 真的在擋（policy 中性化為 `USING(true)` → 20 個整合測試紅 14）+ 旁路 detector（`scripts/assert-no-scope-bypass.mjs`，兩種弄壞法各驗一次）。**W03 交付第 6 個**：產物在 `NODE_ENV=production` 下**必須拒絕啟動且理由正確**（`image-smoke.yml`，run `31371191341` 輸出 `✅ production 啟動被拒絕（exit 1）`）—— 幾乎免費，**行為本來就在，只是從來沒有人以部署時的組態跑過它**。⭐ **W04 發現第 7 個形狀，但刻意沒有交付對應的常駐案例**：缺 `GRANT USAGE ON SCHEMA public` 時**每個端點回 500 而全部 gate 綠**（Day 3 首次探測），根因是 `isms_test` 從 template1 免費繼承該權限、`isms_dev` 不會 —— **兩個環境各自的負面案例都會過，因為它們各自在自己的環境裡是對的**。這是本條規則的第二個邊界（第一個是 `AD-OpensslClaim-1`）：**常駐負面案例對「兩個環境不對等」無效** → 解法歸 `AD-DbBuildPathParity-1`。⭐ **W05 是第 8 個形狀，而這次結構性解法是有效的**：三張新表的 RLS 全部中性化後，「跨實體寫拒」那個測試**仍然是綠的** —— 拒絕由上游的 `ref_code_counters` 代勞，`risks` 自己的 `WITH CHECK` **零覆蓋**。與第 7 個（環境不對等）不同，這次缺的正是一個常駐負面案例，**同一天已補上 int 11b 並在仍中性化的狀態下確認它轉紅** → 通則歸 `AD-BorrowedRefusal-1`。**剩餘 1 個仍無覆蓋**：掃描 job 真的掃到 N>0（要 parse job log，工具升版即壞）。**本條不關閉**。⚠️ **本列是實例清單的唯一權威，其餘文件只 link 不複製計數** —— 過去 `RISK_REGISTER`/`ROADMAP` 各寫一次 `5/5`，W03 交付第 6 個時兩處都沒跟上（審計 AD-8）。且該分數形式本身是錯的：**已交付 6 個**與 **W01 五個形狀中仍有 1 個無覆蓋**是兩個獨立的數 ⚠️ CH-013 順帶量到這條規則的**邊界**：元驗證 3（拿掉 openssl）證明有些缺陷**不造成可觀測故障**，負面案例對它們無效 —— 見 `AD-OpensslClaim-1`。⭐ **W02 改了結構而非再寫一次紀律**：detector 的 self-test **不在旗標後面**，每次執行都跑 —— 同形狀第 3 次不應該再靠「記得跑」 |
| AD-GrepAssertion-1 | ⭐⭐ **W03 又犯 3 次，總計 ≥ 10 —— 且三次都在同一個 session 內**（`cmd 2>&1 \| tail -N; echo $?` 抓到的是 `tail` 的退出碼，其中一次把 **type-check 失敗讀成通過**）。依 `.claude/rules/README.md` 強度階梯，「**同一個 session 內**被違反」是**第 4 級**（`UserPromptSubmit` hook 每回合注入）的判準 —— 那是「中途忘了」不是「不知道」，**在規則檔裡再寫一次沒有用**。⚠️ 但 hook 每回合都付 context 成本且 CH-017 才剛裝一個 → **W03 刻意不當場實作**（節流閘 Step 0.0），由使用者排序。↓ 以下為原始條目 ↓ **拿一段為人類排版的文字去做機器判斷，W02 壞了 2 次** —— (a) `grep -c "^\[warn\]"` 因 prettier 的 ANSI 色碼永不匹配，**0 命中被讀成 format clean 並寫進 commit message 與 PR 描述**；(b) `image-smoke.yml` grep `super=f bypassrls=f`，但 `\|\|` 串接布林得到 `true`/`false` → 對一個正確的受限角色誤擋（PR #25 run `31321090681`）。**(b) 從未以 workflow 裡的形式被跑過，也從來沒有負面案例** | W02 Day 4 | 🟡 P1 | 修法已套用於 (b)：判斷移進 SQL（`count(*) ... AND NOT rolsuper`），本機驗兩個方向（受限→1 過 · superuser→0 擋）。通則：**斷言用退出碼或結構化資料（`--json` / SQL 述詞），不 grep 格式化輸出**；若非 grep 不可，必須有一個「應該被抓到」的負面案例證明 pattern 會匹配。⭐ **W07 再犯 2 次，且第二次是新形狀**：(c) `grep "FORCE ROW LEVEL"`（單空格）漏掉對齊用的兩空格寫法 → 3/7 誤讀為缺失，**且已當成事實對使用者講過一次**（實為 7/7）；(d) ⭐ **`tail -N` 用在多 workspace 的 npm 輸出上** —— `format:check` 與 `type-check` 兩者 `apps/api` 皆紅、`apps/web` 綠，而 `tail` 只留下最後一個 workspace 的成功訊息，**兩個假 gate 宣稱因此進了 Day 2 的 progress**。⚠️ 附帶事實：**jest 不做完整型別檢查**，那兩個 TS2353 有 235 個綠測試也抓不到，**只有 `tsc --noEmit` 看得見**。⛔ (d) 與本條原載的「`tail -N` 抓到 tail 的退出碼」是**不同機制**（一個藏退出碼、一個藏內容），但同一個修法：**gate 輸出一律逐 workspace 可見，不用尾部過濾器**。⭐⭐ **2026-08-13 撿回審計 #3 後，本條在「審計自身」的再現次數由 2 更正為 3**：審計 #3 的 `AD-14@#3`（`grep -c "🔴 P0"` 得 0，實際 5，emoji 不匹配）· 審計 #4 的 `AD-15`（i18n 計數）· 加上審計 #2 的自我修正。⚠️ **審計 #4 的 AD-15 自稱「第二次再現」，那是在不知道 #3 存在的情況下寫的** —— 一個沒被 merge 的分支讓再現次數被低估了一次。依 `.claude/rules/README.md` 強度階梯，**≥3 次應改結構性解法**，本條因此**具備升級到第 4 級（hook）的判準**，仍待使用者排序。⛔⭐ **W15 Day 3 第 N 次，而這次藏掉的是「發現本身」**：中性化還原後的驗收跑用 `Select-String -Pattern "Tests:|Test Suites:"` 只留計數行，結果那一次意外是 `223 / 2 failed` ——**兩條失敗的身分在產生的當下就被過濾掉了**，之後三次全綠，證據**不可回收**。⚠️ 與既載的兩種形態都不同：`tail -N` 藏的是**退出碼**、多 workspace 的 `tail` 藏的是**另一半輸出**，這次藏的是**唯一一次異常的內容**。⇒ 通則補一條：**跑一個預期會綠的驗收時，過濾器必須保留失敗路徑的輸出**（`FAIL`／`●`／`Expected`／`Received`），或**乾脆寫檔再讀** —— 過濾器決定了你事後還能問哪些問題，而異常按定義不會照你的預期出現。⭐ 這是本條第一次的代價不是「讀錯」而是「**再也讀不到**」 |
| AD-CalibrationMetric-1 | ⭐ **`actual` 欄一直在混裝兩種量，所以乘數不可能收斂** —— 三個 phase 的牆鐘跨度 vs 登記值：W01 `4h27m` / ~5.0 hr ✅ · W02 **`8h10m` / ~12.1 hr ❌** · W03 `~4h20m` / ~4.3 hr ✅。**W02 那列算術上不可能** —— 逐項加總大於工作實際發生的窗口，循序執行做不到，故它是「若由人手做要多久」的估計而非量測 | W03 | 🟡 P1 | 用一致的牆鐘定義重算：W01 **0.35** · W02 **0.75** · W03 **0.34** —— 比原本 0.35 / 1.10 清楚得多。提議定義 `actual` = **branch base commit → closeout commit 的牆鐘跨度**：`git log` 機械導出、含間隙成本、**不依賴 AI 有一個鐘**。⚠️ 代價明說：含閒置（W02 有 3h17m 的 CI 修復間隔），系統性高估專注工時 —— 但**一個一致的高估可被乘數吸收，混裝的兩種量不行**。⛔ **拍板後 `AD-TimeTracking-1`/`-2` 應一併結案**：它們要求的資料在目前執行模式下不存在，繼續要求只會每個 phase 記一次「這次也沒做到」 |
| AD-ChecklistTickDrift-1 | **W02 的 Day 4 checklist 整段從未被勾** —— `W02-.../checklist.md:276-283` 八項全是 `[ ]`，但工作**確實做了**（`CALIBRATION-MATRIX.md` 有 spike 那行、`MEMORY.md` 有 W02 指標、`decision-form.md` OQ-3 已拍板、retrospective 存在）。W03 closeout 後同步 main 時順路發現 | W03 → post-merge | 🟢 P2 | ⚠️ **方向與 `AD-StaleRecordRef-1` 相反**：那條是「記錄說做了但其實沒有」，這條是「做了但記錄說沒有」——**後者讓 checklist 失去它唯一的功能**（retro 時比對「計畫了什麼 vs 出貨了什麼」）。`check_status_markers.py` 掃的是 `status:` frontmatter，**不掃勾選狀態**。候選守衛：phase 資料夾 `status: closed` 時，checklist 若仍有 `[ ]` 且沒有 🚧 標記 → fail。⛔ **不要自動勾** —— 勾選是「我驗過這項的 DoD」的宣告，代勾等於偽造證據 |
| AD-JestFileOrder-1 | ⭐ **跨 suite 的資料庫污染是順序相依的，而順序本機與 CI 不同** —— jest 冷快取依檔案大小排序、暖快取依上次執行時間。PR #31：`policy.int.spec.ts`（第一個會**寫入**的 suite）在 CI 先跑，本機後跑 → **同一個 commit 本機綠、CI 紅**。⚠️ 軟刪除的清理**擋不住沒有 `retiredAt` 過濾的查詢**，所以修法在自己的 suite 裡看起來有效 | W03 → PR #31 CI | 🟡 P1 | 真正的缺陷在**斷言**：`toEqual([<單一 title>])` 除了隔離還斷言了「沒有其他 SG1 列」——**fixture 記帳，不是隔離性質**，而測試名稱從沒宣稱過它。已改為斷言測試名稱一直在宣稱的事。**通則：共用可變資料庫的整合測試，斷言必須是順序無關的性質，不是精確列表。** 候選守衛：`--randomize` 常態化跑順序，讓順序相依當場暴露而不是等 CI。⭐ **W15 Day 3 疑似第 2 次，而這次的觸發條件比 CI 更常遇到**：兩次中性化還原完畢、`git status` 空之後的**第一次** int 跑出 `223 / 2 failed`（2 個 suite），接著**在零改動下連跑三次全部 225 / 18**。⚠️ 本條說暖快取「依上次執行時間」排序 ⇒ **任何一次異常跑之後，下一次的順序就與平常不同** —— 而那正是中性化工作的必經狀態，也就是說**每次中性化的「其餘 N 條不動」都比它看起來弱一個等級**。⛔ **證據不完整且不可回收**：那一次的輸出被過濾成只剩計數行，**兩條失敗的身分在產生的當下就丟了**（見 `AD-GrepAssertion-1` 的 W15 實例）。⇒ 重現配方（成本約 3 次 × 2 分鐘）：套用 W15 的 N1（`ALTER TABLE org_entities DROP CONSTRAINT org_entities_jurisdiction_id_fkey` 追加在該片 migration 末尾）→ 跑 int（應 1 紅）→ **還原** → **立刻**再跑並以 `--verbose` **全量寫檔**，比對 suite 執行順序與綠態基準（`npx jest --config jest.int.config.js --listTests`，綠態下 `rls-direct` 第 17、`jurisdiction` 第 18）。⭐ 若確認，`--randomize` 這個候選守衛就從「防 CI 差異」升級為「**防每次中性化之後的第一次驗收**」，價值高得多 |
| AD-DbBuildPathParity-1 | ⭐⭐ **兩條建庫路徑產生的 schema 權限不同，而只有一條被測試過** —— `int-global-setup.js` 用 `CREATE DATABASE`（從 template1 複製，`public` schema 帶內建 `GRANT USAGE TO PUBLIC`），`prisma migrate reset` 則是 `DROP SCHEMA public CASCADE` + `CREATE SCHEMA public`（**ACL 為 null，只有 owner 有權限**）。W04 Day 3 首次探測即 500：`permission denied for schema public` 42501。**該權限從未被本 repo 的任何東西授予過** —— 它是繼承的，而被測試的恰好是繼承到的那條路徑 | W04 Day 3 | 🟡 P1 | ✅ **已修**（`20260810215500_grant_schema_usage`，`GRANT USAGE` 不給 `CREATE`）。⛔ **但通則未關**：`isms_test` 每次重建都綠，**證明不了 `isms_dev` 或任何非 template 路徑可用** —— 這是 `AD-DevDbDrift-1` / `AD-NegativeGate-1` 的第三個變體（**綠燈的涵蓋範圍比讀者以為的窄**）。候選守衛：int 套件加一個「以 app role 連線斷言 `has_schema_privilege(...,'USAGE')`」的前提檢查，比照現有的 `rolsuper/rolbypassrls` 斷言 —— 那條斷言的存在正是同一個教訓的上一次。⭐ **W05 Day 3 量到兩件事**：(a) **reset 路徑本身已驗過** —— 在 throwaway 庫上重現 `DROP SCHEMA public CASCADE`（ACL→`(null)`、`isms_app` USAGE→`f`，即 W04 撞 500 的狀態），跑 `migrate deploy` 後 USAGE `t` / CREATE `f`，五張新表權限逐張如設計。**不需要 reset `isms_dev`**；(b) ⛔ **上述候選守衛放在 int 套件會無效** —— `isms_test` 走 `CREATE DATABASE` 路徑，那條斷言會靠 template1 繼承來的 `=U` 輕鬆通過，**正是本條 AD 自己在講的病**。真正的守衛必須跑在 reset 路徑上（候選：CI 加一個 job 在 throwaway 庫上重現 reset 後跑 `migrate deploy` + 權限斷言） |
| AD-MigrationChecksum-1 | ⭐ **`applied=true` 不代表「套用的是同一份內容」** —— W04 Day 2 撞到：`prisma migrate dev` 拒絕生成，因為 `20260809075152_entity_scope_spike` 的 DB checksum（`ac8d1b35…`）與檔案（`a5eea1df…`）不符。根因是 W02 **先 `migrate dev` 套用、再手動編輯**加入 RLS/GRANT/trigger；後兩個 migration 相符，表示流程之後已改。⚠️ **非 CRLF、非 git 改動** —— 兩者都排除過 | W04 Day 2 | 🟡 P1 | ⭐ **這是 `AD-DevDbDrift-1` 的盲點**：那條的 Day-0 檢查驗「有沒有套用」，**沒驗「套用的是不是同一份內容」**，所以它在 W04 Day 0 回報乾淨而問題仍在。提議：把 Day-0 的 `D-devdb` 擴充為**比對 `_prisma_migrations.checksum` 與檔案的 sha256**，不只比對目錄清單。⛔ 處置需**破壞性操作**（`migrate reset`）—— 且 Prisma 有 AI 專用安全閘會擋，**不接受先前的問答作為同意** |
| AD-DevDbDrift-1 | **`isms_dev` 落後兩天而沒有任何東西察覺** —— W03 Day 3 clean restart 時才發現 `20260810134319_governed_extensions` 從未套用到開發者的資料庫。int 測試每次**重建**自己的 `isms_test`，所以整個 Day 2-3「int 全綠」**不涵蓋「開發者的資料庫可用」** | W03 | 🟢 P2 | 兩個資料庫只有其中一個會自我修復，而綠的是那一個。提議：啟動時比對 `_prisma_migrations` 的 head 與 `prisma/migrations/` 最新目錄，落後即 **warn 不是 fail**（fail 會擋住正在寫 migration 的人）。⚠️ 這是 `AD-NegativeGate-1` 的變體：**不是設定靜默失效，是綠燈的範圍比讀者以為的窄** |
| AD-ExtensionQueryCost-1 | **ADR-0005 的可證偽條件 #1（trigger 每寫入成本 > 5ms）未量** —— 今天 `policies` 2 列、catalog 4 列，`EXPLAIN` 量不出東西。同時可證偽條件 #2（跨記錄過濾擴充欄位）也未量，JSONB GIN 索引**刻意未建**（現在建是 YAGNI）| W03 | 🟢 P2 | 與 `AD-ScopeFnCost-1` 同一類（成本類可證偽條件無法在空資料庫上驗），可一併在 M1 有真實資料量時處理。⛔ **不要因為「未量」就先建索引** —— design note §4 已明記解封條件是「M1 有真實查詢需求」|
| AD-PoolerScope-1 | **pooler（PgBouncer / pgcat）下 tx-local `set_config` 的行為未驗** —— transaction pooling 模式可能讓範疇跨請求外洩或消失。ADR-0004 §可證偽條件 #1 就是這一條 | W02 | 🟡 P1 | ⛔ **引入任何 pooler 之前必須先驗**。若不成立，`decision-form.md` OQ-3 的選項 B（應用層過濾）是已記錄的降級路徑，**降級必須被記錄而非默默發生** |
| AD-ScopeFnCost-1 | **`app_entity_scope()` 的每列成本未量測** —— 標 `STABLE` 應讓 planner 每 statement 求值一次，但沒跑過 `EXPLAIN`。RLS policy 裡的函式若真的每列呼叫，滾升查詢會隨資料量惡化 | W02 | 🟢 P2 | 資料量成長後用 `EXPLAIN (ANALYZE, BUFFERS)` 量測。今天 2 列，量不出東西 |
| AD-Day0Scope-1 | ⭐ **Day-0 對「狀態性行為」的量測範圍過窄，而結論被當成已驗證地基** —— `D-failclosed` 只測了 virgin 連線就判定「fail-closed 由 PostgreSQL 免費提供」，並據此**減了 0.5 hr**。Day 2 量到：連線被 scope 過一次之後，`current_setting` 不再 raise，查詢**靜默回 0 列** | W02 | 🟡 P1 | 提議 `day0-plan-verify.md` Prong 2 增一條：對**有狀態的**行為（GUC / session / cache / 連線）量測時，必須含「第二次呼叫 / 用過之後」的案例。一次性案例的結論只能標 first-call only |
| AD-EnvDrift-1 | ⭐ **改了 `.env.example` 不等於改了 `.env`** —— W02 Day 1 更新了範例檔，Day 2 的探測仍以 W01 的 superuser URL 連線，**十二項全綠而 RLS 全程未生效**，並在過程中把一列 fixture 搬到了別的實體 | W02 | 🟡 P1 | 測試層已有守衛（`int-global-setup.js` 斷言角色）。缺的是**應用程式啟動時**也斷言連線角色非 superuser —— 那是 production 唯一會被察覺的時機 |
| AD-AdRegistry-1 | **plan 裡命名的 AD 沒有進 BACKLOG，closeout 時無處可關** —— W02 plan §0 / §5 AC-9 引用 `AD-RLS-Unverified`，grep 全 repo 只命中 W02 自己的文件。起草 change record 時又順手發明了 `AD-CleanDbMigrate-1` | W02 | 🟢 P2 | Day-0 Prong 1 順便驗 plan 引用的每個 `AD-*` 在 `BACKLOG.md` 存在；不存在就當場註冊，或改用敘述而不是編號。**編號會製造「已登記」的錯覺** |
| AD-TimeTracking-2 | **`AD-TimeTracking-1` 只被遵守了 3/4 天** —— W02 Day 1-3 有逐項工時，**Day 0 沒有**，而它是工作量第二大的一天。第 1 個 `spike` 資料點因此仍是部分回推 | W02 | 🟢 P2 | Day-0 的 progress 條目也要有時間表格，與 Day 1-3 同格式。否則 calibration 每個 class 的第一個資料點都會帶同一個瑕疵 |
| AD-TimeTracking-1 | **`progress.md` 全程零工時紀錄** —— `task-workflow.md` Step 5 要求逐任務「actual Z min」。W01 的 calibration 第 1 個資料點只能由 commit 時間戳回推，`|R-1.0|` 達 65% 卻無法判斷是雜訊還是訊號 | W01 | 🟡 P1 | 每日 progress 條目強制寫 `Task X.Y — actual Z min (est ~W min)`。沒有它，calibration matrix 收集的是估算的估算 |
| AD-Day0Registry-1 | **Day-0 Prong 2 的定義只涵蓋「plan 對現有 code 的斷言」**，而 W01 最有價值的 drift（`D-nest-prisma-ver`：registry 現況 NestJS 11.1.28 vs ADR 寫的 10）來自**外部 registry** | W01 | 🟢 P2 | 已在 W01 實際擴充並奏效（1/3 驗證）。連續驗證 3 個 phase 後，把「plan 引用的外部套件版本」明列進 `day0-plan-verify.md` Prong 2 |
| AD-ChNumber-1 | **「建立前先查最大號」用 `ls` 目錄會撞號** —— W01 checklist 寫「目前最大為 CH-009」，但 `CH-010` 已被四處**前向引用**預留給 Azure 資源清單（`ADR-0010:175` · `ADR-0011:16,157,158` · `CH-009/spec.md:165` · 本檔 `AD-IaCEvidence-1`）。本 phase 改用 CH-011 | W01 | 🟢 P2 | 判準改為 **grep 全 repo 的 `CH-\d+` 引用**，不是 `ls` 目錄 —— 被預留但尚未建立的編號在目錄裡看不見 |
| AD-ImageDigest-1 | **base image 釘的是 tag 不是 digest** —— `apps/api/Dockerfile:41-44` 自稱「it is a **recorded** follow-up」，但 CH-013 grep 全 `docs/` 後確認**它從未被記錄到任何清單**，只存在於那段註解裡。W01 checklist 的 DoD「base image 釘 digest」也因此一直未達成 | W01 → CH-013 發現 | 🟡 P1 | 與 `AD-ImageBuild-1` 同一種病：知道問題、寫下來、寫在沒有人會回頭看的地方。digest pinning 才真正防 tag mutation；`AD-TrivyExempt-1` 到期重拉 base 時是自然的落點 |
| AD-OpensslClaim-1 | **`apps/api/Dockerfile:55-58` 的「then the wrong engine」是推論不是觀測** —— CH-013 元驗證 3 拿掉整個 `RUN` 實測（CI run `31300101058`）：警告確實出現（一字不差），但 `✔ Generated Prisma Client (v7.9.1)`、build 綠、`{"status":"up","db":"up"}`。缺 openssl 在此組態下**只有警告，無可觀測故障** | CH-013 | 🟢 P2 | ⚠️ **刻意不改那段註解**（`AD-EslintSettingsClaim-1` 先例：單次觀測、不知 W01 當時全貌）。**保留 openssl 安裝仍然正確** —— 消除警告、不靠「預設剛好能用」。順帶：`image-smoke` **抓不到這一類**，因為它不造成故障 |
| AD-AllowlistCountClaim-1 | **`scripts/assert-no-scope-bypass.mjs:20` 的 docstring 說 "The allowlist is **four** entries"，實測 `3 allowlisted`** —— `ALLOW` 有 3 個 Map 條目、6 個 file-rule 配對，**沒有一種數法得到 4**。W04 Day-0 跑 `lint:negative` 時發現（輸出 `17 檔, 0 bypass, 3 allowlisted`）| W04 Day 0 | 🟢 P2 | ⛔ **刻意不當場改** —— 與 `AD-EslintSettingsClaim-1` / `AD-OpensslClaim-1` **同一個先例**：只知道「今天是 3」，不知道「W02 當時是幾個」，改掉等於用猜測取代紀錄。⚠️ 這條的特殊之處：**那段 docstring 正在說明「每加一筆就是保證不成立的又一個地方」** —— 一段講 allowlist 紀律的文字，自己的計數是錯的。解法：下次動該 detector 時查 W02 的 commit 確認原始數字，再改寫或刪除 |
| AD-TrivyFullImage-1 | **trivy 仍只掃 base image，不掃 build 出來的完整 image** —— `security-scan.yml:286-291` 明寫此取捨與缺口（`apt-get install` 進去的套件不被覆蓋）。CH-013 現在**會 build 出兩個 image**，餵給 trivy 的路徑首次存在 | CH-013 | 🟡 P1 | 使用者 2026-08-09 拍板**本次不做**：第一次掃完整 image 會涵蓋應用層 `node_modules`，很可能噴出一批現存 findings，需要 `security-scan.yml:13-37` 明訂的分流窗口五步 —— 混進 CH-013 會讓它收不掉 |
| AD-IaCEvidence-1 | **IaC 掃描義務已移交 infra team —— 本專案沒有 IaC 可掃**。infra 建立全部 Azure 資源（2026-08-08 確認）。`07:31` M0 DoD「IaC skeleton scanned」與 `04:73`「apply 前掃描」雙雙失去標的。**義務未消失，只是換手** | CH-010 | 🟡 P1 | ⛔ M0 收尾**不得**逕行打勾或標 N/A —— 二選一：引用 infra 掃描證據，或明記「由內部第三方營運」。同時擴大 Entity Zero 證據缺口（5→9 項）。ADR-0011「IaC tool deferred to CH-010」的答案＝本專案不選（CH-010 已記錄）。**仍未關閉：答案有了，證據沒有** —— PAR 第 7 點已向 RIT 索取 |
| AD-WebCoverage-1 | **`apps/web` 的覆蓋率不受任何門檻約束** —— W01 Day 3 把 `test:cov` 接進 CI 時只涵蓋 `apps/api`（vitest 需另裝 `@vitest/coverage-v8`，且 `page.tsx` 尚無元件測試，今天開啟只會逼出一個「低到能過」的門檻）。約束 5 的 ≥80% 目前只對後端成立 | W01 | 🟡 P1 | 與其設一個假門檻不如記在這裡。解法：裝 `@vitest/coverage-v8` + jsdom/testing-library，替 `page.tsx` 補元件測試（三個渲染分支已在 Day 3 手動驗過，正好是測試的規格），再把 web 納入 CI 的 `test:cov` |
| AD-CovThreshold-1 | **`apps/api` 的 branches 門檻由 80 降為 70**（`jest.config.js`）—— `emitDecoratorMetadata` 每個被裝飾的建構子／回傳型別都會 emit 一個測試到不了的三元分支（lcov 佐證 `BRDA:31,0,1,0`），真實上限 78.57%。**不是放寬品質要求，是移除一個沒有正確做法能通過的 gate** | W01 | 🟢 P2 | statements / functions / lines 維持 80（現況皆 100%）。再收緊條件寫在 `jest.config.js` 的區塊註解裡：Nest DI 不再需要 reflect-metadata，或該類分支變成可排除時 |
| AD-LintOutput-1 | **`run_all.py:80` 失敗時只保留 detector 輸出的最後一行**，而那通常是提示語不是違規清單 —— CI 失敗訊息無法診斷 | CH-006 | 🟢 P2 | 目前用 workflow 的 `--verbose` 繞過。同型再現 → 改成 `returncode != 0` 時保留完整輸出 |
| AD-Placeholder-1 | ⭐ **「模板佔位符未與本專案對齊」已發生 6 次**（`AD-RuleBoundary-1` / `AD-CssToken-1` / `AD-DocIndex-1` / ADR 檔名 / `CLAUDE.md` byte 預算 / `ci.yml`）| CH-006 | 🟡 P1 | **CH-007 只關掉第 6 類**（actionlint + 棘輪 detector）。⚠️ 原提案的「掃全 repo 佔位符」**已證實不可行** —— 512 命中約 500 個是合法慣例語彙（`W{NN}` / `NNN` / `<slug>`），會噴在自己的規則文件上。其餘四類需語義理解，`lint-detector-authoring.md:22` 明訂寫不出可靠 detector。**W01 關掉第 7 個實例**（`scope-boundaries.md` 的範疇表與 import 矩陣，Day-0 `D-boundaries-matrix` 發現）✅。**本條仍保持開啟** |
| AD-ActionsNode-1 | `actions/checkout@v4` 用 Node 20，GitHub 已標 deprecated 並強制跑在 Node 24（4 個 security-scan job 皆有此 annotation） | CH-006 | 🟢 P2 | 現在只是警告；GitHub 移除 Node 20 支援時會直接壞掉 |
| AD-StaleRecordRef-1 | **跨記錄的「編號引用」沒有任何 detector 在看** —— `ADR-0010:73` 指向 `CH-009`，而 CH-009 早已改派給 track-classification fix；同一份 ADR 的 §相關 在 2026-08-08 記了那次改派，73 行卻沒跟著改。`check_path_references.py` 驗的是**路徑**，`CH-NNN` / `ADR-NNNN` / `AD-Xxx-N` 這類引用不在射程內 | CH-010 → **再現 CH-016** | 🟡 **P1**（升級）| 與 `AD-ChNumber-1` 相鄰但不同：那條講**怎麼選號**，這條講**號被改派後舊引用不會有人叫**。⭐ **升級條件已成立** —— 審計 AD-6 原本被判為「detector 分不出未追蹤檔」，CH-016 量測後**推翻**：9 處 `CH-010` 引用中**只有 1 處是 markdown 連結**（`0010:187`，與檔案同時建立所以 `check_doc_links.py` 從沒機會開火），其餘 8 處是 inline code `` `CH-010` `` —— **那不是路徑，是編號**，兩個 detector 都不在射程內。⛔ **實作前必須先拍板一件事**：`AD-ChNumber-1` 明訂**前向引用預留編號是合法的**（CH-010 正是如此），所以 detector 不能單純要求「編號必須解析得到」—— 需要一個「預留 vs 失效」的判準（候選：前向引用要在 BACKLOG 或 ROADMAP 有對應列）|
| AD-RatingBand-1 | ⭐ **旗艦滾升儀表板數的是「分帶」，而平台交付的是「分數」** —— `02a:414` · `03:90` · `08:25` 全部按 `rating_residual ∈ {High, Critical}` 聚合，但 `rating_*` 從未被建。⛔ 更麻煩的是 `02a` **自相矛盾**：`:405` 說它 derived from the configured matrix，`:429` 說 risk owner **enters** it（系統只是建議）| W05 Day 0 | 🟡 P1 | ⛔ **W05 刻意不建它** —— `02a` §3 的 Risk 欄位規格未列此欄，且 `:429` 本身就是**開放決策 #5「Confirm before M7」**；建了就是替未拍板的決定選邊。**Gate 是 M7 不是 M8**（依 `02a:429` 自己的期限）。ADR-0013 §可證偽條件已把它列為「若分帶不從分數導出，則本 ADR 反對 option B 的前提減弱」|
| AD-SchemaMigrationDrift-1 | **沒有任何機械檢查在看「`schema.prisma` 與 `migrations/` 是否一致」** —— W05 量到 generated column 的 `@default(dbgenerated("..."))` 鏡像文字是**逐字比對**，寫錯會讓其後**每一次** `migrate dev` 產出一句必然執行失敗的 SQL，而今天沒有東西會在 CI 叫 | W05 Day 1 | 🟡 P1 | 現成解法：`prisma migrate diff --from-migrations --to-schema --exit-code`（0=一致 / 2=漂移），需先在 `prisma.config.ts` 補 `datasource.shadowDatabaseUrl`（**key 名實測過，不是 `migrations.shadowDatabaseUrl`**）。⛔ 屬 CI 變更 → 需使用者確認，且吃節流閘配額，故本 phase 不做。⛔⭐ **W11 對「現成解法」投下疑慮，而且是實測**：用 `migrate diff --from-schema/--to-schema` 產 SoA 的 migration，Prisma 7 下 **exit 0、無 stderr、輸出檔 0 bytes**，**而兩個 schema 明確不同**（`StatementOfApplicability` 一邊 0 命中一邊 3 命中）。⇒ 若把「空輸出 + exit 0」讀成「沒有差異」，這條 detector 會是一個**永遠報告一致的假 gate**（`AD-NegativeGate-1` 的形狀）。**接線前必須先用一個已知漂移驗它會不會叫**，`--exit-code` 的行為也要實測而不是照文件相信。⭐⭐ **2026-08-16 外部審查抓到本條的第一個真實實例，而且它現在可以當那個「已知漂移」的測試向量** —— W15 的 `OrgEntity.jurisdiction` 是 **optional** relation 且未寫 `onDelete`，Prisma 對 optional 的預設是 `SetNull`，而 `migration.sql:143` 是 `ON DELETE RESTRICT` ⇒ 兩份真相不一致。同批的另三條 FK 是 **required** relation，`RESTRICT` 剛好就是預設，所以**只有 nullable 那條會漂**；`org_entities_jurisdiction_id_idx`（`migration.sql:131`）同時只存在於 migration 而不在 `@@index` 裡。**四層全空**：無測試（全 repo 沒有任何一條測試檢驗過任何表的 `ON DELETE` 行為）· CI 只跑 `migrate deploy`（`.github/` grep `migrate diff` **零命中**）· 無 lint detector · type-check 不看 DDL。已於同日修正（`schema.prisma` 加 `onDelete: Restrict` + `@@index([jurisdictionId])`，DB 端本來就對故不需新 migration），**修正以 `prisma migrate diff --from-empty --to-schema` 生成 canonical SQL 逐行比對驗證**，非目視。⇒ 接 detector 時**不必再自己造漂移**：`git show` 修正前的 `schema.prisma` 就是現成向量，且它有一個好性質 —— 同檔的 `parent_id` FK 是**應該**保持 `SET NULL` 的對照組，能一併驗出 detector 會不會誤報 |
| AD-BorrowedRefusal-1 | ⭐⭐ **同一形狀第 2 次：新表的跨實體寫入被「別人的」RLS 拒絕，於是新表自己的 `WITH CHECK` 零覆蓋而全部 gate 綠** —— W04 是 counter 借了 W03 的 oracle 防護；W05 是 `risks` 借了 counter 的拒絕（`repo.create()` 先經 `issueRefCode`，policy 中性化後「跨實體寫拒」測試**仍然綠的**）| W05 Day 3 | 🟡 P1 | ✅ **本 phase 已補 int 11b**（繞開 repository、不帶 `ref_code` 直接寫），並**在 RLS 仍中性化的狀態下重跑確認它轉紅**。⛔ **通則未關**：依強度階梯，**第 3 次應改結構性解法** —— 候選 detector：有 RLS policy 的表必須有一個不經共用前置路徑（發號／validator）的直接寫入測試。⚠️ CH-012 的「每個 gate 帶常駐負面案例」對這個形狀**有效**，缺的不是新發明，是**讓「哪個機制被哪個案例守著」變成可機械檢查的**。⭐⭐ **第 3 次已於 W07 發生，門檻正式成立**：`control_tests` / `evidence` 兩個「釘 INSERT policy」的測試第一版**都是綠的**，而綠的原因是 **`assert_parent_in_scope` trigger 先擋** —— 該表自己的 `WITH CHECK` **從未被評估**（N2/N4 中性化在修法前皆 **0 紅**，修法後各 **1 紅**）。⛔ **三次的代勞者各不相同**（W05 ref-code counter · W06 `RETURNING` 的 SELECT policy · W07 BEFORE trigger），所以規則**不能列舉代勞者** —— 唯一可靠的判準是**中性化該 policy 後有測試轉紅**。⚠️ 這也是 `AD-NegativeGate-1` 的子形狀：機制在、案例在、案例卻守著別的東西。⭐⭐ **W08 是第 4 次，而這次是在寫測試時就被設計掉的** —— `actions` 的第四個候選代勞者是**複合 FK**：「INSERT policy 自己會擋」那個測試的顯而易見寫法（`SG1_ISSUE` + `orgEntityId: HK1`）會靠 key 的 23503 通過而 `actions_insert` 從未被評估。實際寫法把 `(issueId, orgEntityId)` 寫成**匹配的一對**，key 滿足、只剩 RLS 能擋；N3 中性化後**只有那一個測試**轉紅。⭐ **這是本條第一次沒有先出事** —— 前三次都是元驗證事後抓到的。**判準沒變**（代勞者四次全不同：counter · `RETURNING` · trigger · 複合 FK，規則不能列舉它們），變的是它現在被用在**設計測試的時候**而不只是驗收的時候。⚠️ 同時量到一個相鄰缺陷：測試 6/9 仍在借 counter 的拒絕，但**名稱比證明的寬**而非覆蓋不足 → `AD-TestNameWiderThanProof-1`。⭐⭐ **W11 是第 6 次，代勞者是第五種，而這次代勞的是「同一張表的另一條 policy」** —— `statements_of_applicability_update` 的 `WITH CHECK` 中性化後零轉紅；補了跨實體 UPDATE 測試（raw、無 `RETURNING`）**重跑仍然零轉紅**。逐條放行隔離後量到：擋住它的是 **SELECT policy**（PostgreSQL 拿 UPDATE 的**新列**去對它檢查，錯誤訊息即 `new row violates...`），本表無 trigger。⛔ **兩條 policy 的運算式相同，所以沒有任何測試能區分它們** —— 這是本條第一次出現「**代勞者與被代勞者無法用測試分開**」的實例，判準（中性化後有測試轉紅）在此**不可滿足**。⇒ 正確做法是把「這半邊今天不承重」寫進 migration 註解，而不是宣稱它承重 → `AD-PolicyClaimUnmeasured-1`。⭐ 反面資料點：同 phase 的 **N2（INSERT policy）是本專案第一次讓 INSERT `WITH CHECK` 真的轉紅** —— 測試 7 是照本條教訓**預先**寫的（繞開發號器 + 不產生 `RETURNING`），第 2 次在設計測試時就被防掉。⭐⭐⭐ **W12 是第 7 次，而這次終於量到答案而不是又一個錯誤宣稱** —— `audit_log` 的 append-only 有兩層（GRANT 無 UPDATE / 缺席的 `FOR UPDATE` policy），前六次的失敗模式是「看到 42501 就宣稱 policy 也成立」。本次 N3 **只補回 UPDATE GRANT**（逐層放行，判準因此可滿足），結果：⛔ **測試回報 `Received: "NO ERROR"`，而我沒有把那句話當成答案** —— 「沒報錯」與「沒改到列」的差別是「還有第二層」與「只剩一層的安全事故」。直接數：`has_table_privilege` **true** · cmd IN (UPDATE, ALL) 的 policy **0 筆** · 該連線**可見 7 列**（所以「0 列」不是因為看不到）· `UPDATE 0` · 事後 `operation` **未變**。⇒ **兩層都擋得住，但只有一層會說話：GRANT 給 42501 的明確錯誤，缺席的 policy 給安靜的零列。** ⚠️ 承重的推論：有人下 `GRANT ALL ON ALL TABLES` 時 append-only **仍然成立但變啞** —— 一道還站著卻不再出聲的防線，是最難察覺它已成為唯一一道的那種。已寫進 `migration.sql:104-124` 與 ADR-0014 的脈絡。⇒ **通則更新**：判準（中性化後有測試轉紅）**在兩層可逐層放行時可滿足**；W11 那次不可滿足是因為兩條 policy 的運算式相同 —— **差別在「兩層是不是同一種東西」，不在層數** |
| AD-CalibrationMetric-2 | ⭐ **`AD-CalibrationMetric-1` 拍板的定義內含一個沒寫出來的前提** —— `actual` = branch base → closeout，而 base 是**前一個 phase 的 closeout commit**，不是本 phase 的起工時刻。W05 跨了一夜：字面套用把 10h20m 睡眠算成工時，ratio 從 0.74 變 **2.22** | W05 Day 4 | 🟡 P1 | 提議定義改為 **branch 上第一個 commit → closeout commit**，並明記「plan/checklist 起草在窗口之外，故為**下界**」。⛔ **必須一併決定既有資料點是否重算** —— W04 用新定義是 3.89 hr / ratio **0.66**（登記值 0.81）。重算一個已登記的點是決定不是整理，**不要默默做**。⚠️ 在拍板前 `pattern-reuse-feature` 的有效樣本數是 **0 不是 1** |
| AD-DecisionSideEffect-1 | ⭐ **寫在「決定的句子裡」的附帶動作沒有勾選框，所以沒有東西會發現它沒做** —— W05 有兩個交付漏做：D4 拍板句寫「**同時**更正 `multi-tenant-data.md:63`」、plan §4 #14 列了 `02a` 裁決註記，兩者 Day 2 都沒做，**Day 4 用 `git diff --name-status` 對照 plan §4 才抓到** | W05 Day 4 | 🟡 P1 | 兩條提議：(a) 拍板時若決定帶附帶動作，**當場在 checklist 加一個 `- [ ]`**，不要只寫在裁決表的理由欄；(b) Day 4 closeout 固定加一步 `git diff --name-status <base>..HEAD` 對照 plan §4 File Change List。⚠️ (b) 本次是**手動**做的且立刻有產出 —— 成本 < 1 min，值得寫進 closeout checklist 而不是靠記得 |
| AD-DesignNoteAnchor-1 | **design note 的 `file:line` 錨點隨每個後續 phase 衰減，而沒有任何機械檢查在看** —— W05 在 `schema.prisma` 加 5 個 model，就讓 W04 design note 的四組錨點各偏 3–8 行；`check_path_references.py` 只驗**路徑存在**，不驗那一行是不是宣稱的東西 | W05 Day 4 | 🟢 P2 | ✅ 已手動校準 W04 design note（§2.1/§2.5/§3/§4）。**分流原則**（本次建立）：**design note 是活參考**（`Status: Active`，明寫供 slice 2..N 複製）→ **造成偏移的 phase 負責重新校準**；**change record 是歷史快照** → 不追，追了就是無界工作。長期解：detector 驗 `file:line` 指向的內容含某個 anchor 字串。⭐ **同一族的第二種形態，同一個 phase 內就發生了**：**rebase merge 改寫 commit SHA**，於是 closeout 文件裡寫的七個 SHA 全部不在 `main` 上（`785be55`→`c70e57a` … `f9195da`→`8f08f3f`）。⚠️ **而我第一次掃描只 grep 了其中 2 個就回報「兩行要修」，實際是 5 處** —— 掃描範圍比問題窄，正是 `feedback_evidence_must_support_claim` 的形狀。判準統一為：**文件引用 commit 一律用 merge 後 `main` 上的 SHA**，branch 側 SHA 只在需要說明改寫時並列。detector 候選同時涵蓋兩種形態：`file:line` 內容比對 + **`git merge-base --is-ancestor <sha> main`**。⛔ **原本這裡寫的是 `git cat-file -e`，實跑一次證明它無效**：修正前的 `f9195da`/`785be55`/`d74d093` 三個 SHA **全部通過 `cat-file -e`**，因為本專案 merge 後不刪 feature branch，物件仍可達 —— 那個守衛跑在有 bug 的文字上會是綠的。**「這個 SHA 存在」與「這個 SHA 在 `main` 的歷史上」是兩個問題**，而讀文件的人問的是後者。⚠️ 這是 `AD-NegativeGate-1` 家族在**同一天內第三次**，且第一次的對象是**我自己一小時前寫下的守衛提議** —— 它被抓到只因為那次驗證是**真的跑了**而不是寫完就相信。⭐⭐ **修好的判準第一次真跑就找到 W01 遺留的兩處，那是 detector 現成的驗收案例**：`CALIBRATION-LOG.md:87` 與 `W01/retrospective.md:52` 用 `e6ddff1`（16:30）→ `6ec4bf9`（19:57）論證 W01 的 3h27m 工時，而**兩個端點都不在 `main` 上**。⛔ 且比 W05 那三個更嚴重 —— `git branch -a --contains` 對兩者皆回**空**（W01 的 feature branch 已不存在），**在一份新 clone 上它們連 `cat-file -e` 都不會過**。這條 stale 自 **2026-08-08** 活過四個 phase、五次 closeout，**沒有一次察覺**。⛔ **刻意不當場修**（節流閘：順路發現、不阻塞、非安全）—— 留給 detector 當第一批命中，**那才是驗收它有效的時機**，比手動改兩行有價值。~~slice 3 處理~~（使用者 2026-08-11 排序）→ ⛔ **W06 就是 slice 3，而 detector 沒建、W01 兩個失效 SHA 也沒動** —— W06 的 plan §4 從頭到尾沒有這一項，所以它不是漏做而是**從未被排進去**。⚠️ 這句「slice 3 處理」自己就活成了 stale，**在它所描述的那個 slice 收尾時無人回頭看** —— 與 `CH-015`/`CH-016` 同一個形狀（解封條件寫在沒人會回頭翻的地方）。**重排已完成（W07，2026-08-12）：使用者明確再延，且再延的登記改放 [`ROADMAP.md`](./ROADMAP.md) 主線第 9 列 ⬜ —— 不再放在本欄。** ⛔ **那正是這條 AD 這次被抓到的失效形狀本身**：備註欄沒有人會逐條回頭掃。⭐ W07 另交付了**上游的**便宜解法 `AD-MdAnchorLineShift-1`（被錨定的文件編輯不得改變行數），它讓偏移**不發生**而不是事後告警；但它管不了刻意的結構性改寫，detector 仍需要。⭐⭐ **第三種形態（W06 closeout 實地踩到）**：rebase merge 不只讓**文件**引用的 SHA 失效，**接續的分支本身也建在那些 SHA 上** —— PR #42 從已合併的 feature branch 延續，於是 GitHub 拿它的 9 個 commit 對上 `main` 已有的 8 個等價 commit，**每個 W06 檔案都衝突**。⛔ **我當時用 `git diff origin/main HEAD`（樹相同）就放行了，而那個檢查答錯了問題** —— merge 不比樹，是以 merge base（`eabb437`）做三方合併。判準補上：**rebase merge 之後，接續的 closeout 分支一律從 `origin/main` 開，不從已合併的 feature branch 延續**；驗證用 `git rev-parse HEAD~1` 是否等於 `origin/main`，**不是** `git diff`。修法 `git rebase --onto origin/main <old-base> <branch>` + `--force-with-lease`。⭐ **W15 Day 2 為 detector 量到三個新的現成命中，而且是乾淨的**（本片對 `multi-tenant-data.md` 零行數變動 ⇒ 這三處與 W15 無關，純屬既有 stale）：`entity-scope.resolver.ts:16` · `:39` · `entity-scope.resolver.spec.ts:80` 三處引 `multi-tenant-data.md:145` / `:144-149` 並宣稱那裡講「滾升是子樹不是繞道」，而 `:145` 實際是**「查不到資料時回 404」**那一節；正確位置在 `:160-162`（滾升三條件）。成因是 W04 / W05 對該檔的兩次編輯（`156e8ea` / `8f08f3f`），**自 W02 起活過十一個 phase 無人察覺**。⇒ 這是 `STATUS_AUDIT` §2.7 `AD-16` 的第 **4 / 5 / 6** 個實例（該條只列了 3 個，且明說掃描範圍過窄是它自己的問題）。⛔ **刻意不當場修** —— 與 W01 兩個失效 SHA 同樣留給 detector 當第一批命中，**那才是驗收它有效的時機**。⭐⭐⭐ **2026-08-16（W15，第 7 / 8 次）—— 本條一直在解一個天然約束，而它其實是一個我們自己開的開關**：使用者問「為什麼這個專案的 PR 一直是 rebase and merge」，查證後答案是 **`main` 的 `required_linear_history = true`**（`gh api .../branches/main/protection` 實查；repo 層級 `allow_merge_commit` **其實是 `true`**，是分支保護把那顆按鈕擋掉的）。成文理由在 `git-workflow.md:268`：「保持線性歷史，單人專案沒有合併提交的價值」，設定由 `CH-015` 建立、W01 PR #18 首次套用。⛔ **關鍵在於這句理由說的是「merge commit 沒有好處」，而不是「SHA 穩定沒有價值」** —— 而 **merge commit 根本不會改寫 SHA**。⇒ 本條 AD 累積到第 8 次的全部成本，**直接來自那個設定本身**，不是來自任何無法迴避的技術限制。三個選項的實際取捨：**rebase**（線性 ✅ / SHA 穩定 ❌ **已付 8 次** / 逐 commit 敘事 ✅）· **squash**（✅ / ❌ / ❌ —— 會把每則都是一篇文件的 commit message 壓成一則，摧毀本專案投入最多的那份資產）· **merge commit**（❌ / **✅** / ✅）。⚠️ **不建議現在改**，而且線性歷史對本專案有一個非顯而易見的加分：這是一套 ISMS 治理平台，**一條直線的歷史比 DAG 容易對稽核人員證明「什麼時候發生了什麼」** —— 那可能正是當初的真實理由，只是沒被寫下來。⇒ 本條的登記方式因此要改：它**不是**「rebase 會改寫 SHA，我們得適應」，而是「**我們用 SHA 穩定性換了線性歷史與可稽核性，代價目前是 8 次**」。**若走到第 9 / 10 次，該被重新檢視的是那個設定，不是又一版錨點規則。** ⭐ 連帶今日量到第三個設定的副作用：**`delete_branch_on_merge = true`** —— PR #67 merge 時遠端分支被刪，而同時進行的 closeout push **把它重建了**，`git push` 回報的 `* [new branch]` 是**唯一**訊號。⛔⭐ 而排查時第一個直覺（`git merge-base --is-ancestor <舊 SHA> main`）**給出完全錯誤的答案** —— rebase 改寫過 SHA，12 個 commit 全被列為「main 上沒有」，看起來像損失了一整個 phase。**正解是比內容不比 SHA**（`git diff --stat <branch> origin/main` 回 8 檔 39 行，全是已知的修正）。⇒ 判準補上：**rebase merge 之後，「這個分支還有沒有獨有內容」只能用內容差異回答，`--is-ancestor` 在這個問題上結構性失效** |
| AD-AssetScales-1 | **`02a:208` 列了 `Asset.value` / `Asset.criticality` 但從未定義值域** —— W05 因此**不建**這兩欄，並在 schema docstring 寫明它們是刻意缺席 | W05 Day 2 | 🟢 P2 | ⛔ **不可借用 `02a:116` 的 5 點量表** —— 那是 risk **impact** 的描述子（財務／營運／法規衝擊），不是資產價值。建它們之前必須先規格化值域，否則就是自行發明欄位（已確認參數 #9）。⚠️ 本條於 W05 Day 2 被命名但**直到 Day 4 才真的登記**，正是 `AD-AdRegistry-1` 講的形狀 |
| AD-RiskBand-1 | ⭐ **旗艦儀表板數的是分帶（`rating_*`）不是分數（`score_*`），而分帶怎麼來從未被拍板** —— `02a:414` · `03:90` · `08:25` 全部按 `rating_residual ∈ {High, Critical}` 聚合；而 `02a:405` 說它 derived、`:429` 說 risk owner **enters** it，**同一份文件自相矛盾**，且 `:429` 本身是開放決策 #5「Confirm before M7」| W05 Day 0 | 🟡 P1 | ⛔ **W05 刻意不建 `rating_*`** —— 建了就是替一個未拍板的決定選邊。**M8 之前必須有人拍板**：分帶是從 1–25 分數用設定矩陣算出來的，還是 owner 輸入而系統只建議？前者可以是第五個 generated column，後者不行。⚠️ 這條卡的是**決策不是證據** |
| AD-SpecMergeFieldByField-1 | ⭐ **合併兩份定義的決定，必須逐欄位「含 enum 成員」比對，不能只比對欄位名** —— W09 的「`Assessment` 不建表」裁決是基於欄位重疊做的（`assessor_user_id` vs `assignee_user_id` 等），而兩邊的 `subject_type` enum **各有一個對方沒有的值**（`process` vs `vendor`）。合併於是**無聲刪掉了一整個 subject kind**，而那不是任何人權衡過的取捨 | W09 → CH-025 | 🟢 P2 | ⭐ **這條是 `AD-AssessmentProcessSubject-1` 關閉時留下的通則** —— 那個實例已由 CH-025 修好（使用者裁決補 `process`），但**形狀會再出現**：`02a` 還有多處同一實體在 §0 索引、§3 欄位表、模組文件三處各描述一次。⚠️ 提議：任何「這兩個是同一個東西」的判定，要列出**兩邊的完整欄位集合含 enum 成員**再下結論；差集非空時，差的每一項都要有明確去向。⛔ 不做成 detector —— 判定發生在人的推理裡，不在檔案裡 |
| AD-NeutraliseRebuiltState-1 | ⭐⭐ **對「每次執行都會重建的東西」做中性化，等於什麼都沒做，而它看起來像結果** —— W09 Day 3 的第一次 N1 用 `psql` 對 `isms_test` 直接 `DROP CONSTRAINT`，然後跑 int suite 得到 **20/20 全綠**。那不是「守衛是多餘的」，是 int 的 global setup 每次都 `isms_test rebuilt, migrated and seeded`，ALTER 在第一個測試開始前就被覆蓋 | W09 Day 3 | 🟡 P1 | ⭐ **抓到它的唯一原因是預期方向寫在前面**（`AD-MetaVerificationBug-1`）—— 沒有那張表，20/20 會被讀成「沒有測試依賴這個守衛」，而正確結論是「**這次量測什麼都沒量到**」。修法：中性化一律改**來源**（migration SQL / 設定檔），不是改**狀態**（資料庫 / 執行中的程序）。提議寫進 `verification-discipline.md` 的元驗證段落。⚠️ 同族的還有 Risk Class C（陳舊 dev server 掩蓋 wiring 修正）—— **都是「你以為你在測的東西，跟你實際動到的東西不是同一個」** |
| AD-AssessmentDefinitionUnvalidated-1 | **`assessment_templates.definition` 是完全不驗證的 JSONB** —— `02a:326` 只說模板有 sections 與 questions，`05:42` 補了 question type 與 evidence-required flag，但**沒有任何一處給出結構**。所以一份 `{"anything":[1,2,3]}` 是合法模板（int spec 測試 3 明寫這件事）| W09 Day 1 | 🟢 P2 | ⛔ **不寫一個假的 validator 充數** —— 斷言一個我發明的結構，會讓每個後續讀者把它當成保證。對照組是 `extensions`：它**有**驗證，因為 extension catalog 宣告了什麼可以出現（ADR-0005）；這裡沒有東西宣告。二選一並寫下來：規格補結構，或明記它永遠是自由文件 |
| AD-AssessmentQuestionNoFk-1 | **`assessment_responses.question_id` 指向模板 JSONB 內的鍵，不是 FK —— 所以「未被問過的題目」可以被作答** | W09 Day 1 | 🟢 P2 | 與 `AD-IssueBareEnum-1` 同族（規格給了引用的一半）。⛔ **建 `AssessmentQuestion` 表就是自行發明實體**（已確認參數 #9，且 `02a` §0 明文「不在索引上的不得建置」）。int spec 測試 4 把這個限制寫成**通過的測試**，若日後有 questions 表，那個測試就是要改成拒絕的那一個。M7 之前與 `AD-IssueBareEnum-1` 一起拍板 |
| AD-ResponseRefCodeCost-1 | **每一個答案都要一個 `ref_code`，而發號是 per-entity 的序列化點**（W04）—— 一份 40 題的提交會走 40 次 `ref_code_counters` | W09 Day 2 | 🟢 P2 | ⚠️ **本 phase 沒有量到真實數字** —— 端點一次一筆，沒有批次提交路徑，所以「40 題 40 次」是**從程式碼推導**而非實測，⛔ 未寫成已量測。⛔ **不自行發明豁免** —— §1.1 沒給，而唯一的既有豁免（`User`，`02a:286-292`）是**逐欄位寫了理由**的。有批次端點時實測再決定 |
| AD-VendorAuditorSod-1 | **`05:47` 的職責分離只實作了一半** —— 「reviewer 不得是 assignee」已由 `assessment_instances_sod` CHECK 強制；「供應商稽核的稽核者需獨立於關係經理」**無處可實作**，因為 `vendors` 與關係經理欄位都不存在（`02a:59` 放在 Wave 2）| W09 Day 1 | 🟢 P2 | 已寫進 migration 的註解，**讓缺口從 schema 本身看得見**而不是只在文件裡。⚠️ guardrail 6 要求職責分離，而今天平台對「供應商稽核」這個情境**只做到一半且沒有任何東西會提醒** —— `vendors` 出現的那個 phase 必須一併補 |
| AD-UniqueKeyOracle-1 | ⭐ **任何呼叫端可選的唯一 tuple 都是 existence oracle** —— 唯一索引**不受 RLS 管**（W07 對 RI 檢查量到的同一性質）且**早於複合 FK 觸發**。W10 Day 2 實測 `rm_report_versions(report_id, version_label)`：A 寫入 B 的報告用 B **已有的**標籤 → **23505**（DETAIL 印出 B 的 report id 與標籤）；用 B **沒有的** → **23503**。差異即 oracle，可一次一猜列舉別實體的全部版本標籤 | W10 Day 2 | 🔴 **P0 候選** | ✅ 本 phase 已修（`org_entity_id` 進唯一鍵；對合法列完全冗餘，因複合 FK 已強迫兩者相等）並**重測確認兩者同碼**，加常駐回歸測試（int 12）+ N5 證明它會紅。⛔ **通則未關**：判準是「這個 tuple 是呼叫端給的嗎」—— 是 → `org_entity_id` 必須在鍵裡。⚠️ `ref_code`/`id` 不需要（伺服器發／伺服器生成，呼叫端無從選）。**值得一條 detector**：掃 schema 的每個 `@@unique`，其欄位若全部可由 request body 供給且不含 `orgEntityId` 即 fail。與 `AD-NegativeGate-1` 同族 —— 這是一個**沒有任何 gate 會叫**的形狀。⭐⭐ **W11 取得第 2 個資料點，而它同時確認了判準可移轉並修正了失敗模式的描述**：`statements_of_applicability(framework, clause_ref)` 兩半都來自 body，從第一版就把 `org_entity_id` 放進鍵，N3 實測拿掉後 —— 撞別實體持有的 clause → **23505**，撞沒人持有的 → **成功**。⛔ **不是 W10 的「23505 vs 23503」** —— 本表沒有 parent FK 可以掉下去，所以對比是「錯誤 vs 成功」，**oracle 更響亮且更容易被誤讀成正常行為**。⇒ detector 的判準不變，但「兩個不同的 SQLSTATE」這個描述**太窄**：正確的判準是「兩個**可分辨的結果**」，成功也算一個 |
| AD-NarrowPatternWideClaim-1 | ⭐⭐ **拿一個窄 pattern 的命中數，去回答一個需要讀內容才能回答的問題 —— 同一週 4 次** —— CH-027 E3（嚴格 pattern 只中 88/91，差額才是真相）· CH-027 E8 · W11 D3（`REF_CODE_PREFIX` 寫死常數名得 10 個，實為 **14** 個）· W11 D8（grep `refCode:` 零命中就推論「那兩張表沒有 ref_code」，實際原始碼寫的是 shorthand `refCode,`）| CH-027 · W11 Day 0 | 🔴 **P0 候選** | ⛔ **四次沒有一次是被 gate 或紀律抓到的，全部靠交叉檢查**（14 個 prefix vs 15 個帶 refCode 的 model 對不上）。⚠️ 兩次的**結論碰巧是對的**，所以錯誤的方法**被自己的正確結果掩護**了 —— 這是它能重複四次的原因。與 `AD-GrepAssertion-1` 相鄰但不同：那條是「拿為人類排版的文字做機器判斷」（工具層），本條是「拿代理指標回答需要語義的問題」（推論層）。依 `.claude/rules/README.md` 強度階梯已達**第 3 級**，且 CH-027 與 W11 是**同一週**⇒ 逼近第 4 級（`UserPromptSubmit` hook）。提議：非零命中**逐處讀**；零命中先反問「它若存在會長什麼樣、會在哪」；枚舉類問題**必須有第二條獨立路徑交叉檢查**。⚠️ hook 每回合付 context 成本 → 由使用者排序，不當場實作（節流閘 Step 0.0）。⭐ **W12 再現 3 次，而三次的處置品質分成兩級**：D7（追 CRLF 代價時連續下錯**兩個**結論 —— 抓到第 1 個的是拿 W11 當**對照組**，抓到第 2 個的是換一個儀器 `git ls-files --eol`）是**事後**才發現；而 D13（`repeat('00',64)` 產生 128 個字元而非 64，讓 genesis 比對誤判為鏈壞了）· D15（`tail -25` 讀 type-check log，把被截掉的那段當成不存在）· **D19**（第一版 benchmark 說「稽核讓寫入變快」）三次都是**當場**先懷疑量法。⭐ D19 最值得留：多插一列不可能讓寫入變快，所以我沒有去找「為什麼稽核比較快」的解釋 —— 那正是前面幾次踩坑的動作。⇒ 通則收斂成一句可執行的：**當一個便宜的量法給出一個「意外」的答案時，第一個要懷疑的是量法本身，不是被量的東西**。⛔ 但本 phase 同時證明它**還沒內化**：closeout 寫覆蓋率時直接引用了 R4 的手寫計數（19），導出分母才發現真值是 21 → `AD-RiskTableCountManual-1` |
| AD-PolicyClaimUnmeasured-1 | ⛔ **migration 註解宣稱了一個從未量過的因果，而量出來是相反的** —— W11 Day 1 寫「只有 `USING` 的話呼叫端可以把自己的列搬進別的實體」；Day 3 逐條放行量到擋住它的是 **SELECT policy**（PostgreSQL 拿 UPDATE 的新列去對它檢查），`_update` 的 `WITH CHECK` 今天**完全不承重** | W11 Day 3 | 🟡 P1 | ⚠️ **同一句話 W06 也寫在 `controls` 的 migration 裡**，而那裡**可能是對的** —— `controls` 的讀比寫寬（group-shared 列可被不該擁有它的實體讀到），所以它的 `WITH CHECK` 可能真的承重。⛔ **今天沒有人量過** → 需要一次跨實體 UPDATE 的實測。通則提議：policy 註解裡任何「拿掉這半邊會怎樣」的宣稱，**要嘛有一個中性化實測，要嘛明確標「未量測」**。⭐ 抓到它的不是 review 也不是 lint，是「中性化零轉紅 → 補測試 → 仍零轉紅 → 去查」——**唯一有效的偵測機制仍然是中性化**。與 `AD-BorrowedRefusal-1` 同族，但本條的難點是**兩條 policy 運算式相同時測試分不開它們** |
| AD-DevDbChecksumDrift-1 | ⛔ **`prisma migrate dev` 對任何人都被擋住** —— W10 在 `20260813071857_rm_report_snapshot` **套用之後**就地更正了它的註解，Prisma 因此要求 reset 開發資料庫（**為了一段註解丟掉整個 dev DB**）| W11 Day 1 | 🟡 P1 | 影響：W11 的 migration 只能**手寫**。⚠️ int suite 不受影響（每次 DROP+CREATE，`_prisma_migrations` 為空，無 checksum 可比）—— 這也是為什麼它一直沒被發現。⭐ **W11 因此建立了一條可複用的前置檢查**：改任何 migration 的註解之前，先查 `_prisma_migrations` 該筆在不在（W11 查過，`20260814023210_soa` 不在 → 零漂移）。⛔ 修法需要人決定（reset dev DB / 手動改 checksum / 接受現況），且屬環境操作 → 不當場做。⚠️ **2026-08-14（W12）Day 0 確認仍在**（`isms_dev` 的 `_prisma_migrations` 最新是 `20260813153153`，`20260814023210_soa` 不在裡面）⇒ `20260814065711_audit_log` **第二次手寫**。本 phase 全部 DB 實驗改跑 scratch DB（`isms_w12_scratch`，用完即刪）與每次重建的 `isms_test`，**未動 `isms_dev`**，故不新增漂移。⚠️ **W14 第三次撞上、第三次繞開**：兩個 migration 都因此**手寫**（`--create-only` 跑不起來），且 checklist 1.2 的 Verify 指令 `prisma:migrate` **未照抄執行**，改以 int suite 的 `rebuilt, migrated and seeded` 為證。⛔ **三次繞開意味著沒有任何 phase 會去修它** —— 每次繞開都很便宜，而那正是它活到第三次的原因。⭐⭐ **W15（2026-08-16）是第 4 次，而本列自己漏記了它 —— 這條 AD 因此有了一個比計數更精確的症狀** —— W15 的 plan §8 **事先**要求「在 retro 明寫，因為『每次繞開都很便宜』正是它活到第四次的原因」，retro（`:173-175`）與 progress（`:25`、`:244`）**都照做了**，而 **closeout 的 BACKLOG 更新清單漏了本列** ⇒ 直到下一個 phase 起草前的排查才發現本列仍寫「第三次」。⛔ **所以真正的失效不是「繞開很便宜」，是「繞開的紀錄散在三份文件而只有一份是單一來源」** —— retro 與 progress 是**該 phase 的**紀錄，本列才是**跨 phase 的計數器**，而更新前者不會更新後者。⇒ 這與 `AD-CountBeforeLastEdit-1` 同族（計數器手動維護必漂），只是這次漂的是**次數**不是條數。⭐ **W15 第 4 次還留下一個具體的未驗證面**，下一個 Day 0 直接可用：Prong 3 的證據是 int suite 的 `[int] isms_test rebuilt…`，**不是 `_prisma_migrations` 查詢** ⇒ **`isms_dev` 的 head 從未被驗證**，而 int suite 每次 `DROP + CREATE`、`_prisma_migrations` 為空，**結構上看不見 checksum 漂移** ⇒ 用 int suite 當 Prong 3 的證據，對本條 AD 而言等於沒驗。⛔ **升級判準**：`.claude/rules/README.md` §強度階梯是「違反 3 次以上 → 結構性解法」，W15 的 `AD-TestNameWiderThanProof-1`（3/3）與 `AD-TextEditStructuralScope-1`（第 3 次）都被明確援引該階梯，**本條 4 次卻從未被援引** —— 那本身就是它一直沒被解的機制 ⭐⭐ **W17 Day 1 關掉了它，而關掉它的是一個六次繞開都沒有人試過的指令**：`npx prisma migrate deploy` 把 `isms_dev` 從 **17 / 23** 一次補到 **24 / 24**，零錯誤。⇒ **本條的射程比它的標題窄**：被擋的是 `migrate dev` 的**額外功能**（shadow DB + drift 偵測 + 產生新 migration），**不是套用 migration 本身**。六個 phase 的「繞開」其實只需要換一個子指令。⚠️ 因此本條**不關閉**但降級為記錄：`migrate dev` 仍不可用（要 reset dev DB），而那只影響「用 Prisma 產生 migration」這一件事，本專案自 W11 起一律手寫 migration，所以那個限制今天沒有成本。⭐ 真正的教訓是**繞開的敘述會固化**：五次 phase 記錄都寫「`migrate dev` 被擋」而沒有人問「那 `deploy` 呢」。 |
| AD-ModuleFileZeroCoverage-1 | **每新增一個 `*.module.ts` 就稀釋一次全域 stmts/lines** —— 11 個 module 檔全部 **0%**（DI wiring 只被 int suite 走過，而 int 跑在另一個 jest config，不計入 unit coverage 報告）| W11 Day 2 | 🟢 P2 | W11 實測：stmts 92.01 → 91.83、lines 93.44 → 93.29，而 branches / funcs **上升**。⚠️ 問題不在覆蓋率本身（門檻 80/70/80/80 全過），在於 **plan §5 的驗收寫「coverage 不低於 baseline」而那條規則對本專案的結構是不可滿足的** —— 每個新模組都會讓它失敗一次，於是它會慢慢變成一條大家學會忽略的驗收。提議二選一：把 `*.module.ts` 排除在 unit coverage collection 之外，或把驗收改成「branch / funcs 不低於 baseline」。⛔ 不要靠「每次解釋一下」——那正是驗收失去意義的方式 |
| AD-CalibrationDay0InOrOut-1 | ⭐ **`pattern-reuse-feature` 的四點跨 0.23~0.84 不是雜訊也不是雙峰，是同一欄混了兩種量法** —— 0.23（W08 逐段）/ 0.24（W10）**不含 Day 0**；0.50（W09）/ 0.84（W08 窗口）**含**。三個 phase 都把它讀成「樣本髒」 | W10 Day 4 | 🟡 P1 | `CALIBRATION-MATRIX.md` 需增一欄「窗口含不含 Day 0」。⛔ 在那之前算 3-phase 移動平均是在平均兩種不同的量，**調乘數只會把問題藏得更深**。⚠️ 與 `AD-CalibrationIdleGap-1` 相鄰但不同：那條問「等待要不要扣」，本條問「Day 0 算不算」。⛔ 不回頭重算舊點（`AD-CalibrationMetric-2` 前例）。⭐ **W11 是第一個事先宣告量法的點**（plan §7 寫「含 Day 0」），ratio **1.13 IN** —— 本欄第一個 IN-band 點。⛔ **但它同時暴露第三個模糊處，而 W10 的分界線沒有涵蓋**：宣告的窗口是「branch 第一個 commit → closeout commit」，而 plan / checklist 的**起草**發生在第一個 commit **之前** ⇒「含 Day 0」本身有兩種讀法（含起草 / 只含驗證）。所以新增的那一欄不能只寫 yes/no，要寫**三態**（不含 / 只含驗證 / 含起草）。⛔ 今天符合「同一種量法」的點數是 **1**，離 3-phase 證據還很遠 |
| AD-ImmutableRowRetention-1 | **不可變表與軟刪除／保存期限互斥，而規格同時要求兩者** —— `05:76` 要 RM report 版本保存 3 年後歸檔，`02a:99` 要每個實體有 `retired_at`；而 `rm_report_versions` 沒有 `FOR UPDATE` policy，所以**沒有任何路徑寫得了 `retired_at`** | W10 Day 1 | 🟡 P1 | 欄位刻意保留（schema docstring 明記它是結構性凍結的），讓缺口從 schema 看得見而非藏在文件裡。歸檔路徑出現時二選一：加一條窄的 policy（只允許改 `retired_at`，需 column-level GRANT —— 本 repo **無先例**），或改由另一張處置紀錄表承載。⛔ 不要為了「補齊」而現在就加 policy —— 那會拆掉本 phase 唯一的不可變性保證 |
| AD-PartialGateReportedAsFull-1 | ⛔ **一個較窄的檢查被當成較寬的結論回報** —— W10 Day 3 的 commit 寫「gate 全綠」，實際只跑了 int · lint · cov · run_all，**少了 format / type / build / web**；Day 4 最終掃描才發現 `format:check` 在 Day 3 就已經紅了 | W10 Day 4 | 🟡 P1 | `AD-GrepAssertion-1` 的同族形狀，換了位置：這次被冒充的不是某條 policy 而是**自己的流程**。⚠️ 危險在於它**每次都會成功一段時間** —— 窄的那組通常是綠的，所以錯誤的結論看起來被證實了。提議：`_templates/phase/checklist.md.tpl` 的 Day 3 §3.x **逐項複製 Day 2 §2.x 的 gate 清單**，不要寫「跑 gate」這種可以任意縮小的字。⛔ 不要靠「記得跑完整套」—— 本條的證據就是我在最警覺的那天沒記得。⭐⭐ **W11 是第 3 次，而成因不同且更難防**：W11 Day 2 的 full gate **十一項全跑且回報正確**，紅的是 Day 3 —— **中性化本身改了 `soa.int.spec.ts`**（補 `track` 參數 + 測試 12），而 full gate 停在那之前。⇒ 這不是「回報縮水」而是「**gate 的時間點早於最後一次改動**」，同一個提議的修法仍然涵蓋它（Day 3 逐項複製清單），但理由要換成「**中性化會改 code**」。⚠️ 三次全部是 `format:check`，因為它是唯一對「純空白／換行」敏感的 gate |
| AD-SchemaHeaderStale-1 | **`schema.prisma` 的 header 連續兩片未更新** —— MHist 停在 W07（W08 / W09 皆未加行），`Purpose` 的「13 models」在 W10 動手前實際已是 18 | W10 Day 1 | 🟢 P2 | ⚠️ 本 phase 只修正**自己這次改動讓它變得更錯的部分**（count → 20 · Scope → W10 · 加 W10 一行），缺的 W08/W09 兩行**刻意不回填** —— 連續兩片漏同一件事是值得記錄的 pattern，不是值得靜靜補掉的瑕疵（節流閘）。提議 detector：header 宣稱的可導出數字（model 數）與實際比對，不符即 fail。與 `check_entity_index.py` 同形狀，成本極低。⭐ **W15 後外部審查：第 3 次，且範圍比本條原本描述的更寬** —— `schema.prisma:2` 宣稱 22 而 `grep -c '^model'` = **26**；同一天在 `audit.module.ts:50` 量到**同形狀的第二個檔案**，而那一處的份量更重：它自稱 `⭐ DERIVED, NOT TRANSCRIBED — rerun the derivation rather than trusting these sixteen strings` 並**寫出了推導指令**，照它自己的指令重跑得 26 而非它宣稱的 23，連帶 `:53` 的 `FIVE MODELS ARE ABSENT` 應為**八個**（漏 `Jurisdiction` / `Regulation` / `Obligation`）。⇒ 提議的 detector **不該只掃 `schema.prisma` 的 header**：判準應是「**任何自稱可導出的數字，都要能被它自己寫出的那條指令重現**」。⚠️ 這比原提議好驗證也好寫 —— 指令就在註解裡，detector 只要執行它並比對 |
| AD-W15ConstraintSurfaceUntested-1 | ⭐ **W15 交付的全部價值就是一個約束面，而其中一半沒有任何測試會因為它消失而變紅** —— 該片零端點／零 repository／零 controller，交付物是 4 條 FK + 1 條 UNIQUE + 3 條 GRANT + 1 個 enum + 欄位形狀。逐項量測：(a) 第四條 FK `regulations_jurisdiction_id_fkey`（`migration.sql:134`）**零測試** —— 測試 4/5 插 `obligations`、6 插 `org_entities`，7 雖然插 `regulations` 但走 `asApp()` 在 42501 就停且傳的是**有效** jurisdiction id；(b) `GRANT SELECT` 只有 `jurisdictions` 被正面讀取證明（`codes()` 只查該表），`regulations` / `obligations` 撤掉 SELECT 仍全綠；(c) 測試 7 名為「can write none of the three」但**只覆蓋 INSERT 一個 verb**，改成 `GRANT SELECT, UPDATE, DELETE` 三個 `rejects` 仍全部 42501；(d) plan §5 AC-2 的欄位形狀（含 D2 刻意省略的四個 base 欄位）無任何可執行檢查 | W15（外部審查 2026-08-16） | 🟡 P1 | ⚠️ **寫下來的 7 條測試品質很高、沒有一條恆真**（全用 SQLSTATE 不用訊息字串、`IN_SCOPE` 取自 `15:41` 是外部事實而非從 fixture 導出）—— 問題在**沒被寫下來的那些**。⛔ (c) 的份量最重：測試 7 是 plan §3.1 D3（三張表不進 `AUDITED_MODELS`）的**唯一支撐**，而一條被誤放的 `GRANT UPDATE` 產生的正是**無稽核軌跡的寫入**（guardrail 5）。修法就在同 repo：`rls-direct.int.spec.ts:104-112` 對 `policies` 把 INSERT / DELETE / SELECT 三個面各寫一條；更好的是比照測試 3 改用 catalog 斷言 —— 對 `information_schema.role_table_grants` 斷言 `isms_app` 的 privilege 集合**恰好等於 `{SELECT}`**，一次釘住全部 verb 且未來新增 verb 會自動紅。⚠️ **測試 4 / 5 / 7 從未被任何中性化實驗證明過會紅**（progress.md 的 N1/N2 預測表把三者都列為 GREEN，且 `progress.md:381` 自己誠實寫了「今天不宣稱」）—— 這是記錄在案而未登記成待辦的缺口，補證成本極低（各一行 `DROP CONSTRAINT`）。⭐ 現況不緊急的唯一理由是 `GRANT SELECT` only 讓應用層寫不進去；**M6 補上 repository 與端點的那一片，這四個缺口會同時變貴**，因為屆時有應用層路徑可以繞過它們 |
| AD-EntityScopeNoDriftGuard-1 | ⭐⭐ **稽核維度有 drift guard，實體範疇維度沒有 —— 而後者是 guardrail 4** —— 加表忘了接稽核，`audit-coverage.int.spec.ts:515-546` 會**自動轉紅並指名該模型**（W14 實測過，Attestation 就是這樣被抓出來的）；加表忘了啟用 RLS，**沒有任何東西會紅**。實測：`pg_class` / `pg_tables` / `information_schema` 在 `apps/api/src`（排除 `generated/`）**只命中 1 處**，就是 W15 新增的 `jurisdiction.int.spec.ts:115`，而它**硬編碼三個表名**（`:52` 的 `THREE`），釘的是 W15 那三張表而非「未來所有表」。`multi-tenant-data.md:363-369` 建議的六個 detector（`check_business_table_entity_col` / `check_entity_scoped_query` / `check_endpoint_scope_dep`…）**一個都沒實作**；`run_all.py` 的 8 個 detector 全是文件／規則 hygiene 類，`check_entity_index.py` 檢查的是「model 有沒有登記在 `02a` §0 索引」而**完全不看 entity_id 或 RLS** | W15（外部審查 2026-08-16） | 🟡 P1 | ⛔ **這不是 W15 造成的，是既有缺口 —— 但 W15 讓它第一次有具體代價**：該片論證的核心命題是「全域是刻意的，不是忘了加 RLS」，這個命題今天由測試 3 + 兩次中性化撐得很好，**但它成立的前提是「哪天真的有人忘了，會有東西告訴我們」** —— 那個東西在實體範疇維度上不存在。⇒ 一個未來的 phase 加了帶 `org_entity_id` 卻沒 `ENABLE ROW LEVEL SECURITY` 的表，會安靜地通過全部 gate，而它與 W15 這種**刻意**不加 RLS 的表在事後**長得一模一樣**。⚠️ 這是 `AD-NegativeGate-1` 的家族（規則寫了、沒有東西在數），但**不要當成它的第 N 次就算了** —— 稽核維度已經證明這類 guard 做得出來且會叫，兩邊的落差是可以直接抄的。**隔離失敗不是一般 bug，是合規事故**（CLAUDE.md 約束 8） |
| AD-W15InvariantInCommentOnly-1 | **W15 三張全域表有兩個結構不變式只寫在註解裡，沒有任何約束在維持它們** —— (a) `schema.prisma:936-938` 自述 `obligations.jurisdiction_id` 「redundant against `regulation.jurisdiction_id` **by construction today**」並以「讓『某管轄區的 obligations』變成單表查詢」為保留理由，但 `migration.sql:137` 與 `:140` 是**兩條各自獨立的單欄 FK**，沒有東西在 construct 它 ⇒ `regulation_id = PDPA[SG]` 配 `jurisdiction_id = HK` 同時通過兩條 FK，此後單表查詢與 join 查詢對「HK 有哪些 obligations」給出互相矛盾的答案，而單表查詢正是該欄位存在的唯一理由；(b) `regulations` / `obligations` **無自然鍵唯一約束**（`jurisdictions.code` 有，`migration.sql:119`），而 schema `:940-947` 為「保持 global」提出的論證正是「**一個條文 = 一列，不把法條複製一份**」 | W15（外部審查 2026-08-16） | 🟢 P2 | ⭐ **(a) 的修法是這個 repo 自己的既有 pattern，不是新發明**：`schema.prisma` 有 **7 處** `@@unique([id, orgEntityId])` 配合複合 FK `references: [id, orgEntityId]`，用途一字不差就是「子表帶著冗餘的父鍵時要引用整組，兩者才不可能互相矛盾」。`Regulation` 加 `@@unique([id, jurisdictionId])`、`Obligation` 改用 `references: [id, jurisdictionId]` 即可 —— **W15 是唯一符合這個 pattern 卻沒套用的地方**。⚠️ **嚴重度被 `GRANT SELECT` only 壓低**：三張表對 `isms_app` 是唯讀，矛盾資料今天只可能由 migration / seed 產生，不可能由 API 產生 ⇒ 這是「Wave 2 開寫入路徑**之前**的前置條件」，不是「現在會壞」。⛔ 但要在那之前做 —— (b) 的直接後果是**沒有唯一鍵就寫不出 idempotent 的 importer**（`ON CONFLICT` 沒有 target 可指），而 Wave 2 的法規內容 import 依本質會被重跑；等到那時再補，可能已經有重複資料要先清 |
| AD-RegulationVersionCollision-1 | **規格欄位與 base 欄位撞名，而規格的那個語意靜默消失了 —— 第 2 次** —— `Regulation` 依 `02a:198` 需要「版次」（例如 **`2012 Rev.3`**），而 base 已有一個 int `version`（樂觀鎖計數器）。W15 §3.1 D4 決定**只留 base int**，複製 `Policy` 的既有做法（`schema.prisma:272`）⇒ **「這條 obligation 屬於法規的哪一版」表達不出來**。⛔ **`Policy` 已經付過完全相同的一筆代價，而沒有任何地方記錄它** —— 這是同形狀第 2 次，依專案規則（2 次即立規則）本條成立 | W15 Day 1（plan §3.1 D4） | 🟢 P2 | 被否決的替代方案與理由要一起記著，否則下次會重新爭論一遍：改名 **`edition`** 被否決是因為**偏離規格字面**；而 W11「欄位名不叫 `framework_id`」的先例**管不到這裡** —— 那條先例說的是「名字不該宣稱一條不存在的邊」，`edition` 沒有那個問題，它只是換個字。⇒ 提議的規則：**base 欄位與規格欄位撞名時，規格語意不得靜默消失** —— 要嘛改名並在 design doc 記錄偏離，要嘛保留原名而在 schema docstring 明寫「規格的 X 語意本表不承載，去向是 Y」。⚠️ **解封條件**：Wave 2 真的要回答「這條 obligation 引用的是哪一版法規」的那一片（`10:69` 的 `ObligationControlMapping` 之後）。⭐ 本條的價值有一半在**它是被 plan 主動抓出來的** —— D4 自己寫了「代價要寫下來」，而 `Policy` 那次沒有人寫 |
| AD-CalibrationNoTimeRecord-1 | ⭐⭐ **執行完整發生了，而追蹤文件一格都沒動 —— 兩個獨立症狀，同一個根因** —— (a) **W15 的 progress.md 全檔零時間記錄**（`min｜hr｜分鐘｜actual｜elapsed` 不分大小寫全部零命中），而 `task-workflow.md` §Step 5 明寫「per-day 估算住在 progress.md」；(b) **checklist 的 Day 0 / Day 1 共 21 項在 closeout 時仍是 `[ ]`**，儘管 progress.md 對這兩天有含數字、原文輸出與 commit SHA 的完整證據。⇒ 後果不是「沒做」，是**做了而沒有被記錄成做了**：calibration 的分子只能事後從 commit author date 反推，且**量法是在知道答案之後才選的** ⇒ ratio **1.235 是下限不是測量值** | W15 Day 4 | 🟡 P1 | ⛔ **這正是 `task-workflow.md` §Common Violation Patterns 已經列名的兩條**（「事後補 checklist ⇒ 資料品質差 ⇒ 估算無用」「跳過 progress.md ⇒ 細節已遺失」）—— 規則寫了、正確、而**沒有任何東西在執行它**（`AD-NegativeGate-1` 的家族，這次落在流程紀律上而非設定上）。⚠️ 與 `AD-CalibrationDay0InOrOut-1` 的關係要講精確：那條要求**量法事先宣告**，W14 照做了、**W15 漏了** ⇒ 本條不是它的重複，是它**首次被違反**的紀錄。候選修法（機械的，因為勸世文已經證明無效）：closeout 前跑一個 detector，對 active phase 檢查 (1) `progress.md` 是否含至少一個時間數字、(2) `plan.md` §7 是否含量法宣告字串、(3) checklist 的未勾項是否在 progress.md 有對應敘述 —— 三者任一不成立即 fail 並指名。⭐ 判準與 `check_backlog_counts.py` 同形：**比對兩份互相宣稱的文件，而不是要求人記得** |
| AD-IsActiveNeverBuilt-1 | **`02a:100` 把 `is_active` 列為 §1.1 base field，而全 schema 零張表有它** —— `information_schema` 實測 **0**，W02 至今 31 個 model 沒有一個實作。每一片都在「照 §1.1 抄」，而這一項從來沒被抄過 | W16 Day 3 | 🟢 P2 | 二選一：從 §1.1 移除，或補 generated column。⛔ 一份**有成員無實作**的基準清單比沒有清單更糟 —— 它讓「照 §1.1 建了」這句話不可驗證。⚠️ 與 `AD-SchemaHeaderStale-1` 同族（宣稱與現實分歧），但這次分歧在**設計文件**側不是註解側 |
| AD-IsmsProfileSpecGaps-1 | **`13` 有三處資料層解不了、M6c 前必須裁決** —— (a) `iso_officer_name`（`13:27`）與 `ISMSContact(role='ISMS lead')`（`13:41`）是**同一個人的兩份紀錄**，使用者裁定「全建 `13:27`」故兩者都建了；(b) `13:33` 說 OpCo administrator **can edit**，而設計交付物 `permMatrix.js:11` 給該角色 **Read**，且另給 `13:33` 完全沒提的 Regional ISO **Edit**；(c) `status` / `region_code` / `posture` 三欄被 W16 拒絕（各有依據），M6c 若需要必須有裁決而非默默補 | W16 | 🟡 P1 | 三者都要 UI 與角色模型存在才有標的 —— `Role`/`Permission` 被 `02a:71` 擋到 **M4**。⭐ **W16 的 AP-3 那 1 次也在這裡解封**：`ISMSProfileVersion` 今天零消費者（五張表都是），使用者知情下裁定建它 |
| AD-SchemaTemporalConventionSplit-1 | **同一個 schema 有兩種時間慣例，而宣稱「對齊」的那句註解已經不成立** —— `RMReportVersion:1817-1818` 逐字寫「Timestamptz, matching Issue.dueDate and ControlTest.scheduledFor. **A second temporal convention for one column would be the invention, not the alignment**」，但 `Risk.reviewDue`（W05）與 `Regulation.effectiveDate`（W15）**都是 `@db.Date`** | W16 Day 1 | 🟢 P2 | W16 是第 3 個必須重新判斷的人（選 `@db.Date`，理由：憑證發證／到期是**日曆事實**，帶時區的瞬間是這份資料沒有的精度）。提議：定一條可執行判準（來源文件的日曆日期 → `Date`；系統事件 → `Timestamptz`）並修那句註解。⛔ 不要靠每次重新判斷 |
| AD-IntSuiteNoMutex-1 | **兩個 int suite 並行會互相摧毀，而錯誤訊息完全不指向真正的原因** —— `int-global-setup.js` 無條件 DROP + CREATE `isms_test`。W16 實測（我自己造成）：**13 紅 / 4 suites**，含 `soa` · `policy` · `entity scoping` 這些與該次改動無關的 suite，外加一個 `PrismaClientConstructorValidationError` | W16 Day 2 | 🟡 P1 | setup 取一把 advisory lock，或以 worker id 分資料庫名。⭐ **本條的價值在於它差一點被當成成功**：那次是中性化實驗，而「比預期更紅」看起來像中性化更徹底 —— 會回頭質疑的唯一理由是**紅的位置解釋不通**（N1 只動一條 FK，解釋不了 `soa` 為什麼紅）|
| AD-NeutralisationCountUnderPredicted-1 | **五次中性化，三次預測不正確，三次都是把紅的條數估低**（N2a 0 vs 預測 1、N3a 2 vs 1、N3b 3 vs 1）。⭐ N2a 揭露更根本的一件事：**seed 本身是一條對 schema 的斷言，而沒有任何東西把它標成斷言** —— 縮小父表唯一鍵時 fixture 先炸，得到 setup crash 而非具名失敗 | W16 Day 2 | 🟡 P1 | (a) 預測**承諾形狀與位置，對條數給區間** —— 一個好的測試套件本來就會讓一個改動觸發多條斷言；(b) ⭐⭐ **「比預期更紅」本身不帶資訊，帶資訊的是紅的位置**：W16 的 N1（多 12 紅＝汙染）與 N3b（多 2 紅＝覆蓋）是相反的失敗模式，分辨方法只有「每一條紅是否都能由該改動解釋」|
| AD-RetentionDurationUnstructured-1 | **`retention_policies` 的三個欄位建了但填不了** —— `duration` 是 TEXT 而非 interval，`trigger` / `disposition` 是 nullable enum 且六列 seed **全部留空**。⭐ 原因不是偷懶：`05:73-80` 只有三欄（class / retention / basis），而 `02a:314` 自己寫著「the six confirmed **classes and periods**」。`duration` 的六個值也不是同一種量 ——「3 years after closure」（相對事件）·「3 years per version」（相對版本）·「Contract term + 2 years」（相對一份此處讀不到的外部合約）·「7 years, immutable」（帶著拒絕處置的旗標） | W17 Day 1-2 | 🟢 P2 | 結構化前必須先回答兩個今天無來源的問題：「**per version 的錨點是哪一列**」與「**contract term 從哪裡讀**」。⛔ 在此之前 M6b 的處置排程**無法從這張表算出到期日** —— 這是本片交付的東西的一條明確上限，寫在這裡而不是留給 M6b 自己撞上。**解封條件**：M6b 開工時與使用者一起裁決值域，或確認公司程序另有來源。⚠️ 六列 seed 逐字取自 `05:73-80`（已確認參數 #9），**不得**為了讓欄位有值而回頭補寫 —— 那正是發明欄位 |
| AD-LegalHoldScopeRefUnguarded-1 | ⛔ **`legal_holds.scope_ref` 沒有任何參照完整性**，而它是本 repo 第三個多型欄位 —— 前兩個（`evidence.linked_id` · `attestations.subject_id`）都由 W14 的 `assert_polymorphic_parent_in_scope()` 守著。⭐ **那支函式在這裡結構上不可用，這是量出來的不是推測的**：`polymorphic_parent_guard/migration.sql:47` 把 id 欄 `::uuid` cast，而 mapping walk 在 `:52-59` ⇒ **cast 早於分支**；`02a:318` 的 `class` 目標是紀錄類別（`'Security incident records'`），會拋 **22P08/22P02** 而非契約承諾的 23503。且 `record` 泛指**任何**業務表（今天 31 張），而該函式要求每個 type value 對應**一張具名的表** | W17 Day 1 | 🟡 P1 | ⛔ **刻意不建只涵蓋 `entity` 分支的守衛** —— 那會是綠燈、有斷言、對兩個真正需要檢查的分支全盲，即 `AD-VacuousScopeTest-1` 的形狀（W14 的 D5 在 group-shared control 上踩過同一個坑）。**解封條件**：M6b 第一個必須解析 hold 的消費者 —— 屆時 `scope_type` 的值域才有真實需求可依，而不是照抄一份規格裡的三個詞。⚠️ 今天的緩解只有一條：`legal_holds` **無 `GRANT UPDATE`**，所以一列寫錯的 `scope_ref` 連改都改不了，只能新增一列；那降低了汙染但**完全不阻止**第一次就寫錯 |
| AD-SpecFieldValueSourceUnverified-1 | ⭐ **Day-0 的 content verify 能證明「欄位名對」，不能證明「這個欄位填得出來」** —— W17 Day 0 的 `D-fields-r` 勾了 ✅「`02a:314-316` 的 6 個欄位名逐字比對相符」，**而那是真的**。直到 Day 2 寫 seed 才被逼問出 `trigger` / `disposition` / `review_cadence` **沒有 per-row 來源**：`05:73-80` 只有三欄，而 `02a:314` 自己就寫著「the six confirmed **classes and periods**」。⇒ Day 1 把兩者建成 NOT NULL，Day 2 必須改 schema + migration + 回退重套 `isms_dev` | W17 Day 2 | 🟡 P1 | 提議：Day-0 Prong 2 對**新表**加一列 —— 逐欄回答「**這一欄的值從哪裡來？**」，答案是「規格只給了值域」就標記為 nullable 候選。⚠️ 成本很低（規格本來就要逐欄讀），而漏掉的代價是**改 schema 的 migration 已經套用之後才發現**。⭐ 與 `AD-NarrowPatternWideClaim-1` 同族但方向相反：那條是**問對問題但量錯**，這條是**量對了但問錯問題**。⛔ 本條不可用「以後記得」結案 —— 它已經以「欄位名比對」的形式存在於 checklist 裡，而那一格**打勾了**卻沒能防止它 |
| AD-CatalogAssertionPredictionBlindSpot-1 | **中性化預測連續兩次低估條數，而兩次是同一個盲點** —— W17 的 N3（`GRANT UPDATE`）與 N5（`GRANT INSERT`）各預測 1 紅、實測 2 紅，多出來的那條**都是測試 12**：一條用 `toEqual` 斷言**整個 grant 集合**的 catalog 測試，所以它對「grant」這個維度的**任何**改動都會反應 | W17 Day 3 | 🟢 P2 | ⭐ **教訓不是「條數估高一點」**（那是 `AD-NeutralisationCountUnderPredicted-1` 已經說過而顯然不夠的版本），是**認出哪些測試是「維度級」的**：一條斷言整個 catalog（grant / policy / RLS flag / 欄位清單）的測試，在預測任何改動該維度的實驗時**必須預設算進去**。⚠️ 判準仍然成立且本片通過了：**每一條多出來的紅都能由該改動解釋** ⇒ 是覆蓋不是汙染（W16 的 N1 沒過這關 —— 多出的 12 紅是並行跑 suite 的汙染）|
| AD-GuardMatchesItsOwnDisclaimer-1 | ⭐ **一個「某構造不存在」的斷言，命中了說明它不存在的那段散文** —— W17 為中性化寫的守衛 `assert "FOR UPDATE" not in text`（先確認沒有 UPDATE policy，否則 N3 不代表任何事）**fire 了，而且是誤判**：命中的是 migration 註解裡的「⛔ NO \`FOR UPDATE\` policy and NO \`GRANT UPDATE\`」。**那個片語出現在檔案裡，正是因為那個東西不存在** | W17 Day 3 | 🟢 P2 | 修法：`sql_only()` 先剝除註解行再比對。⭐ 與 W17 Day-0 的 **D4**（`FORCE` 單空格 pattern 漏掉 5/8 命中，差點推出「16 張表有 guardrail-4 缺口」的假 P0）、**D7**（`AD-UniqueKeyOracle-1` 的計數在其 base commit 上三個數字都不可重現）同一族：**拿裸文字回答一個需要結構的問題**。⚠️ 本次代價方向是好的 —— 守衛 **fail-closed**，得到的是誤報而不是假結果。⇒ 通則：**寫得越好的程式碼，越會在註解裡提到它刻意不做的事**，所以「grep 不到就是沒有」在有註解的檔案上系統性不可靠 |
| AD-MergeLabelRuleConflict-1 | ⭐ **兩條專案規則對「CLAUDE.md 的 `Current Phase` 格能不能放 PR 編號與 commit SHA」給出相反的指令，而兩條都沒有指出對方** —— `task-workflow.md` §Phase Closeout 的禁止清單明列「**commit SHA / PR 編號**」不得進表格格；`git-workflow.md:223-226` §Post-Merge Status Flip 則**要求**把那一格翻成 `MERGED (PR #N, <sha>)` | W16 post-merge | 🟢 P2 | ⛔ **不是紙上衝突，2026-08-16 具體化了**：依 git-workflow 翻標籤使 `CLAUDE.md` 從 29,984 → **30,003 bytes**，`check_rules_hygiene.py` **fail**（budget 30,000）。修法是刪掉同一格裡的 `calibration 0.84 IN` —— ⭐ **而那也在同一份禁止清單上** ⇒ 為了容納一條被禁止的內容，刪掉了另一條被禁止的內容，而 lint 對兩者都無意見（它只數 bytes）。⚠️ 真正的缺口不是「哪一條對」，是**衝突只有在預算恰好卡住時才會現形** —— 若當時餘裕有 100 bytes，這兩條規則會繼續各自為政不知道多久。候選解法：(a) `git-workflow` 改為只翻 BACKLOG 與 phase 文件，`CLAUDE.md` 那一格不帶 PR/SHA（**導航檔不該是狀態面**）；(b) `task-workflow` 的禁止清單明文豁免「當前 phase 的 merge 標籤」。⛔ **不當場修** —— 改規則走 Change 軌（節流閘：順路發現、不阻塞、非安全）|
| AD-RebaseStaleShaRef-1 | ⭐ **closeout 文件裡的 commit SHA 引用在 merge 之後全部失效，而在作者的機器上永遠驗得過** —— branch protection 要求 linear history ⇒ PR 一律 **rebase merge** ⇒ feature 分支的每個 SHA 都被改寫（W16 七個 · W17 六個，`Compare-Object` 兩次實測**零重疊**）。而 closeout 文件是在 merge **之前**寫的，所以「預測鎖在 commit `X`」這類**證據錨點**指向的 SHA 在 `main` 上根本不存在 | W17 post-merge | 🟡 P1 | ⛔ **本機是偽陰性，所以自檢抓不到**：舊 SHA 仍在本地 feature 分支上 ⇒ `git cat-file -t 57d13c6` 回 `commit`、`git show` 成功 —— **任何在作者機器上做的驗證都會通過**。正確判準是 `git merge-base --is-ancestor <sha> origin/main`（W16 的 `020fe11` 實測 **EXIT=1 ⇒ 不在 main 上**）。⚠️ **已發生 2 次**：W17 的 4 處已於本次 post-merge 修正（`05c2156`→`d2abdc8` · `57d13c6`→`0e80253`），**W16 的 `020fe11` 未修** —— 順路發現、不阻塞、非安全 ⇒ 依節流閘不當場改。⭐ 形狀屬於 `feedback_evidence_must_support_claim.md`：**拿一個便宜的代理指標（本機能否解析）去回答一個只有 `origin/main` 能回答的問題**。候選解法：(a) closeout 文件改用**內容錨點**（「progress.md §N 的預測表」）而非 SHA；(b) post-merge 步驟加機械檢查 —— 掃 phase 資料夾裡的 7 位 hex 並逐個對 `origin/main` 跑 `is-ancestor`。⚠️ (b) 要先確認不會誤判文件裡引用的**他人 repo** SHA |

**優先度判準**：

| 級別 | 判準 |
|------|------|
| 🔴 P0 | 正在傷害使用者 / 阻擋其他工作 / 是已知的 Potemkin |
| 🟡 P1 | 有明確價值，但可以等 |
| 🟢 P2 | 想做，沒有明確觸發條件 |
| ⚪ P3 | 記著，可能永遠不做（誠實一點，不要假裝會做）|

> **定期誠實化**：P3 的東西放超過 10 個 phase 還沒動 → 刪掉它。
> 一份沒人相信的待辦清單比沒有清單更糟。

> 📋 **跨來源審計 2026-08-10（首次）—— 6 個漂移發現（AD-1 ~ AD-6）**：
> [`STATUS_AUDIT.md`](./STATUS_AUDIT.md) §2.7。**細節不複製到這裡**（PROCESS R7 / STATUS_AUDIT §5）。
> AD-4 揭露的 M0 缺口已登記為 `AD-SecDoDAutomation-1`（本表）——
> 審計當下它是六條裡唯一**沒有任何 AD 在追蹤**的一條。

---

## §Shipped Phases Pointer Index

<!-- 每個完成的 phase 一行。完整敘述在 memory subfile + retrospective。 -->
<!-- 這裡**只放指標**，不放 file:line / 測試數 / drive-through 細節。 -->

| Phase | Date | 一句話 | Detail |
|--------|------|--------|--------|
| — | 2026-08-07 | CH-001：跨境欄位分級，residency 邊界改為設定 | `docs/03-implementation/changes/CH-001-cross-border-field-classification.md` |
| — | 2026-08-07 | CH-002：24 個設計交付物資料檔對照規格審計（關閉 `AD-Mockup-1`）| `docs/09-analysis/mockup-data-vs-spec-audit-20260807.md` |
| — | 2026-08-07 | CH-003：`02a` §0 實體索引 + 三項 P0 模型決策（關閉 `AD-DesignAlign-3/4`、`AD-Mockup-4`、`AD-Model-Vendor`、`AD-Model-AuditIssue`）| `docs/03-implementation/changes/CH-003-entity-index-and-p0-model-decisions.md` |
| — | 2026-08-07 | CH-004：30 個 screen fragment 對照規格審計（純發現）| `docs/09-analysis/screen-fragment-audit-20260807.md` |
| — | 2026-08-07 | CH-005：三份基礎 ADR 拍板（0001 NestJS+Prisma · 0006 Azure 分區 · 0007 Entra ID）—— **解封 M0**；關閉 OQ-1/2/5 | `docs/03-implementation/changes/CH-005-foundation-adrs/` |
| — | 2026-08-07 | CH-006：修好 `ci.yml`（自第一個 PR 起 11/12 次 run 失敗）—— **CI 首次綠燈**，`run_all` 首次在 CI 執行，gitleaks 首次掃描全歷史 | `docs/03-implementation/changes/CH-006-repair-ci-gates.md` |
| — | 2026-08-07 | CH-007：actionlint（pin+SHA）+ 未填佔位符棘輪 detector；修 3 個既有 shellcheck 問題。**原「掃全 repo 佔位符」提案經枚舉後推翻** | `docs/03-implementation/changes/CH-007-placeholder-detector.md` |
| — | 2026-08-08 | CH-008：**中國移出範圍**，拓撲收斂為單一區域 × 3 環境（ADR-0010 取代 0006）。覆蓋 grep 掃 34 檔／173 處，改 18 檔；順帶發現 CH-005 漏更新 `docs/architecture.md` §3+§5 | `docs/03-implementation/changes/CH-008-china-out-of-scope/` |
| — | 2026-08-09 | **CH-012**：三個 gate 各帶一個常駐負面案例（boundaries fixture + `lint:negative` · i18n parity 雙向 · 安全標頭逐條對照 `16`）—— MERGED (PR #20, `2a4160f`)。**每個都用「弄壞它看它紅」驗過**；順帶抓到 `Permissions-Policy` 缺席與 `helmet({xPoweredBy:false})` 靜默失效。關 W01 checklist 2.3 | `docs/03-implementation/changes/CH-012-resident-negative-gates/` |
| — | 2026-08-09 | **CH-013**：`image-smoke.yml` + `smoke-probe.mjs` —— 兩個 Dockerfile 首次被 build 並啟動探測。**首航即抓到一個真實缺陷**（build 階段在 schema 存在前跑 `prisma generate`），該缺陷通過了全部既有 gate。關 `AD-ImageBuild-1`；`AD-NegativeGate-1` 3→4/5 | `docs/03-implementation/changes/CH-013-image-build-and-run-smoke/` |
| — | 2026-08-08 | CH-009：修三軌分類在 pre-code 階段的失效 —— Change 判準加「既定設計」+ 兩條「不走軌」路徑；**修好 `PROCESS.md` §3.1 與 §4.1 對同一輸入的相反路由**；關 `AD-RuleBoundary-1`（ADR forcing-function 判準入 `14-adr/README.md`）| `docs/03-implementation/changes/CH-009-track-classification-fix/` |
| **W01** | 2026-08-08 | **Monorepo scaffold** — MERGED (PR #18, `ce72564`)，`closed_partial`。八個休眠 gate 從「回報 SUCCESS 但什麼都沒檢查」變成真的會叫；**同形狀「綠燈但空轉」一個 phase 內 5 次** → `AD-NegativeGate-1`。M0 DoD：3 關閉／2 部分／1 無標的 | `docs/01-planning/W01-monorepo-scaffold/retrospective.md` · `docs/03-implementation/changes/CH-011-w01-monorepo-scaffold.md` |
| **W02** | 2026-08-09 | **Entity-scoping RLS spike** — MERGED (PR #25, `02dffef`)，`closed`。第一張業務表與它的隔離同一個 migration；**約束 8 四個範疇測試在應用層與資料庫層各自成立**（20 個整合測試，8 個完全不經應用層）。**推翻 Day-0 一項承重結論**：fail-closed 只在從未被 scope 過的連線上免費 → 補 `app_entity_scope()`。**裁決 ADR-0001 §可證偽條件 #1 未觸發** → ADR-0004 拍板，關 OQ-3 | `docs/01-planning/W02-entity-scope-rls-spike/retrospective.md` · `docs/03-implementation/changes/CH-014-w02-entity-scope-rls.md` · `docs/02-architecture/design-notes/W02-entity-scope-rls.md` |
| — | 2026-08-10 | **CH-015**：六個 CI check 設為 required（`strict: false`）—— 在此之前 `required_status_checks` 整個 key 缺席，全綠擋不住任何東西。關 `AD-CIRequired-1`（解封條件成立兩次未被察覺）。✅ **擋與放兩個方向都已觀測**（PR #29：check 未完成 → `BLOCKED`，六綠後 → `CLEAN`，兩時點 `mergeable` 皆為 `MERGEABLE`）| `docs/03-implementation/changes/CH-015-required-status-checks.md` |
| — | 2026-08-10 | **CH-017**：`task-workflow.md` §Step 0.0 節流閘 + hook 擴充 —— **順路發現的問題預設記進 BACKLOG，不當場開 CH**（例外：阻塞／安全／使用者要求），每 phase 最多 1 個治理工具 CH。⭐ 根因：rolling planning **只綁 Phase 軌**，Change 軌無節流。實據：第 4 天產品 911 行 vs 工具 3,287 vs 文件 20,909，16 個 CH 裡 0 個產品功能 | `docs/03-implementation/changes/CH-017-discovery-throttle.md` |
| — | 2026-08-10 | **CH-016**：`ROADMAP` / `RISK_REGISTER` / `DEFERRED_REGISTER` 三份**從未被使用**的權威來源首次填入。關審計 AD-2 / AD-3。⭐ 與 CH-015 **同一個根因第 4、5 次**（解封條件寫在沒人回頭看的地方）；`RISK_REGISTER` 兼作 Entity Zero 的**過渡承載體**，四條標記待 M4/M5 遷入平台自己的系統。順帶量到 **M1 的 DoD 依賴尚未拍板的 ADR-0005**（`07:32`）| `docs/03-implementation/changes/CH-016-activate-tracking-registers.md` |
| **W04** | 2026-08-10 | **M1 slice 1 — the shape every table copies** — MERGED (PR #34, `5bb0c9f`)，`closed`。ADR-0012 拍板（`users` **全域無 `org_entity_id`** —— 範疇是 role assignment 的屬性不是人的）+ `Policy` base field 缺口 6→1 + 伺服器端發號。⭐ 改的是 `multi-tenant-data.md` 的**分類本身**（identity 立為第三類），不是在例外清單加一列。⭐ 元驗證產出**新知識**：counter 的 RLS 失效讓 W03 的 oracle 防護**重新變得可區分** —— W04 的發號路徑成了 W03 那個保證的一部分。🚩 Day 3 首次探測即 500（`permission denied for schema public`）而**全部 gate 綠** → 第 7 個負面 gate + `AD-DbBuildPathParity-1`。關 `AD-UserEntitySpec-1`（`User` 半邊；`Role`/`Permission` 改由 `02a` §0 + M4 承載）| `docs/01-planning/W04-m1-user-and-base-fields/retrospective.md` · `docs/03-implementation/changes/CH-019-w04-user-and-base-fields.md` · `docs/02-architecture/design-notes/W04-user-and-base-fields.md` |
| **W06** | 2026-08-12 | **M1 slice 3 — row-level scope, and the test that measured the wrong half** — MERGED (PR #41, `3a3606b`)，`closed`。`Control` 是第一張**範疇屬於列而非表**的表（ADR-0014）：三條 per-command policy，**`SELECT` 寬 / `INSERT`·`UPDATE` 窄且拒 `group` 值 / 無 `FOR DELETE`**。⭐ 量測否決了兩個看似可行的形狀 —— 單一不對稱 `FOR ALL` 讓任何 OpCo 能**自建 / 升格 / 刪除 / 奪取** group 列（`DELETE` 沒有 `WITH CHECK`），而 `extension_fields` 的 catalog 豁免**不可移轉**到業務表。⭐ **缺席的 policy 比窄的 policy 更嚴格**（故意授予 `DELETE` 後量到全拒）。🚩 元驗證找到真缺口：`WITH CHECK` 全中性化後**一個測試都沒紅** —— Prisma `create()` 的 `RETURNING` 讓 SELECT policy 先擋，所以三個「繞開發號」測試（含 W05 條款 2 的形狀）證明的是**讀的 policy 藏住了列** → `AD-ReturningMasksCheck-1`，改用 `createMany` 後 0 紅變 2 紅。⭐ **US-6 裁決：W05 條款 1 夠用 / 條款 2 需再加**（被實測推翻）；**W04 七不變式 可複製 6 / 需補充 1**（權限檢查在 RLS 之前）。關 `AD-ClaudeMdBudget-1`（headroom 196 → 1,521）與 **W05 checklist 2.4 的 🚧**。⛔ calibration **無有效 actual** | `docs/01-planning/W06-m1-control-and-asset-endpoints/retrospective.md` · `docs/03-implementation/changes/CH-021-w06-control-and-asset-endpoints.md` · `docs/14-adr/0014-row-level-entity-scope-and-per-command-policies.md` · `docs/02-architecture/design-notes/W06-row-level-scope.md` |
| **W07** | 2026-08-12 | **M1 slice 4 — the guard a foreign key could not be** — MERGED (PR #44, `19bc4f7`)，`closed`。第一張**父表拒絕複合錨點**的子表：`controls` 明文不建 `@@unique([id, org_entity_id])`（M7 連結表兩側 entity 本就不同），`evidence.linked_id` 多型**連 FK 都沒有**。⭐ 先量再設計：**RI 檢查繞過 RLS**（M1 —— 指向看不到的父列 INSERT **成功**），與 M2c 的 `23503` 合起來是**存在性 oracle**，發生在資料庫層、controller 改不掉。機制由量測導出：`BEFORE INSERT OR UPDATE` + `SECURITY INVOKER` trigger。⭐⭐ **關掉 oracle 的是「執行順序」不是「有 trigger」**（M5）—— BEFORE 跑在約束之前，不存在的 id 也走 `NOT EXISTS`，`23503` 兩路同碼；順序相反的話 oracle 會原封不動活下來**而外觀上像修好了**。SQLSTATE 由 `42501` 改判 `23503`（歸因：父列不可達 vs 列自身越界）並**重新量測**。🚩 元驗證 **8/8 全紅**，其中 N2/N4 **修法前是 0** —— 兩個「釘 INSERT policy」的測試綠在 trigger 先擋 → `AD-BorrowedRefusal-1` **第 3 次，門檻成立**。⛔ Day 2 報了**兩個假 gate**（`tail -N` 藏住 api 的紅）→ `AD-GrepAssertion-1` 新形狀。⛔ Day 3 工時表把**推估當量測**（82 min 實為 31 min）→ `AD-EstimateAsMeasurement-1`。關 `AD-GroupRowTheft-1`（先看到紅才修）· `AD-ReturningMasksCheck-1` · `AD-CalibrationNoActual-1`。⭐ calibration **首次乾淨適用**：ratio **0.27**，但 `actual/bottom-up` **0.17** 低於 0.4 下限 → **該修估算不是乘數** | `docs/01-planning/W07-m1-control-test-and-evidence/retrospective.md` · `docs/03-implementation/changes/CH-022-w07-control-test-and-evidence.md` · `docs/02-architecture/design-notes/W07-cross-entity-references.md` |
| **W08** | 2026-08-13 | **M1 slice 5 — the criterion finally had two answers** — MERGED (PR #47, `74d8d56`)，`closed`。`Issue` + `Action`（CAPA）+ 兩組端點 → **14 / 36 實體**。⭐⭐ **W07 的 D1 判準第一次導出「另一個」答案**：它說 trigger 是父表**結構上**給不起錨點時才用的，而在此之前每個父表都給不起 —— 只有一個答案的判準還不算判準。`issues` 沒有 M7 連結表的約束，所以 `actions` 走**複合 FK**。🚩 **N1 是本 phase 的結論**：移掉那把鑰匙，跨實體引用**插入成功**，恰好 3 個測試轉紅、其餘 8 個不受影響 —— 擋住它的確實是它。順帶量到 **FK 免費涵蓋 UPDATE**（W07 的 trigger 必須明寫 `OR UPDATE`）。⭐ **`AD-BorrowedRefusal-1` 第 4 次在寫測試時就被設計掉**：測試 8 的 `(issueId, orgEntityId)` 刻意寫成**匹配的一對**好讓複合 FK 無法代勞，N3 證明只有它轉紅。⭐ **第一個 body 帶 enum 的端點** —— Prisma 拒絕未知 variant 的錯誤不帶本 app 認得的 SQLSTATE，所以 `severity:"urgent"` 沒有守衛時是 **500**；合法值由 `Object.values()` **導出**不抄寫。⛔ **N6 第一版是壞掉的元驗證**（改名後仍是孤兒，`run_all` 照樣 7/7、EXIT=0 讀起來像通過）→ `AD-MetaVerificationBug-1`。⛔ 對 poll 失敗給了未驗證的 IPv6 解釋，實測推翻，改記為**根因未確定**。關 `AD-EntityCountDerivation-1` · `AD-EntityIndexIncomplete-1`（detector `check_entity_index.py`，`run_all` 6/6→**7/7**，且它一跑就推翻分母：**36 不是 35**）| `docs/01-planning/W08-m1-issue-and-action/retrospective.md` · `docs/03-implementation/changes/CH-023-w08-issue-and-action.md` |
| **W05** | 2026-08-11 | **M1 slice 2 — the asset-based risk chain** — MERGED (PR #36, `700f5d6`)，**`closed_partial`**（AC-4 的 `AssetGroup`/`Asset` 四項範疇測試未達成，解封：slice 3 的 `POST /assets`）。已確認參數 #7 / #8 從散文變成 runtime：五張表（3 entity-scoped + 2 全域庫）+ `POST/GET /risks`，評分是 **generated column** 所以呼叫者物理上寫不了（ADR-0013）。⭐ 量測否決了一個**看起來更好**的選項：`IMMUTABLE` 函式版把公式從 8 處塌縮成 1 處，但 `CREATE OR REPLACE` 在有相依 generated column 時**成功且不重算** → 一欄兩世代。⭐ **FK 檢查繞過 RLS** → entity-scoped 表之間的 FK 全部改複合。🚩 元驗證找到真缺口：RLS 全中性化後「跨實體寫拒」**仍綠**（拒絕由 W04 的 counter 代勞）→ 第 8 個負面 gate + `AD-BorrowedRefusal-1`；同一天補測試並在中性化狀態下證明它會紅。⭐ **US-6 裁決：W04 七個不變式 可複製 6 / 不適用 1 / 需調整 0** —— 形狀確認可複製，另交兩條新條款給 slice 3 | `docs/01-planning/W05-m1-asset-and-risk-chain/retrospective.md` · `docs/03-implementation/changes/CH-020-w05-asset-and-risk-chain.md` · `docs/14-adr/0013-risk-scoring-and-calibration.md` |
| — | 2026-08-13 | **CH-025**：`process` 回到評估引擎的 subject types。⭐ **不是規格衝突晚被發現，是一個基於別的證據做出的決定的副作用** —— 「`Assessment` 不建表」是看**欄位重疊**決定的，而兩邊的 enum 各有一個對方沒有的值，合併於是無聲刪掉了 `process`。使用者裁決補回（取聯集，`vendor` 不動）。⛔ **`02a:223` 不改** —— 反向修法（宣告 RCSA 不涵蓋流程）是靠刪掉能力來讓規格自洽。⭐ 新測試**單獨釘 `process`** 而非併入既有的 `Object.values` 迴圈：那個迴圈斷言「每個成員都被接受」，成員被移除時它照樣綠 —— `AD-TestNameWiderThanProof-1` 在它咬人之前就套用。關 `AD-AssessmentProcessSubject-1`，留下通則 `AD-SpecMergeFieldByField-1` | `docs/03-implementation/changes/CH-025-assessment-process-subject.md` |
| **W09** | 2026-08-13 | **M1 slice 6 — the shared assessment engine** — MERGED (PR #50, `6446099`)，`closed`。`AssessmentTemplate` / `Instance` / `Response` + 六個端點 → **17 / 35 實體**（分子 +3、**分母 −1**，因為 `Assessment` 停止是一個要建的列）。⭐ `05:39` 明文「build once rather than three times」，而 W07 的 `ControlTest` 已經開始了它警告的事。⭐⭐ **`template_version` 的三選一**：呼叫端傳（那是宣稱不是快照）· 開 `assessmentTemplate` delegate（為一個整數拆掉三個 phase 的 oracle 防線）· **DB 在 `BEFORE INSERT` 填**（選它）。而那個 trigger **差一點自己開一個 oracle** —— 不可達模板若 `RAISE` 會讓「別人的」與「不存在的」得到不同錯誤；`COALESCE(...,0)` 不 RAISE，讓複合 FK 用 23503 拒絕兩者。⚠️ 0 不是 NULL，因為 BEFORE trigger 也跑在 `NOT NULL` 之前。⭐ **`evidence` 首次回頭補複合錨點** —— 前三次父子表都同 phase 出生，「父表給得起但還沒有」從未發生。⭐ **本 schema 第一個跨欄位約束**（SoD CHECK），四個方向實測，且 **UPDATE 是免費的**。🚩 **元驗證 6/6 方向全中**，含兩個反直覺預測（N2 的 16 仍綠 = `AD-BorrowedRefusal-1` **首次事先預測**；N4 的 9c 錯誤地仍綠 = `AD-TestNameWiderThanProof-1` **首次由元驗證抓到**）。⛔ **第一次 N1 什麼都沒量到而看起來像結果**（改資料庫被 int setup 重建覆蓋）→ `AD-NeutraliseRebuiltState-1`。⛔ **使用者「不建 Assessment」的裁決有未預見的代價**：`process` subject type 失去落點 → `AD-AssessmentProcessSubject-1`。⛔ **gate-only verified**，無 UI 故無 drive-through | `docs/01-planning/W09-m1-assessment-engine/retrospective.md` · `docs/03-implementation/changes/CH-024-w09-assessment-engine.md` |
| **W10** | 2026-08-13 | **M1 slice 7 — a unique index that answered a question nobody should be able to ask** — MERGED (PR #52, `afa667a`)，`closed`。`RiskManagementReport` + `RMReportVersion` + 4 端點 → **19 / 35 實體**。⭐⭐ `02a` 同時給了兩種「哪一版現行」的說法（父表 `current_version_id` :253 / 子表 `state` :257）而它們**互斥** —— 翻 `state` 就是編輯一列 :260 說永不編輯的資料。只建父表指標，換到版本表**一條 `FOR UPDATE` policy 都沒有**，「至多一個現行版」**構造上為真**，superseded 導出。⛔⭐ **本片最重要的產出是量出來的漏洞**：`(report_id, version_label)` 兩值皆來自 request body，而唯一索引**不受 RLS 管且早於複合 FK** —— 撞別實體的標籤回 23505、不撞回 23503，可一次一猜列舉別人的版本史 → `AD-UniqueKeyOracle-1`（P0 候選）。⭐ promote 移進 DB trigger（`runScoped` 讓每 op 各成一交易，而 W04 已拒絕穿 `$transaction`），副作用是介面**更窄**。🚩 元驗證 **6/6**：N1a 證明缺席的 policy 自己撐得住（GRANT 放行後零筆被改）· N1b 兩層皆除則快照真被改寫 · **N4 零轉紅如預測** → `AD-BorrowedRefusal-1` 第 5 次。⛔ 補的測試第一版被 `RETURNING` 遮蔽 —— **已記錄的陷阱再次被踩，抓到它的是重跑中性化**。⛔ migration 註解的因果被一個附了預測的 int 測試推翻（GRANT 先於 policy），當天就地更正 | `docs/01-planning/W10-m1-rm-report-snapshot/retrospective.md` · `docs/03-implementation/changes/CH-026-w10-rm-report-snapshot.md` |
| — | 2026-08-13 | **分支生命週期清理** —— 遠端 **25 → 1**（只剩 `main`）、本機 **3 → 1**，每一個先用 `git cherry`（patch-id）驗過內容才刪，**不用 ancestry**（`AD-RebaseMergeBranchCheck-1`）。`deleteBranchOnMerge` `false → true`（`gh repo view` 複驗）。⭐ **兩個特例分開處置**：`chore/w03-status-flip` 的原文拆 9 個片段比對 main（7 個逐字、2 個實質保留）→ 直接刪；`docs/status-audit-20260810-3` **從未 push**，`31f76e2` 只存在一個本機 clone，而 `STATUS_AUDIT` §3 明訂歷史快照不留全文 → **先封存為 tag `archive/status-audit-20260810-3` 並 push，再刪**（本 repo 第一個 tag；刪除後複驗 279 行仍可取回）。⚠️ 新設定**不涵蓋**從未 push 的分支 → `AD-UnpushedWorkInvisible-1` | `docs/01-planning/STATUS_AUDIT.md` §3.1 · `git show archive/status-audit-20260810-3` |
| — | 2026-08-15 | **CH-032：把 runtime base 移離一個沒有來的重建** —— MERGED (PR #66, `52a74ac`)，`done`。`nodejs22-debian12` → `debian13`，`.trivyignore.yaml` 清空。⏰ **關掉本 repo 唯一有日期的死線**（`AD-TrivyExempt-1`，2026-09-07；trivy 是 required check，到期＝所有 PR 停止可 merge）。⭐ **照著豁免檔自己寫的程序跑**：讀四個 image 內的 `dpkg` 資料庫（非由 tag 推論）—— debian12 三個變體**全部**仍是 `libssl3 3.0.18-1~deb12u2`（W01 的逐字原值，七天零進展），debian13 是 `libssl3t64 3.5.6-1~deb13u2` ⇒「刪掉整個檔案」不成立，而「延期」需要對六條各寫**新**論證，其中五條仍只有「等上游」。⛔ **三個預測寫在執行之前，2 中 1 錯**：Prisma engine 在 OpenSSL 3.5.x 上起得來 ✅（無 `binaryTargets`）· trivy 接受空清單 ✅ · ⛔ **trivy 對缺失的 `--ignorefile` 會 FATAL** ❌ —— `security-scan.yml:325` 無條件傳它（同 step 的 `:303` 有 `[ -f ]` 保護），該行自 W01 起**從未在檔案缺席時跑過** ⇒ 13 秒兩次 FATAL、**掃描根本沒跑**，而 job 是紅的：**看起來像掃描器說了話，其實它沒開口**。裁決保留檔案並清空清單（`vulnerabilities: []`），`security-scan.yml` **一行未改** —— 明確的空是版控中的陳述，檔案不見了是沉默（ADR-0014「缺席即最嚴格」的反向）。⭐ 實據非綠燈：log 逐處讀到豁免 group 空、兩個 Dockerfile 各真的掃一次、`Vulnerabilities` **0**。⚪ gate-only verified | `docs/03-implementation/changes/CH-032-trivy-base-image-refresh.md` |
| — | 2026-08-13 | **撿回審計 #3（2026-08-10，W04 收尾後）** —— 一份從未進 main 的完整跨來源審計，隨 `AD-RebaseMergeBranchCheck-1` 在同一次分支清理中被找到。關 `AD-LostAudit20260810-1`。⭐ **它的 `AD-12b@#3` 其實已經生效** —— W05 照著它加了 `retrospective.md.tpl:120` 的 RISK_REGISTER 複查列，所以那次審計的內容進了 main，只有**報告本身**沒有。⛔ 代價是**編號衝突無法修好**：兩次審計都從 `AD-12` 編起且兩套都已被引用，只能靠審計序號消歧（2026-08-12 那次由 `#3` 改號為 `#4`）。⭐ 連帶更正 `AD-GrepAssertion-1` 在審計自身的再現次數 **2 → 3**（達強度階梯的結構性解法門檻）| `docs/01-planning/STATUS_AUDIT.md` §3.1 |
| — | 2026-08-14 | **CH-027：BACKLOG 的計數自己會叫** —— MERGED (PR #54, `5a676f9`)，`done`。`check_backlog_counts.py`（`run_all` **7 → 8**）比對開頭宣告的四個數字與 §Open **逐列解析**的真值，不符即 fail。關 `AD-CountBeforeLastEdit-1`。⭐ **落點是使用者改判的**：原排「W09 Day 0，不另開 CH」，W09 / W10 各漏一次 —— 先例 `check_entity_index.py` 成功是因為它**進了 W08 的 checklist**，而本條只在 ROADMAP 有一列，**ROADMAP 的列不是 checklist 的列**（`AD-5` 再現）。⛔⭐ **枚舉推翻了兩個天真實作**：優先度儲存格有 **3 種**寫法（嚴格 pattern 只中 88/91，**差額本身就是異格式清單** `:130`/`:159`/`:174`），且 `:145` 儲存格內有**裸 pipe** 使固定欄位索引錯位 → 改為「整格匹配且恰好一個，否則 FAIL 不 skip」。🚩 中性化 **6 個方向 5 中**：⛔ **N4b 的預測是錯的**（`!= 1` → `< 1` 只打開兩半中的一半）→ 補 N4c 證另一半；⭐ **N4a 零轉紅的歸因差點寫錯** —— 「現為」擋的不是錯誤計數（那由「恰好一次」擋）而是**誤報**，補測試後它成為唯一守衛。⚠️ **範圍界線**：證明宣告與表格一致，**不證明**表格與現實一致 | `docs/03-implementation/changes/CH-027-backlog-count-derivation/` |
| — | 2026-08-16 | **CH-035：記錄保存與法務扣留 —— 一張全域表 + 一張 entity-scoped 表** —— MERGED (PR #73, `cf7cf07`)。30/36 → **32/36**（分母不變）。⭐ **本片最重要的產出是一個「不做」**：W14 的 `assert_polymorphic_parent_in_scope()` 對 `legal_holds.scope_ref` **結構上不可用**（`::uuid` cast 早於 mapping walk，而 `class` 不是 uuid；`record` 泛指 31 張表）⇒ 只涵蓋 `entity` 分支的守衛會是 `AD-VacuousScopeTest-1` 的形狀，故明說不建並登記。**`retention_policies` 是第三張擴充豁免清單的全域表**（舉證三處，`multi-tenant-data.md` 行數不變）。⭐ **N4 零轉紅揭露真缺口**：刪 INSERT policy 全綠 ⇒ 補測試 13 | `docs/03-implementation/changes/CH-035-w17-retention-and-legal-hold.md` |
| — | 2026-08-16 | **CH-034：APAC ISMS profile 五張 entity-scoped 表** —— MERGED (PR #71, `0086ba5`)。25/35 → **30/36**（分母 +1，第五個實體 `ISMSProfileVersion` 依 `02a:18` 同 change 上索引）。**關閉 `AD-DesignAlign-7`**。零端點；三處刻意缺席（`posture` / `status` / 版本 `state`）寫進 migration banner、schema docstring 與 `13` §Implementation record | `docs/03-implementation/changes/CH-034-w16-isms-profile.md` |
| **W11** | 2026-08-14 | **M1 slice 8 — the layer that was actually doing the work** — MERGED (PR #56, `dcc680f`)，`closed`。`StatementOfApplicability`（ISO 27001 的強制產出）+ 2 個端點，**20 / 35 實體**。⭐ `02a:215` 開頭是 `framework_id`，而 Day 0 量到比預期更強的事實：**`Framework` 在整份規格全檔零命中** —— 不只不在 §0 索引上，規格從未說過它是什麼 ⇒ 建 FK 等於**先發明一個實體**。沿用 W06 對 `Control.framework_refs` 的裁決，且**欄位名不叫 `framework_id`**（名字不該宣稱不存在的邊）。⭐⭐ **`AD-UniqueKeyOracle-1` 第 2 個資料點，判準確認可移轉但描述被修正**：拿掉 `org_entity_id` 後撞別實體的 clause → **23505**、撞沒人的 → **成功**；沒有 parent FK 可掉下去，所以對比是「錯誤 vs 成功」而非 W10 的兩個 SQLSTATE ⇒ detector 的判準要寫「兩個**可分辨的結果**」。🚩 **本片最重要的產出是一次自我推翻**：N4 中性化 `_update` 的 `WITH CHECK` 零轉紅 → 補跨實體 UPDATE 測試 → **重跑仍零轉紅** → 逐條放行隔離量到**擋住它的是 SELECT policy**（PostgreSQL 拿 UPDATE 的新列對它檢查）⇒ 我 Day 1 寫在 migration 裡的因果是錯的，已更正 → `AD-PolicyClaimUnmeasured-1`（並要求複驗 `controls` 的同一句）。`AD-BorrowedRefusal-1` **第 6 次**，且**首次出現判準不可滿足的情況**（兩條 policy 運算式相同，測試分不開）。⭐ 反面：**N2 是本專案第一次讓 INSERT `WITH CHECK` 真的轉紅** —— 測試 7 照該教訓**預先**寫。⭐ `02a` 的 deviation 選 **inline 不選 blockquote**，實測 514 → 514 行，零行號位移（`AD-MdAnchorLineShift-1`）。⛔ **M1 的 DoD 仍未達成** —— 其餘 15 張表是 slice 9..N | `docs/01-planning/W11-m1-statement-of-applicability/retrospective.md` · `docs/03-implementation/changes/CH-028-w11-soa.md` |
| **W13** | 2026-08-15 | **M3 slice 2 — 覆蓋，以及一個被實驗擋下的 Potemkin** — MERGED (PR #61, `91bd789`)，`closed`。`AUDITED_MODELS` **1 → 15**，每個模型一條**移除該名字就轉紅**的測試 + 一條**漂移守衛**（從 `core-model` 原始碼導出寫入面，雙向比對）⇒ R4 十個 phase 的失效模式自此有機械守門。⭐⭐ **plan §4 的做法在寫第一條測試之前被實驗推翻**：11 個模組 spec 用 module-local 圖，同一個 create 在那裡 `before=9 after=9`、在 AppModule 圖下寫一列 ⇒ 原做法只能永遠紅或寫成 `>= 0`（AP-3）。⭐ **N2 是驗收核心且完全命中**：只移除 `Issue` → **恰好 2 紅**（該模型 + 漂移守衛）、其餘 14 條未動。⛔ **`RefCodeCounter` 不接的理由由 2 條推理變成 3 條實測**，第 3 條是 N3 撞出來的：`issueRefCode` 在自己的交易裡先跑，**失敗的寫入會留下稽核列** —— 稽核軌跡會記錄一件沒發生的事。⛔ **覆蓋率要連著限定讀**：全 codebase 零個 `client.*.update`/`.delete`，15 個寫入全是 create ⇒ 「15 / 21」是「每個有寫入路徑的模型其 create 被稽核」而非「所有狀態變更類型」。關 `AD-AuditCoverageOneTable-1`（🔴 P0）+ `AD-VacuousScopeTest-1`（通則，13 個 spec 掃完補 4 處）。⛔ **gate-only verified**，無 UI | `docs/01-planning/W13-m3-audit-coverage/retrospective.md` · `docs/03-implementation/changes/CH-030-w13-audit-coverage.md` |
| **W14** | 2026-08-15 | **M1 slice 9 — 一條只寫它就什麼都不證明的測試** — MERGED (PR #63, `e9ab83a`)，`closed_partial`。`02a:235` 的 `Attestation` + 2 個端點 → **22 / 35 實體**；`evidence` 的多型守衛得到第二個分支；`AUDITED_MODELS` 15 → **16**。⭐ **W13 漂移守衛的第一次實戰**：在建表 + repository 但**刻意不碰** `AUDITED_MODELS` 的狀態下跑 int suite ⇒ **恰好 1 紅**、訊息自己指名 `Attestation`（落在 `unaudited` 側）、15 條逐模型覆蓋測試**一條未動** ⇒ 偵測**獨立於**它們。⛔ 順序是這條檢查唯一的價值來源 —— 先改再跑得到的是綠色，而綠色**不能區分**「守衛有效」與「守衛從不看新表」。⭐⭐ **本片最有價值的產出是 Day 0 的 D5**：`controls_read` 含 `applies_to_scope = 'group' OR …`，group-shared control 對**任何**實體合法可讀（`02a:434` / ADR-0014）⇒ 若中性化拿 group control 當標的，測試**修法前後都會通過**（`AD-VacuousScopeTest-1` 的形狀，W13 才剛補過 4 處）。實測把兩側釘死：拆掉守衛後測試 8（entity-local control）**紅**、⭐ 測試 7（group control）**仍綠** ⇒ **只寫 7 得到恆真測試，只寫 8 證不出那條放寬是刻意的**。⭐ 而 D5 是**因為 checklist 明確列了 `D-subject-both-built` 才被問出來的** —— 那一項正是為 `AD-Day0ReadNotApplied-1` 加的 ⇒ **AD → checklist 這條迴路第一次指得出具體戰果**。⭐ `assert_parent_in_scope()` **結構上擴充不了**（讀 `TG_ARGV`，那是建 trigger 時的字面值，而分支必須逐列發生）⇒ 新建 variadic 的 `assert_polymorphic_parent_in_scope()`，舊函式**一行未動**（三個 phase 依賴它）；⛔ **非 AP-5** —— 當下兩個呼叫端且映射不重疊。⛔ **`status` 不建**：`02a` §4 與 `:417` 兩份 lifecycle 清單**都沒有 Attestation** ⇒ 無值域來源（W07 移除 `ControlTest.result` 的鏡像）。**RLS 只 2 條**、連 `GRANT UPDATE` 都無（`rm_report_versions` 先例）。⛔ **N3b 預測錯，而錯得比預測有用**：Prisma 的 **generated client 在 runtime 也驗證 enum** ⇒ 不是兩份真相是**三份**（schema / client / DB catalog）—— `ALTER TYPE ADD VALUE` 必配 `prisma generate`，否則分歧**只在部署時現形** → `AD-PrismaEnumThreeTruths-1`。⛔ **AP-7 違規 1 次（我造成的）**：Day 2 改清單沒改 docstring，而那段 docstring 的第一句正是「rerun the derivation」。🚧 US-3 移出 → M6。⛔ **gate-only verified**，無 UI | `docs/01-planning/W14-m1-attestation/retrospective.md` · `docs/03-implementation/changes/CH-031-w14-attestation.md` |
| **W15** | 2026-08-16 | **M1 slice 10 — 一個每次都一樣所以沒人問的東西** — MERGED (PR #67, `d01d505`)，`closed_partial`。`Jurisdiction` / `Regulation` / `Obligation` 三張**全域**參考表 + `residency_policy` enum + `OrgEntity.jurisdiction_id` → **25 / 35 實體**。⛔ **零端點、零 repository、零 controller** —— 交付的全部價值是一個**約束面**（4 FK + 1 UNIQUE + 3 GRANT + 1 enum + 欄位形狀），消費者在 M6。⭐⭐ **最有價值的產出是 Day 0 的 D7，而它的價值在於它不在任何一個決策點裡**：plan 有 D1–D5 五個明確標示的決策點，**GRANT 不在其中** —— 前九片的表全是 entity-scoped，GRANT 每次都一樣。D7 量到先例是 `SELECT` only ⇒ (a) D3「不進 `AUDITED_MODELS`」的論證從「今天沒有寫者」升級為「**寫者被資料庫擋著**」；(b) ⭐ **AC-5 的 FK 測試不能走應用層** —— app 角色無 INSERT 權限，PG 在**評估任何約束之前**以 42501 拒絕 ⇒ 照 plan 原樣寫，兩條 FK 測試會**全綠而什麼都沒測到**（`AD-VacuousScopeTest-1` 最難察覺的那一種：綠燈、有斷言、斷言還通過）。⇒ **一個每次都一樣所以不必問的東西，在第一次不一樣的時候不會舉手。** ⭐ **恆真檢查在被寫進 checklist 的當下就被抓到**：1.1 原文「觀察漂移守衛**仍綠**」是恆真的 ⇒ 拆 1.1a/1.1b，後者用暫時 stub 逼出 1 紅並指名 `Jurisdiction`；附帶量到 **stub 無人 import 卻仍被偵測** ⇒ 守衛讀**文字**不讀 build graph。⭐⭐ **N1 的直覺「2 紅」在寫下之前被 grep 推翻**（`rls-direct` 排第 17、先跑完，看不到多出來的第 6 列）⇒ 正解 1 紅：**「多一個紅看起來像是中性化更成功，沒有人會回頭質疑一個比預期更紅的結果。」** ⛔ **calibration 1.235 OVER，而 plan §7 預測的方向是 UNDER** —— 零端點讓**實作**便宜（64.4 min）卻讓**驗證變貴**（65.9 min）⇒ 假設的 `schema-only` class 乘數該**比 0.50 高**；且 progress **零時間記錄** + plan **未宣告量法** ⇒ **1.235 是下限不是測量值**（`AD-CalibrationNoTimeRecord-1`）。⭐ **R4 的敞口沒有變大，W02 以來第一次** —— `GRANT SELECT` only 讓「無寫入路徑可稽核」是**構造保證**而非「還沒接」；導出 **16 / 25**。⭐⭐ **`AD-26` 在本次 closeout 進行到一半時真的發生**：文件寫完時 PR 是 OPEN、五處都寫了「未 merge」，**幾分鐘後 PR 被 merge，五處同時變錯** ⇒ 那不是「有人會忘記回頭改」，是**這份文件按構造會在寫完之後失效**。連帶第 7 次 rebase SHA 改寫（11 個全改）與**第 7 次 author date 逐秒不變** —— 本片 calibration 完全由 author date 導出，**數字撐過了 rebase，即使 SHA 沒有**。⚠️ **`closed_partial` 的理由只有一個**：**AC-2 從未被驗證**（「欄位逐個對上 `02a`」沒被執行也沒被寫下來，今天加減一個欄位**全部 gate 都不會紅**）。⛔ **gate-only verified**，無 UI | `docs/01-planning/W15-m1-jurisdiction-and-obligations/retrospective.md` · `docs/03-implementation/changes/CH-033-w15-jurisdiction-and-obligations.md` |
| **W12** | 2026-08-14 | **M3 spike — 稽核軌跡，以及「拒絕」與「說出來」的差別** — MERGED (PR #58, `ea58fdb`)，`closed`。`audit_log`（append-only、per-entity hash chain、`BEFORE INSERT` trigger 在 DB 內算）+ `contracts/audit-hook.ts` 反轉的攔截點 + `verifyChain` → **21 / 35 實體**，ADR-0003 由實測拍板、OQ-4 關閉。⭐⭐ **決定不是靠成本**：序列下 A 與 B **量不出差別**（兩次順序翻轉、差距落在 control drift 內），併發下 A 是 B 的 **1.63 / 1.59** 倍**而仍然選 A** —— 分勝負的是**斷點定位**（A 指到那一列、B 只指到那一段，**寫成測試不是形容詞**）與**誰算 hash**。⛔ **「驗證成本」這個 plan 列為兩大維度之一的東西量到沒有訊號**（B/A 0.84–1.20，10k 時共用的 fetch 714-753ms 壓過 walk 249-279ms）—— 那反而移除了 B 被預期的優勢。⭐ **追加了 plan 沒要求的併發量測**，因為 A 的成本是 per-entity 鎖，而**單執行緒 benchmark 結構上量不到它**。⭐⭐⭐ **N3 答出了 `AD-BorrowedRefusal-1` 問了 7 次的問題**：⛔ 沒把 `Received: "NO ERROR"` 當答案 —— 直接數列（可見 7 列 / `UPDATE 0` / 0 列被改）⇒ **兩層都擋，只有一層會說話**（GRANT 給 42501，缺席的 policy 給安靜的零列）。⚠️ 承重推論：`GRANT ALL` 之後 append-only 仍成立但**變啞**。🚩 **N2 揭出四個範疇測試裡的第 1 個在稽核全關時仍全綠**（空陣列上 `every` 真）→ 補非空前提並**重跑** 7 紅 → 10 紅 → `AD-VacuousScopeTest-1`。🚩🚩 **coverage 紅燈揭出「策略 B 的正確性從未被斷言過」**，而我正要拿它當成本基準線 —— bench 斷言的是時間，時間不在乎寫入者是壞的。⛔ **D11**：`AFTER INSERT` 寫不出來（改不了 `NEW` ⇒ 要 `UPDATE` ⇒ 撞 append-only 自己），落點改 `BEFORE`，plan / checklist 原文保留。⛔ **D14**：`TIMESTAMPTZ(6)` 會讓 verify 變成**永遠在響的警報**且無 gate 抓得到 → 全 schema 唯一的 (3)。⛔ **gate-only verified**，無 UI。⛔⭐ closeout 導出 R4 分母時**推翻了 R4 自己的手寫計數**（18 → **21**）→ `AD-RiskTableCountManual-1` 首次實地擊中 | `docs/01-planning/W12-m3-audit-trail-spike/retrospective.md` · `docs/03-implementation/changes/CH-029-w12-audit-trail.md` · `docs/14-adr/0003-audit-trail-hash-chain.md` · `docs/02-architecture/design-notes/W12-audit-trail.md` |
| **W03** | 2026-08-10 | **Governed extension spike** — MERGED (PR #31, `b20f3f1`)，`closed`。ADR-0005 拍板（JSONB + entity-scoped catalog，**應用層 + DB trigger 雙層**）+ 平台第一個業務端點。⭐ **元驗證量到兩層是獨立的**：validator 中性化後，三個「database refuses」測試**仍然通過**。關 `AD-CacheControl-1` / `AD-ScopedClientDI-1` / `AD-ScopeConcurrency-1` + OQ-6 → **M1 前置清空**。CI 首航抓到「產物從來沒有以部署時的組態被執行過」→ 第 6 個負面 gate | `docs/01-planning/W03-governed-extension-spike/retrospective.md` · `docs/03-implementation/changes/CH-018-w03-governed-extensions.md` · `docs/02-architecture/design-notes/W03-governed-extensions.md` |

---

## §Pending Decisions（需要人來拍板的）

<!-- 不是「要做什麼」而是「要選哪個」的事項。 -->

| 決策 | 選項 | 卡在哪 | 誰決定 |
|------|------|-------|--------|
| ~~**中國跨境資料層級**~~ | — | ✅ **已消滅** —— 中國於 2026-08-08 移出範圍（`CH-008`），沒有中國邊界，`03` §Questions for Legal 的四個問題作廢。`AD-Decider-1` 一併關閉 | — |
| ~~**計算平台**~~ | — | ✅ **已拍板 2026-08-08 — Azure Container Apps**（[ADR-0011](../14-adr/0011-compute-platform.md)）。決定性證據是 `unified-operation-platform` 已在 ACA 上跑同一個 stack；「App Service 預設值已知」的論據在起草時**被自己推翻**（ACA 的 `*.azurecontainerapps.io` 同樣是平台預設）| — |
| **其餘開放決策 —— 清單與計數的唯一權威是 [`../decision-form.md`](../decision-form.md)** | 同左 | 各範疇 | ⛔ **本格刻意不寫數字也不列 OQ 編號**（審計 #5 `AD-17` / #6 `AD-22` 的處置，2026-08-15）：原文寫「其餘 **3** 項（OQ-4/7/8）」，而 OQ-4 已於 2026-08-14 由 W12 拍板 —— **這一格在複製 `decision-form.md` 的內容，違反 `STATUS_AUDIT.md` §5**，而複製的副本必然比來源晚一步。⭐ 修法是**改結構不是改數字**（審計 #2 的 `AD-8` 已證明後者無效）。⚠️ 共通性質仍值得留在這裡，因為它不會隨個別 OQ 變動：**卡的是證據不是決策者** |

---

## §Known Issues / Accepted Debt

<!-- 已知但**有意識決定不修**的東西。寫下來是為了不要重複發現它。 -->

| Issue | 為何不修 | 什麼條件下要重新考慮 |
|-------|---------|-------------------|
| `15` §7 的 action 清單會 stale —— #4 在 `05` 早已完成卻仍標 High，CH-003 才發現 | 追蹤清單本來就會漂移；重點是**定期跨來源比對**而非要求它永遠準確 | 每 5–10 個 phase 跑一次 `/status-audit`。若同類 stale 再出現 2 次以上，代表 closeout 沒有回填 `15`，那要改流程不是改清單 |
| 資料模型分散在 `02a` 與 `11`/`12`/`13`/`17` 五份文件 | **有意識選擇**（CH-003）：共用實體集中以符合 guardrail 3，模組實體貼近來源表單。全收進 `02a` 會讓它膨脹到 25KB+ 且模組文件殘缺 | ✅ **條件成立且已交付（W08）** —— 不同步發生了 2 次（`RefCodeCounter` 缺席三個 phase；分子在四份文件裡是四個數）。`scripts/lint/check_entity_index.py` 現在由 `schema.prisma` ∩ `02a` §0 導出 `built N / total M`，`run_all` 7/7。⚠️ **它一跑就推翻了分母**：36 不是 35。本列保留是因為**分散本身仍是有意識的選擇**，只是現在有東西在守它 |

> **這一節很重要。** 沒有它的話，同一個問題會被反覆「發現」，
> 每次都花時間分析一遍才想起來「喔對這個我們決定不修」。
