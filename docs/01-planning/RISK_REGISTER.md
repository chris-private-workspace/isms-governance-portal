# Risk Register

**Purpose**: Living 風險登記。**不是寫一次就算的文件** —— 每次 phase 收尾要複查。
**Created**: 2026-08-07
**Last Reviewed**: 2026-08-16（W17 closeout）—— **只複查並改寫 R3 / R4 兩列**：
**R3** 全域表類別長大而「屏障有沒有被裝上」無機械檢查（`AD-EntityScopeNoDriftGuard-1`）；
**R4** ⭐⭐ **敞口沒有變大，W02 以來第一次** —— 三張新表 `GRANT SELECT` only，
「無寫入路徑可稽核」是**構造保證**不是「還沒接」；導出值 **16 / 25**（分母 +3 分子不變是正確的）。
⛔ **R1 / R2 / R5 / R6 / R7 / R8 仍停在 2026-08-10，跨 W05–W15 十一個 phase** —— `AD-RegisterUpkeep-1`。
前次：2026-08-15 —— ⚠️ **兩次，由兩個 phase 各做一半，而它們同一天收尾**（審計 #6 `AD-25`：原文把整天歸給 W13，於是 W14 的那一半在本檔上不存在；**日期是對的、歸屬是錯的，所以任何日期檢查都發現不了**）：**(a) W13 closeout** —— R4 覆蓋 1 / 21 → 15 / 21，分子分母皆機械導出，新增漂移守衛讓「下一張表忘了接」會轉紅；**其餘七條逐條看過無變化**。**(b) W14 closeout** —— 只複查並改寫 **R3 / R4 兩列**（group-shared control 的逃生口第一次傳播到第二張表 · 漂移守衛的第一次實戰）。⛔ **因此 R1 / R2 / R5 / R6 / R7 / R8 的「最後複查」仍停在 2026-08-10，跨 W05–W14 十個 phase** —— 見 `AD-RegisterUpkeep-1`，本檔不假裝那六條被看過
**Status**: Active

> **和 BACKLOG 的分工（這條最容易搞混）**：
> BACKLOG 裝**待辦** —— 「要做的事」。本表裝**風險** —— 「可能發生的壞事」。
> 「pooler 下 `set_config` 未驗」是待辦（`AD-PoolerScope-1`）；
> 「隔離保證依賴一個未經 production 負載驗證的機制」是風險（R3）。
> **本表不複製 AD 細節**，只 link。

---

## 活躍風險

| # | 風險 | 可能性 | 影響 | 緩解 | 狀態 | 最後複查 |
|---|---|---|---|---|---|---|
| R1 | **自建路徑本身** —— build-vs-buy 分析曾建議不要自建；stakeholder 在知情下確認。其風險自此是設計約束而非未知數 | 中 | 高 | Foundation-first 排序；兩個最小模組端到端證明骨幹，而不是先鋪功能面 | 已接受（知情決定，`00` D10 / 已確認參數 #14）| 2026-08-10 |
| R2 | **單人開發，沒有第二雙眼睛** —— `required_approving_review_count: 0`，沒有 reviewer 存在 | 高 | 中 | ①PR 開著睡一晚，隔天用 reviewer 心態重讀；②六個 required check（`CH-015` 起真的會擋）；③每個宣稱會擋東西的機制附常駐負面案例 | 緩解中 | 2026-08-10 |
| R3 | ⭐ **實體隔離只剩一層屏障** —— ADR-0010 拿掉物理隔離後，RLS 是**唯一**的隔離機制（`07:33`）。隔離失敗不是一般 bug，是合規事故 | 低 | **極高** | W02 交付：RLS + `FORCE`、`WITH CHECK`、不 GRANT DELETE、`app_entity_scope()` fail-closed、20 個整合測試（8 個完全不經應用層）、旁路 detector。**已用「弄壞它看它紅」驗過**（policy → `USING(true)` 導致 14/20 紅）。**W03 補上並行汙染的常駐測試**（40 次交錯查詢逐列斷言）| 緩解中 —— 剩餘缺口**只剩** `AD-PoolerScope-1`（pooler 下 tx-local `set_config` 未驗）。~~`AD-ScopeConcurrency-1`~~ 已於 W03 關閉。⚠️ **W14 複查：敞口形狀有一項需要寫下來** —— ADR-0014 的 `applies_to_scope = 'group'` 逃生口**第一次傳播到第二張表**：`attestations.subject_id` 的守衛靠**呼叫端自己的** RLS 解析父列，所以 group-shared control **可以被任何實體 attest**。⛔ **這是設計（`02a:434`）不是缺口**，但它意味著「跨實體被拒」對本表**只有 `subject_type = policy` 時是全稱的** —— int 測試 7 / 8 把兩側都釘住了。⇒ 後續每一個引用 `controls` 的多型欄位都要重問一次這個問題。⭐ **W15 複查：本片沒有擴大敞口，但揭露了一個守衛的缺席** —— 三張新表**刻意全域**（無 `org_entity_id`、無 RLS），複製 `threats`/`vulnerabilities` 的形狀且**舉證程序走完**（`multi-tenant-data.md:81`：該檔 + migration banner + PR #67 描述三處）。⛔ **但「全域是刻意的」與「忘了加 RLS」在事後長得一模一樣，而只有前者有人寫測試** —— W15 的測試 3 查 `pg_class`/`pg_policies` 斷言 rls=false/policies=0，那是**硬編碼三個表名**的一次性斷言，不是目錄層守衛。實測：`pg_class`/`pg_tables`/`information_schema` 在 `apps/api/src`（排除 generated）**只命中 W15 這一個新檔**；`multi-tenant-data.md:363-369` 建議的六個 detector **零實作**。⇒ **稽核維度加表忘接會自動紅並指名，實體範疇維度什麼都不會發生**（`AD-EntityScopeNoDriftGuard-1` 🟡 P1）。本條的緩解因此有一個結構性上限：RLS 是唯一屏障，而「屏障有沒有被裝上」沒有機械檢查 | 2026-08-16 ⭐⭐ **W16（2026-08-16）：這條風險在本片差一點具體化，而攔下它的不是任何 gate。** plan 原文只寫 `ENABLE ROW LEVEL SECURITY`，**漏了 `FORCE`** —— 沒有 FORCE，表的 owner（= migration 執行的角色）**繞過全部 policy**，而 int suite 連的是 app 角色，所以五張新表的範疇測試會**全綠**。抓到它的是 Day-0 對 `attestations` migration 的逐字閱讀（DR3），不是測試、不是 lint。⇒ 本條的「只剩一層屏障」現在要加一句更精確的：**那一層屏障有一個開關，而沒有任何東西在看它有沒有被打開**。本片已在 int spec 測試 3 斷言 `relforcerowsecurity`，但**只釘住這五張表** —— `AD-EntityScopeNoDriftGuard-1` 要的是對「未來所有表」成立的守衛，那個仍不存在。 ⭐ **W17（2026-08-16）複查：本片首次把 `FORCE` 寫進 plan 而不是靠 Day 0 抓，並用中性化量了它的可觀察性 —— 結果比 W16 的敘述更尖銳。** N1 拿掉 `FORCE`（保留 `ENABLE`）後**恰好一條測試轉紅**（本片測試 6，斷言 `relforcerowsecurity`），其餘 **246 條一條未動**。⇒ 「那一層屏障有一個開關」現在有了數字：**開關被關掉時，這個 repo 裡唯一會反應的東西是一條刻意為它寫的斷言**，因為所有範疇測試連的都是 app 角色而 `FORCE` 只治理 owner。⛔ 而那條斷言仍然是**逐表硬編碼**的（本片兩張、W16 五張），`AD-EntityScopeNoDriftGuard-1` 要的目錄層守衛**依舊不存在** —— 現在有 **26 張**開了 RLS 的表（實測 ENABLE 24 → 加本片 `legal_holds`… 導出方式見下），而其中有幾張的 `FORCE` 有測試在看，是靠人記得。**可重跑的導出**：`Select-String -Path apps/api/prisma/migrations/*/migration.sql -Pattern 'ALTER TABLE\s+"([a-z_]+)"\s+(ENABLE|FORCE)\s+ROW LEVEL SECURITY'` 兩側取集合差 —— 2026-08-16 實測 **ENABLE 25 / FORCE 25 / 缺口 0**。⚠️ 那個「缺口 0」是**今天**的事實，不是機械保證。 ⭐ **W18（2026-08-17）複查：`FORCE` 的可觀察性取得第二個資料點，數字與 W17 逐位相同。** N2 拿掉 `posture_snapshots` 的 `FORCE`（保留 `ENABLE`）⇒ **恰好 1 條測試轉紅**（本片測試 8），其餘 **264 條一條未動** —— 與 W17 的 N1（1 紅 / 246 未動）**同形**。⇒ 「開關被關掉時，這個 repo 裡唯一會反應的東西是一條刻意為它寫的斷言」現在是**兩片獨立實測**而非一次觀察。⛔ 目錄層守衛（`AD-EntityScopeNoDriftGuard-1`）**依舊不存在**，兩片各自硬編碼自己的表。**可重跑的導出（本片改用 Grep 重做，且過程本身是一個教訓）**：`ENABLE ROW LEVEL SECURITY` 與 `FORCE\s+ROW LEVEL SECURITY` 在 `apps/api/prisma/migrations` 的出現次數 —— 2026-08-17 實測 **ENABLE 27 / FORCE 27 / 缺口 0**（W17 的 25 + 本片 2）。⚠️ **我第一次用的 pattern 帶了 `ALTER TABLE "[a-z_]+" ` 前綴，得到 18 而不是 27** —— 差額 9 全在 `isms_profile` 那一個檔（5 張表寫在連續行上而前綴形式不同）。若沒有拿 W17 的 25 當對照，那個 18 會被寫成「缺口 0」並且**看起來完全正確**（兩邊同樣窄、同樣漏、同樣相等）。這是 `AD-NarrowPatternWideClaim-1` 在本 phase 的第二次。 |
| R4 | ⭐ **稽核軌跡尚不存在，而資料已經在寫** —— guardrail 5 要求所有狀態變更進 append-only 防篡改日誌；**沒有任何稽核軌跡**。滾升是合法的跨實體讀取，它**必須被稽核**才不會變成後門。⚠️ **敞口逐 phase 擴大**：W02 兩張表 → W04 +2（`users` / `ref_code_counters`）→ W05 +3（`asset_groups` / `assets` / `risks`）→ W06 +1（`controls`）→ W07 +2（`control_tests` / `evidence`）→ **W08 +2**（`issues` / `actions`）→ **W09 +3**（assessment 三表）→ **W10 +2**（`rm_reports` / `rm_report_versions`），今日**十七張表、無一有稽核**。⛔ **W06 的擴大不只是 +1 張表**：`asset_groups` / `assets` 在 W05 沒有寫入路徑，它們的無稽核寫入當時只是理論上的；W06 給了它們端點，**三條無稽核寫入路徑同時變成真的可達** | **確定（現況）** | 高 | 攔截點已就位（ADR-0004 的 client extension 就是 M3 的落點）。⚠️ **緩解措施本身沒有進展** —— 五個 phase 以來擴大的是敞口不是防護；`created_by`/`updated_by` 欄位存在但**永遠是 NULL**（刻意：填佔位使用者會讓 M3 的問題用謊話被回答）。⭐ **W06 新增一個 M3 必須答的問題**：group-shared control 的**跨實體讀取**是合法的滾升路徑，依 guardrail 4「滾升路徑自身也要被稽核」，它需要的不只是寫入稽核。⭐⭐ **W07 讓 R4 從「敞口」變成「反諷」**：`evidence` 這張表**存在的目的就是證據等級的主張**（`hash` 是完整性錨點），而**寫入它的動作本身沒有任何稽核軌跡** —— 一筆沒有人知道是誰、何時、憑什麼建立的證據記錄。⚠️ `hash` 目前**也沒有任何東西驗證它**（無演算法約定、無重算比對）→ M3 必須同時解這兩半。⭐⭐ **W08 讓它從「反諷」變成「合規閉環缺一角」**：`issues` / `actions` 是 CAPA —— ISO 27001 要求矯正措施**可追溯到誰在何時決定了什麼**，而這兩張表的 `status` 欄位可以被任何值覆寫任何值、**沒有任何東西記錄那次覆寫**。⚠️ 更具體的：`Issue.status = risk_accepted` 是一次**正式的風險接受**（`02a:379` "formal acceptance"），今天它是一個沒有簽名、沒有時間戳、沒有理由欄位的 enum 值。⭐⭐ **W09 再多一角，而這一角特別難看**：`assessment_instances` 帶著本專案**第一個資料庫強制的職責分離**（`assessment_instances_sod`），但 `assignee_user_id` / `reviewer_user_id` **誰在何時把誰換掉了，沒有任何記錄** —— 一個可以被無聲重新指派的職責分離，稽核時證明不了它在事發當下成立。⚠️ 三張表 → **十七張，無一有稽核**。⭐⭐ **W10 讓它從「缺一角」變成「這張表的存在理由被自己否定」**：`rm_report_versions` 是**不可變的受控交付物** —— 平台花了整個 phase 保證「已核准的版本不可被改寫」，資料庫層兩道獨立防線各自量過。但**誰在何時發布了哪一版、由哪個委員會核准**這件事本身**沒有任何稽核軌跡**：`prepared_by`/`approved_by` 是呼叫端自己填的字串，`created_by` 依舊永遠是 NULL。⛔ 也就是說：內容不可篡改，**而出處不可驗證**。稽核第一個會問的正是後者。⭐⭐ **W11 把它推到最尷尬的位置：SoA 是稽核員第一份會讀的 ISO 27001 文件，而它是第十八張沒有稽核軌跡的表**。`statements_of_applicability` 記的正是「哪些控制適用、哪些排除、理由、**誰核准**」—— 而 `approved_by` 是呼叫端自己填的自由文字（`02a:215` 沒說它是 user id，且 SoA 核准人常是委員會），`created_by` 依舊永遠 NULL。⇒ 平台儲存的是**該實體宣稱**誰核准了它，而對這個宣稱**本身無法提供任何證據**。⛔ 更精確地說：一列 `applicable = false` 是一次**正式的控制排除**，它可以被任何人在任何時候寫成任何理由，而**沒有任何東西記錄那次寫入** —— 這與 W08 的 `Issue.status = risk_accepted` 是同一個形狀，只是這次落在認證審查的必備文件上。⚠️ 十七張 → **十八張，無一有稽核**。⭐⭐ **W12 交付了第一個機制，並且順帶推翻了上面那串手寫計數**：`audit_log`（append-only、per-entity hash chain、`BEFORE INSERT` trigger 計算、與領域寫入同交易）+ 應用層攔截點 + `verifyChain`（回報第一個斷點的位置而非布林值），ADR-0003 由實測拍板。⛔ **但覆蓋是 1 / 21，不是「已解決」** —— `AUDITED_MODELS` 只有 `StatementOfApplicability` 一個名字，接上其餘 20 張是改那一行。⛔⭐ **上面「十八張」是錯的，真值 21**（`AD-RiskTableCountManual-1` 第一次被實地擊中）：逐個 migration 的 `CREATE TABLE` 加總是 2·**1**·2·**5**·1·2·2·3·2·1 = **21**，而本列的鏈**完全跳過 W03 的 `extension_fields`**、且 **W05 記 +3 而實際建了 5 張**（`threats` / `vulnerabilities` 兩個全域庫被漏掉）。**原文保留不刪**，因為錯的是計數方式不是某一次筆誤 —— 修法是讓本列引用 `schema.prisma` 的 `^model` 數（`check_entity_index.py` 已經在數它），不是再修一次數字。⭐⭐ **W13 把覆蓋接上，而數字這次是導出的不是寫下的**：`AUDITED_MODELS` 1 → **15**，每個模型各有一條**移除該名字就會轉紅**的測試（N2 實測：移除 `Issue` → 恰好 2 紅、其餘 14 條未動），外加一條**漂移守衛** —— 它從 `core-model` 原始碼導出寫入面並與清單雙向比對，所以「下一張表忘了接」會讓測試紅而不是安靜地擴大敞口。⇒ **R4 十個 phase 的失效模式（敞口逐 phase 擴大而沒有 gate 會叫）自此有機械守門**。導出方法（可重跑）：`AUDITED_MODELS` 字串數 = 15；`grep -c '^model' schema.prisma` = 22；減去 `AuditLog`（稽核表自身）= **21 張業務表**。⛔ **仍未涵蓋的三項，逐項寫出來而不是四捨五入成「已解決」**：（a）**5 個模型今天沒有 repository 寫入路徑**（`OrgEntity` / `User` / `ExtensionField` / `Threat` / `Vulnerability`）—— 加進清單等於加一個不會被觸發的名字（AP-3），它們有端點時漂移守衛會說話；（b）**`RefCodeCounter` 刻意不接**，三條理由全部實測：兩列且第二列 `resource_id`/`after` 皆 null、多實體 scope 下 throw `UnattributableWriteError`（滾升角色的每個 create 都會失敗）、⭐ **失敗的寫入會留下稽核列**（`issueRefCode` 在自己的交易裡先跑）—— 稽核軌跡會記錄一件沒發生的事；（c）**raw query 不被稽核**（無 model 可辨識，ADR-0003 已命名）。⛔ 還有一條**限定**必須連著數字讀：全 codebase **零個 `client.*.update` / `.delete`**，15 個領域寫入**全是 create** ⇒ 「15 / 21」的正確讀法是**每個有寫入路徑的模型其 create 都被稽核**，不是「所有狀態變更類型都被稽核」 | 🟡 **部分緩解** —— ⛔ **本格刻意不再寫死分數**（審計 #6 `AD-23`，2026-08-15）。原文寫「1 / 21 → **15 / 21**」，而**同一列的內文已經寫著「新增第 16 個模型」「16 個寫入」** —— 兩個矛盾的值住在同一列的兩個欄位裡，不需要跨文件比對就看得見，而它仍活了下來。⭐ 那是 `AD-RiskTableCountManual-1` 的**第 2 次實地擊中**，且這次**分子和分母都動了**（W14 同時加一張表和一個稽核名字），所以「只改分子」也會錯。**現行覆蓋率請當場導出，兩條指令都可重跑**：分子 = `audit.module.ts` 的 `AUDITED_MODELS` 字串數；分母 = `check_entity_index.py` 印出的 `models in schema.prisma` 減 1（`AuditLog` 自身）。⚠️ **2026-08-15 導出值為 16 / 22**，寫在這裡是為了讓讀者知道量級，**不是為了讓它被維護**。⛔ **仍不得讀成已解決**：5 個模型無寫入路徑、`RefCodeCounter` 刻意不接、raw query 是洞、`update`/`delete` 路徑今天不存在故零測試覆蓋（`AD-AuditWriteOpsUntested-1`）。`07` §Security gate 對**已存在的寫入路徑**已達成。⭐ **W14 是這條緩解的第一次實戰驗證**：新增第 16 個模型時，漂移守衛在**建表而未接稽核**的狀態下**恰好 1 紅**且訊息自己指名 `Attestation` ⇒ 「下一張表忘了接」確實會被機械攔下，而不是安靜擴大敞口。⚠️ 限定不變：16 個寫入**仍全是 create**。⭐⭐ **W15 複查：本片加了三張表而敞口沒有變大 —— 這是 W02 以來第一次。** 上面那條「W02 兩張 → W04 +2 → W05 +3 → …」的鏈，每一節都是「新表沒有稽核」；W15 加了 3 張，而**沒有一張需要接** —— `GRANT SELECT` only（`migration.sql:147-149`）讓應用角色**物理上寫不進去** ⇒ 沒有可稽核的狀態變更。⛔ **這不是「還沒接」，是「按構造沒有東西要接」**，兩者對稽核員的意義完全不同。漂移守衛也正確地保持綠（`client.jurisdiction|regulation|obligation.*` 在 `apps/api/src` **零命中** ⇒ 不在 `reachable` 集合），且**它會在第一個 repository 落地時說話**。⚠️ **但這個保證的測試只覆蓋了三分之一** —— 測試 7 只斷言 INSERT 被拒（`AD-W15ConstraintSurfaceUntested-1`）：把 GRANT 改成 `SELECT, UPDATE, DELETE` 它**仍然全綠**，而一條被誤放的 `GRANT UPDATE` 產生的正是**無稽核軌跡的寫入**。⇒ 覆蓋率導出方式不變（分子 = `AUDITED_MODELS` 字串數；分母 = `check_entity_index` 的 model 數 − 1）；**2026-08-16 導出值 16 / 25**，分母 +3 而分子不變**是正確的**，不是退步 | 2026-08-16 **W16（2026-08-16）：敞口未變大，連續第二片。** 五張新表**零 repository、零寫入路徑**，故不進 `AUDITED_MODELS`；漂移守衛已用暫時 stub 逼出 1 紅並指名 `ISMSProfile`（落在 `unaudited` 側）證明它非恆真。⚠️ 但與 W15 的「`GRANT SELECT` only ⇒ 寫者被資料庫擋著」**不同**：本片四張表是 `GRANT SELECT, INSERT, UPDATE`，**資料庫沒有擋住寫入，只是今天沒有應用層路徑**。⇒ M6c 補 repository 的那一片，這五張表必須**同時**接稽核，而那一片也正是 `AD-W15ConstraintSurfaceUntested-1` 的解封點。⛔ **覆蓋率要連著限定讀**（`AD-AuditWriteOpsUntested-1`）：`AUDITED_MODELS` 實測 **16**（grep 計數）；分母**看你問哪一個** —— 已建實體 **30**（`check_entity_index`）／帶 `org_entity_id` 的表 **24**（`information_schema` 實測）。W15 記的「16 / 25」用的是前者，故本片的同義延續是 **16 / 30**；但真正該問的分母是「**有寫入路徑**的表」，而那個數今天仍是 W13 導出的 15，因為本片零 repository。 ⭐ **W17（2026-08-16）：敞口未變大，連續第三片 —— 而本片的理由是 W15 與 W16 兩種的混合，值得分開讀。** `retention_policies` 是 **`GRANT SELECT` only**（W15 型：寫者被**資料庫**擋著，「無寫入路徑可稽核」是構造保證）；`legal_holds` 是 **`GRANT SELECT, INSERT`**（W16 型：資料庫**沒有**擋住寫入，只是今天沒有應用層路徑）。⇒ M6b 補 repository 時，**只有 `legal_holds` 需要接稽核**，而 `retention_policies` 要接的前提是先放寬它的 grant —— 那本身應該是一個要被質疑的動作。⛔ **`legal_holds` 的 `GRANT INSERT` 值得單獨記一筆**：它是一張**法務扣留**表，而「誰在何時對哪批紀錄下了扣留」正是稽核第一個會問的；今天那個 INSERT 可達（app 角色有權限）而**無稽核軌跡**。緩解是它**無 `GRANT UPDATE`** ⇒ 一筆扣留下了就改不掉、解除不可表達，所以敞口是「新增無紀錄」而非「竄改無紀錄」。漂移守衛保持綠已**經閱讀 `audit-coverage.int.spec.ts:515-546` 確認**（掃 `client.<delegate>.<write>(` on `core-model/*.ts`，本片零 repository ⇒ 不進 `reachable`），**非假設**。導出方式不變；2026-08-16 實測 **16 / 32**（分母 = `check_entity_index` 的 32，分子 = `AUDITED_MODELS` 字串數 16），分母 +2 分子不變**是正確的**。 ⭐ **W18（2026-08-17）：敞口未變大，連續第四片。** `events` 與 `posture_snapshots` 皆 **`GRANT SELECT, INSERT`**（W16 / `legal_holds` 型：資料庫**沒有**擋住寫入，只是今天沒有應用層路徑），零 repository ⇒ 不進 `AUDITED_MODELS`。漂移守衛保持綠已**經閱讀** `audit-coverage.int.spec.ts:516-531` 確認（它從 `core-model/*.ts` 的 `client.<delegate>.<write>(` 呼叫點導出寫入面，本片零 repository ⇒ 不進 `reachable`），**非假設**（Day-0 D5）。⛔ **`events` 的 `GRANT INSERT` 要單獨記一筆，理由比 `legal_holds` 更直接**：這是一張**安全事件**表，而稽核第一個會問的正是「誰在何時記錄了這起事件、後來有沒有人改過」。今天那個 INSERT 可達而**無稽核軌跡**。緩解與 `legal_holds` 同形：**無 `GRANT UPDATE`** ⇒ 敞口是「新增無紀錄」而非「竄改無紀錄」，且本片的 int 測試 7 把那半釘住了。⚠️ **導出值 2026-08-17 為 16 / 34**，而這裡有一個**巧合值得寫下來以免後人誤用**：本列說分母是 `models in schema.prisma` 減 1（35 − 1 = 34），W17 那則說分母是 `check_entity_index` 的已建實體數（今天也是 34）—— **兩個不同的定義今天給出同一個數，W17 時（33−1=32 與 32）也是**。⇒ 那是巧合不是等價，`RefCodeCounter` 被 `EXCLUDED` 而 `AuditLog` 被減掉剛好抵銷。**引用時要說明用的是哪一個定義。** · ⭐⭐ **2026-08-19（W22）—— 本條的敞口第一次不是「缺席」而是「被否認」**：詳情頁對一筆**真實**風險渲染了 6 筆帶 SHA-256 hash 的稽核軌跡、兩個具名簽核人、`append-only` / `SHA-256 chained` / `Tamper-evident` / `Record locked · tamper-evident ledger active`。⇒ 在**十九張無稽核的表**之上，平台正在畫面上聲稱自己有稽核軌跡。已清空（`signOff` / `trail` / `cycles`，**保留區塊標題** —— 「平台有簽核鏈」是真的，「這筆風險有這些簽名」不是）。⚠️ **緩解不在本條而在 `AD-FixtureProseBecomesForgedEvidence-1`**：同樣的文案還活在其餘 28 個畫面上，每接一頁重演一次 |
| R5 | **設定型強制力靜默失效** —— 設定損壞時不報錯，而是安靜地什麼都不做，外層 `EXIT=0` 讀起來像通過 | 高 | 中 | 結構性解法：每個宣稱會擋住某件事的機制，必須附一個**會被它擋住**的常駐負面案例，且在 CI 裡執行。⚠️ **實例清單見 `AD-NegativeGate-1`（唯一權威）—— 本表刻意不複製計數**，因為手動維護的計數器在三處各寫一次時必然漂移（審計 AD-8）| 緩解中 —— `AD-NegativeGate-1` **刻意保持開啟**（見「已實現」表 E1 / **E4**）| 2026-08-17 ⭐ **W19 揭出一個新形狀**：E1 是守衛壞了，**E4 是根本沒有守衛而全綠讓人以為有**（25 個死控件通過全部 9/9）。⚠️ 本片新增的 `check_hover_rules` 只覆蓋 `data-hov` 一種靜默失效，**「控件有沒有行為」這一層仍無機械守衛** |
| R6 | **AI 輔助開發的一致性漂移** —— 單人 + AI 的組合缺少「另一個人會覺得奇怪」這道天然檢查 | 高 | 中 | 凍結模板作為絕對錨點（不模仿上一個 phase）；always-loaded 規則有 byte 預算機械強制；跨來源審計（`STATUS_AUDIT.md`）| 緩解中 —— 見「已實現」表 E2 / E3 | 2026-08-17 ⭐ **W19 一個 session 內 7 次**（E2 已更新）。新事實：**7 次裡 4 次是別人發現的** ⇒ 自我察覺對這個形狀無效 ⇒ `AD-ProxyMetricAsAnswer-1` 提議把守衛移到「進入交付物的數字必須由當場執行的指令產生」|
| R7 | **M0 DoD 有兩項不是本專案單方面能關閉的** —— DAST 需 VNet 內 runner；IaC 沒有標的（義務已移交 infra team）| 高 | 中 | ⛔ 不得逕行打勾或標 N/A。二選一：引用對方的掃描證據，或明記「由內部第三方營運」| **🟡 敞口變小，且本列的標題已經半錯** —— `AD-DAST-1` 仍卡外部；**`AD-IaCEvidence-1` 不再是「沒有標的」** | 2026-08-18 ⭐⭐ **W21 複查：這一列存在的前提之一被自己的交付物推翻了。** `infra/azure/provision.sh` 落地 ⇒ **本專案有 IaC 了**，`CH-010:51`「本專案不寫 IaC」不成立（後繼記錄 `CH-041`）。⇒ `AD-IaCEvidence-1` **不關閉，而是分裂成兩半**：**(a)** RIT 營運的資源仍需對方的掃描證據（**仍卡外部，本列的原始描述對這一半成立**）· **(b)** 我們自己的 IaC —— **這一半不再是外部阻塞，是我們自己的工作**。⛔ 而 (b) 落地當天就量到一個缺口：`provision.sh` **沒有任何掃描器覆蓋** —— semgrep 只掃 `apps packages scripts`、`trivy config` 不支援 shell（支援的是 Terraform / CFN / K8s / Helm / ARM / Dockerfile）。已把 `infra` 加進 semgrep 目標，**但「加了目標」不等於「產生了覆蓋」**：semgrep 只在 CI 跑、本機未安裝 ⇒ 答案在 PR 的 CI log（`AD-IaCScanCoverageUnmeasured-1`）。⇒ **M0 DoD #3 從「⛔ 結構上無標的」變成「有標的，覆蓋未量測」—— 仍不得打勾。** ⭐ **順帶：M0 DoD #5（TLS／憑證／管理埠）在同一片真的關掉了** —— 20 個 phase 以來首次有標的，三項都是**明確選的而非繼承的**（ACR admin 關閉／scoped pull token／`allowInsecure: false`），六條安全標頭對真實網址複驗。⚠️ **仍缺 CSP**（`AD-NoCspHeader-1`）。⛔ **本列的標題「兩項」現在需要改寫成「一項半」，但刻意不改** —— 改了會失去「當初為什麼把它們綁在一起」的軌跡；下一次審計若仍是這個狀態，那時再拆成兩列 |
| R8 | ⭐ **Entity Zero（guardrail 2）在 Wave 1 沒有承載體** —— 平台要以資產身分登記在自己系統內、受自己的控制約束，但那個模組要到 M4/M5 才存在 | **確定（現況）** | 中 | **本表即為過渡承載體**（見下方 §Entity Zero 遷移標記）。模組上線時把標記過的列遷進平台自己的 Risk Register | 緩解中 | 2026-08-10 |

> ### Entity Zero 遷移標記
>
> **R3 · R4 · R5 · R7** 是**平台自身**作為受管資產的風險，不只是專案交付風險。
> Risk Register 模組（M4/M5）上線時，這四條要遷入**平台自己的系統** ——
> 那次遷移本身就是 guardrail 2 的第一次證明，而且用的是**真實資料**，
> 不是種子資料（guardrail 7 禁止產生假的示範資料，這正好繞開它）。
>
> 其餘（R1 · R2 · R6 · R8）是專案交付風險，留在本表。

---

## 已實現的風險（變成事件）

| # | 風險 | 何時實現 | 實際影響 | 事後 |
|---|---|---|---|---|
| E1 | **R5 設定型強制力靜默失效** | **5 次**：W01 一個 phase 內 5 次（boundaries 六種失效皆 lint 綠 · 三個掃描 job success 卻掃 0 目標 · build 成功但產物起不到 · `helmet({xPoweredBy:false})` 對 Express 無效 · CI 跑 `test` 而非 `test:cov` 導致 45% 覆蓋率一路綠）| 每一次都是「gate 回報通過但什麼都沒檢查」。最久的一個橫跨整個 W01 | 無 postmortem（不是 bug 軌）—— 結構性解法在 `AD-NegativeGate-1`，CH-012 / CH-013 / W02 各交付負面案例 |
| E2 | **R6 漂移：拿代理指標回答需要讀內容的問題** | **6 次**（W02 一個 session 內）+ ⭐ **W19 又 7 次（同樣一個 session 內）** + ⭐⭐ **W23 又 2 次，而第 2 次升了一層**（Day 1 把 `docs/architecture.md` 依它的**角色**「薄轉址層」分類而沒讀它，漏掉一張帶 ✅ 標記的活決策表；Day 2 為寫 detector 做的**枚舉本身是窄的** —— 枚舉了「我想得到的拼法」這個**開放集合**，漏 3 種格式 + 1 個搜錯範圍 ⇒ 上線時對 9 個活的 stale marker 漏 4 個）：`.env` 未同步 → 殘留 fixture 汙染斷言 → detector 對註解開火 → `grep "^\[warn\]"` 撞 ANSI 色碼 → `super=f` 撞 `true/false` → haste 警告本機快取熱 | 其中一次**把「format clean」寫進了 commit message 與 PR 描述**，而那個 0 命中是 pattern 永不匹配造成的 | 已於 `bd107cf` 更正。第 5 次起改結構：斷言用退出碼或 SQL 述詞，不 grep 格式化輸出（`AD-GrepAssertion-1`） ⭐⭐ **W19 的 7 次裡有 4 次是別人發現的**（agent 回報衝突 ×3、我自己回頭跑加總 ×1）⇒ **自我察覺對這個形狀無效**，這是 W02 那 6 次沒有量到的新事實。已開 `AD-ProxyMetricAsAnswer-1`（P1）。|
| E3 | ⭐ **R3 隔離失敗（開發環境）** | W02 Day 2：改了 `.env.example` 但 `.env` 沒改，整輪探測以 **superuser** 連線 —— `FORCE ROW LEVEL SECURITY` 對 superuser 無效，**十二項全綠而 RLS 全程未生效** | **一列 fixture 從 SG1 被搬到了 HK1** —— 這是一次真實的跨實體寫入。發生在開發資料庫，非 production。⚠️ 若沒跑那一輪而直接寫 provider 加測試，**測試會全綠而且證明不了任何事** | 無 postmortem（開發環境）。修法：每個量測腳本與 `int-global-setup.js` **先斷言前提**（角色 `super=f bypassrls=f`）。缺口仍在應用程式啟動時 —— `AD-EnvDrift-1` |
| E4 | ⭐⭐ **R5 的新形狀：不是設定失效，是「可用性」從來沒有被任何 gate 表達過** | W19 Day 3 drive-through：**25 個按鈕無 handler、未停用、`cursor:pointer`，其中 4 個還會 hover 發亮** | 它們通過了 format · lint · type-check · build · **76 個測試** · `run_all` **9/9** —— 包含**本片自己當天新加的 hover 守衛**。⚠️ 與 E1 的差別要記著：E1 是「gate 宣稱檢查了但什麼都沒檢查」，**E4 是「沒有任何 gate 宣稱要檢查這件事」** —— 前者是守衛壞了，後者是**根本沒有守衛，而全綠讓人以為有**。⇒ drive-through 不是 gate 的補充，是**唯一**覆蓋這一層的東西 | 25 個全部處置（2 個接上變成真的能用 · 24 鈕+2 span 停用 · 3 列移除假可點外觀），複驗零違規。⛔ **沒有新增機械守衛** —— 這一層目前仍只能靠開車，這件事本身就是結論 |
| E5 | ⭐⭐ **R4 + R6 的新形狀：畫面對真實資料做出了平台做不到的陳述** | W22 Day 3 drive-through：詳情頁對一筆**真實**風險渲染簽核鏈（含具名簽核人與日期）、**6 筆帶 SHA-256 hash 的稽核軌跡**、`append-only` / `Tamper-evident` / `Record locked` | 全部是 W19 的 fixture 字串，**整頁都是樣本時無害** —— 敞口在「表頭變成真實資料」的那一刻打開，而那次改動的 diff 裡**一個字都沒碰到那些區塊**。⇒ 這不是 E4 的「沒有守衛」，是**守衛存在而方向反了**：`DemoBadge` 的存在理由是防止「樣本被當成真的」，混血畫面讓它宣稱整頁是樣本而表頭是真的。通過 lint · type-check · **484 + 95 測試** · build · `run_all` **9/9** | 已清空並在真瀏覽器覆驗（`forgedStillPresent: []`）。⛔ **第一版修正只換文字保留綠色盾牌徽章 —— 綠色盾牌不管寫什麼都讀作認證通過**。**沒有新增機械守衛**；緩解是 `AD-FixtureProseBecomesForgedEvidence-1` 要求每一片接 API 的 phase 有一個具名 `[ ]`，逐條問「這一句是對這筆記錄的陳述嗎、API 送得出來嗎」 |

| E6 | ⭐⭐ **R5 的第三種形狀：不是設定失效，是 gate 的偵測力隨它守的缺陷被修好而歸零** | W23 Day 3 變異測試：對新加的 E5 做 5 個變異，其中 **3 個是真實迴歸**（刪掉一種 marker 格式 · 授權解不出來時改用猜的 · **E5 算完但不被 `find_violations` 收集**）| 三者的 `check_status_markers` **全部回報 `OK (30 pre-doc(s), E1/E2/E3/E4/E5 clean)`**、`run_all` **全部 9/9**。⛔ `test_live_repo_is_clean` 在 Day 2 修掉 5 個真陽性之後，對其中 2 個變異**完全失明** —— 它守的東西已經不在了。⚠️ 第 3 個變異是教科書等級的 Potemkin：函式跑了、結論沒進聚合，**任何 end-to-end 觀察都看不出來** | **`run_all` 9/9 不是「某個檢查有效」的證據。** 守住射程的是**具名單元測試**，每個變異各只有 1 個會紅 —— 而那 3 個測試若被當成「太囉唆」刪掉，沒有任何 gate 會反對。⇒ `AD-GateGreenDecaysAfterFix-1`；候選結構解法：變異測試列為改 detector 時的常規動作 + `run_all` 輸出附每個 detector 的**檢查項數** |

> 這張表比上面那張更有價值：**它告訴你當初的可能性評估準不準**。
> 如果一堆「低可能性」都實現了，你的評估方式有問題，不是運氣不好。

---

## 慣例

- 每個 phase 的 `plan.md` §Risks 只放**該 phase 特有**的風險；跨 phase 的長期風險住這裡
- 風險關閉要寫**為什麼關**（緩解完成？還是條件消失？）
- Bug postmortem 若揭露新的風險型態 → 加進本表（PROCESS §4.5 步驟 10）
