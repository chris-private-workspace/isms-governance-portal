# Deferred Register — 「暫時不做」的決定

**Purpose**: 記錄**反覆出現或結構性**的 defer 決定 + **恢復條件**。
防止同一個「要不要做 X」每三個月被重新辯論一次。
**Created**: 2026-08-07
**Last Reviewed**: **2026-08-21**（W25 Day 4 —— **D004 重新評估：維持 defer，但解封條件改寫**，
原條件「撐不住 M5 負載」不可證偽。其餘各條條件未成立。
⚠️ 本檔在此之前停更 **9 天**（2026-08-12 → 2026-08-21），期間 `AD-33`/`AD-49`/`AD-55`
連續四次審計指出同一件事 —— 這次是那條漂移第一次被真的修掉，而不是又記一筆。
前次：2026-08-12 審計 **#4**，**D002 恢復**；該次審計原標 #3，2026-08-13 改號，見 `STATUS_AUDIT.md` §3.1）
**Status**: Active

> **和 BACKLOG 的分工**：BACKLOG 是「還沒做」；這裡是「**已經決定現在不做，而且知道什麼時候才做**」。
> 一個項目進了這裡，就不該再出現在 BACKLOG 的可開工區。

---

| # | 項目 | 決定日期 | 為什麼不做 | **恢復條件** | 來源 |
|---|---|---|---|---|---|
| D001 | ⭐ **資料在地化 / in-country 處理能力** | 2026-08-08 | 中國移出範圍後，**11 個在範圍管轄區沒有一個有硬性落地要求**。CLAUDE.md guardrail 8 講得比「暫緩」更重：**建了就是 AP-5**（為未來預留的抽象）。這不是省工，是拒絕一個沒有使用案例的複雜度 | **任一 OpCo 進入有硬性資料落地要求的管轄區** —— 具體觸發：中國重回範圍、或印度 DPDP 變成適用。⚠️ Legal 對 `03` §Questions for Legal 的回覆**不是**恢復條件（那只會放寬設定），除非它推翻的是拓撲本身 | [`0010`](../14-adr/0010-single-region-deployment-topology.md)（取代 `0006`）· `CH-008` |
| D002 | **雙層強制（RLS + 應用層過濾）於高風險表** —— ADR-0004 選項 C | 2026-08-09 | **不是被否決，是沒有標的**。今天只有一張業務表，第二層無法被證明抓得到第一層抓不到的東西 —— 一個不可證偽的防護層就是 AP-3 | ~~**稽核 / 證據 / 事件表出現時**（M3）。`multi-tenant-data.md:212` 已指名這三類是它的目標~~ → ✅ **已恢復 2026-08-12 → `BACKLOG.md` `AD-DualLayerHighRisk-1`**。使用者裁決（`STATUS_AUDIT.md` §2.7 AD-14）：條件是「**證據表出現**」而**非** M3 里程碑；W07 建了 `evidence`（`schema.prisma` `model Evidence`），條件成立。⚠️ 錨點原寫 `:197`（偏 15 行），一併更正 | [`0004`](../14-adr/0004-entity-scoping-enforcement.md) §Options C · `W02` |
| D003 | **法規內容訂閱（填充義務庫）** | 2026-08-07 | 已確認參數 #15：Wave 2 **建介面不填充**。內容來源與授權未定，先填會把介面設計焊死在某一家供應商的資料形狀上 | **Wave 2 且內容來源與授權皆已確定** | `10` · 已確認參數 #15 |
| D004 | **重量級 BPM workflow engine** | 2026-08-07 | ~~邊界尚未定義~~ → **2026-08-21 重新評估：維持 defer。** OQ-7 已拍板（ADR-0002），邊界也**已定義成五條可證偽的判準**。W25 實測：policy 生命週期 7 條邊、任一時刻單一作用中狀態，**沒有任何一條需要 BPM 獨有的能力**。⚠️ 解封條件因此**改寫**（原條件「撐不住 M5 負載」不可證偽 —— 沒有人定義過「撐不住」）| **改寫後（任一成立即重新評估）**：① OQ-9 答「OpCo 需要各自的簽核流程」⇒ 自建表與函式庫**都不夠** ② SLA timer / 升級進入實作（並行區域推翻判準 2）③ 出現需要 fork-join 或補償語義的 Wave 1 流程 | `decision-form.md` OQ-7（已拍板）· OQ-9（未答）· [`0002`](../14-adr/0002-workflow-engine.md) §可證偽條件 |
| D005 | **`strict: true`（PR 必須先含最新 `main` 才能 merge）** | 2026-08-10 | `strict` 防的是**並行 PR 的 semantic conflict**（兩個各自綠、合起來壞）。單人序列化開發下同時開兩個 PR 近乎不存在，而代價是每次 `main` 動了就要 rebase | **多人協作開始**（第二個 committer 出現），或**單人但開始同時開兩個以上 PR** —— 那時 `strict: false` 會從合理變成疏漏 | [`CH-015`](../03-implementation/changes/CH-015-required-status-checks.md) |

---

## 使用規則

1. **恢復條件是必填。** 「以後再說」不是恢復條件；
   「當日活躍使用者超過 1000」或「當第三個客戶提出同樣需求」才是。
2. 恢復條件成立 → 移出本表，進 BACKLOG，並在此列標 `已恢復 -> {去向}`
3. **不要刪除列** —— 被否決的想法會被重新提出，
   保留當初否決的理由比每次重新辯論便宜得多
4. 若某個 defer 決定牽涉架構取捨 → 同時寫 ADR，本表只留指針
